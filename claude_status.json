{
  "version": "0.25.0",
  "date": "2026-06-02",
  "branch": "develop (PUSHED; parity campaign — feature work cut as feature/cir-parity-claude off develop and ff-merged back per cluster). MIR fork /workspace/mir branch `develop` @ 772efeb, PINNED via repo-root MIR_COMMIT=772efeb (correspondence MIR develop<->madc develop, master<->master at parity) — madc links its libmir.a; rebuild the fork if libmir.a is stale after a container rebuild.",
  "build_status": "green",
  "backend": "madc parser -> cir_node (MC11-IR) -> c2mir -> MIR is the SOLE backend. The asmjit JIT and the Gecko parser/MIR-transpiler were BOTH removed (commits 42e9b6e + 64f44b3). There is no --backend=jit; --backend=mir aliases to cir.",
  "ir": "MC11-IR: the cir_node tree DERIVES FROM c2mir node_t (c2mir consumes the lowered C11 view) AND carries each node's originating lexed tokens + parse subtree + file/line/col (madc retains the high-level view). It is deliberately BOTH. SET IN STONE \u2014 see docs/rules/mc11-ir.md and docs/superpowers/plans/2026-05-28-cir-node-unified-ast.md.",
  "test_status": {
    "unit_tests": "green; ~100 eval/exec/compile cases skipped \u2014 the libmadc in-process compile/exec/eval surface is stubbed pending reimplementation on CIR->c2mir->MIR",
    "integration_tests": "451 passed, 12 failed, 1 flaky (testfortypedcomma flips fail<->timeout), 55 skipped. 2026-06-02 SIXTH c2mir-grind fix — __builtin_*_overflow u64 HELPER SELECTION (pr85095, madc-only, NO fork change): [gcc-torture 1557->1558 (92.5%, gap 87), +1, zero regressions, fulltest 451, SMAUG boots port 4502]. __builtin_add/sub/mul_overflow with BOTH operands unsigned 64-bit miscompiled: operands reach the runtime helper as `long long` which sign-extends a large unsigned value (2^64-18 -> -18), and overflow_helper_name (cir_builder) selected the generic `_u64` helper that signed-widens them -> wrong overflow flag/result (pr85095 f1(16,-16) gave 0 want 1). The correct `_uu64` helper (reinterprets inputs as unsigned) already existed in va_helpers.cpp but was never selected. FIX: key the `_uu64` selection on the OPERANDS' signedness, not the destination type. Both pr85095 (unsigned long operands -> _uu64) and pr91450-1 (__builtin_mul_overflow(int,int,&u64): signed operands + uint64 dest -> must stay signed-widening _u64) pass. NOTE: first attempt keyed on the uint64 DESTINATION -> regressed pr91450-1 (caught by the torture failset-diff); overflow semantics follow operand VALUES not dest type. SIX c2mir-grind fixes now (6 of 32). 2026-06-02 FIFTH c2mir-grind fix — __builtin_conjf + _Complex WIDTH CONVERSION (20020411-1, two-part; MIR fork 838b116, MIR_COMMIT bumped 8f97e4f->838b116): [gcc-torture 1556->1557 (92.4%, gap 88), +1, zero regressions, fulltest 451, SMAUG boots port 4501]. (a) madc (cir_builder): lower __builtin_conj/conjf/conjl(z) -> ~z (N_BITWISE_NOT on a complex operand = conjugate in c2mir); Tier-1 (madc owns the front end; stock c2mir has no __builtin_conj* -> fell through to a nonexistent dlsym symbol). User OK'd keeping it as a madc lowering (not raised into c2mir). (b) c2mir (fork 838b116, the real bug): _Complex float<->_Complex double conversion was mishandled at THREE sites -- N_CAST (scalar-cast of the aggregate), N_ASSIGN (block-move of LHS-size bytes from the narrower source), and the local initializer (same block-move flaw) -- all producing garbage; added complex_to_complex() (load each component at source width, cast to dest component type, store into a fresh temp) called from all three. Verified 1 -1 for cfloat<->cdouble both directions. FIVE c2mir-grind fixes now (5 of 32). 2026-06-02 FOURTH c2mir-grind fix — STATIC-LOCAL INITIALIZERS (pr53084, two-part; MIR fork 8f97e4f, MIR_COMMIT bumped 772efeb->8f97e4f): [gcc-torture 1552->1556 (92.3%, gap 89), +4 (pr53084, 20071029-1, pr124358, string-opt-17), zero regressions, fulltest 450->451 (+teststaticlocalinit), SMAUG boots port 4500]. (a) madc PARSER (the real prize, src/parser.cpp ~16499): a DEAD asmjit-era hoist — for a `static` local with an initializer the parser pushed td->initialize into tkProgram->statements and cleared it (relying on the removed JIT's TokenDecl::compile()). The CIR/c2mir pipeline never emits those hoisted file-scope statements, so it SILENTLY DROPPED EVERY scalar static-local init: `static int x=7` read 0, `static double d=2.5` read 0, `static const char *p=foo` was left null -> SIGSEGV on p[0]. Masked because madc does NOT propagate main's return to the process exit code (rc-based tests read green; only printing the values exposed it). FIX: keep td->initialize on the decl so var_decl emits it as the SPEC_DECL's constant initializer -- c2mir initializes a static ONCE at load (gcc semantics; counters/accumulators still correct). (b) c2mir (fork 8f97e4f): check_const_addr_p gated an N_STR base on curr_scope==top_scope, rejecting a COMPUTED string-literal address (foo+1) for a block-scope static; a string literal has static storage at ANY scope (C11 6.4.5p6) -> return TRUE (data emitter already handles N_STR at any scope via get_string_data). Both halves needed: c2mir alone passes the array form, every scalar static stays broken without the parser fix. 2026-06-02 THIRD c2mir-BUG FIX — zero-length array members `T x[0]` (TWO-PART; MIR fork 772efeb, MIR_COMMIT bumped 74adb6a->772efeb): [gcc-torture 1551->1552, +1 (zerolen-1), zero regressions, SMAUG boots port 4448, fulltest 450]. (a) madc PARSER: the nested/anonymous-struct member path didn't record per-dim shape and dropped a trailing `[0]`/`[]` member to a SCALAR (`char name[0]`->`char name`), so name[0] read wrong storage; now records inner_dims + passes to addMember like the top-level path (src/parser.cpp). (b) c2mir (fork 772efeb): set_type_layout skipped zero-size members (continue), leaving a GNU zero-length array at offset 0 instead of running offset; now gets current aligned offset without growing type. C99 FAM `[]` unchanged. Both confirmed: madc emits `char name[]` (was scalar), zerolen-1 passes, non-trailing `char a[0]` also correct. NOTE: the c2mir half was pushed last tick (772efeb) but pin not bumped until the madc half landed — completing the bug per user ('why half-fix a bug?'). 2026-06-02 SECOND c2mir-BUG FIX (bugs-before-features; MIR fork 74adb6a, MIR_COMMIT bumped caa6ff9->74adb6a): statement-expression ending in POST-increment/decrement [gcc-torture 1550->1551, +1 (950906-1), zero regressions, SMAUG boots port 4447, fulltest 450]. ROOT CAUSE (c2mir front-end bug, confirmed stock c2m): a stmt-expr's value is its block's last expression, but c2mir gens expression-statements value-discarded (top_gen hardcodes val_p=FALSE); post-++/-- (gen N_POST_INC/N_POST_DEC) only materialize their old value when val_p||true_label, so `({ x--; })` as a value/return/condition left an UNDEF MIR operand -> codegen crash ('unexpected operand mode ... undef'). Only post-++/-- affected (surveyed every last-expr kind: vars/binops/calls/assign/pre-++/comma/ternary all materialize). FIX (c2mir.c): N_STMTEXPR marks the block's last expression (gen_ctx stmtexpr_last_expr, saved/restored for nesting); N_EXPR gens that one in value context (val_p=TRUE). HOW TO FIND c2mir bugs (this session's method): for each torture fail, run gcc + clang + stock `c2m -ei`/`-eg`; gcc-pass+clang-pass+c2m-fail = a genuine c2mir/MIR bug (32 found in the failing set; 17 clang-also-fails = gcc-specific/UB like bitfld-3 reduced-precision-bitfield-arith, deferred). bitfld-5=inline-asm floor. 2026-06-02 FIRST c2mir-BUG FIX (bugs-before-features; user: MIR/fork work LAST, so c2mir front-end bugs first): struct/union-valued STATEMENT-EXPRESSION miscompile [gcc-torture 1549->1550, +1 (20020320-1), zero regressions, SMAUG boots port 4446, fulltest 450]. ROOT CAUSE (confirmed a c2mir bug, NOT a MIR-machine bug, NOT madc: stock `c2m -ei`/`-eg` both miscompile a minimal reducer returning 0 where gcc returns the correct diff): a stmt-expr `({ ...; obj; })` whose value is a struct returns the in-block local's LVALUE, but c2mir reuses that local's fp slot across non-overlapping sibling scopes; with two in one expr `({..A..}).x - ({..B..}).x` both deferred member loads read whichever block ran last -> 0. FIX in the fork (caa6ff9, c2mir.c N_STMTEXPR gen): when the stmt-expr type is TM_STRUCT/TM_UNION, copy the aggregate result into a fresh ALLOCA temp (same mechanism complex_temp uses) so each value has independent storage. PRE-EXISTING UPSTREAM bug (blame: c8e3c4f 'Support statement expressions', untouched by our fork) -> clean UPSTREAM candidate to vnmakarov/mir, reduces fork divergence. MIR_COMMIT bumped 1fdf44d->caa6ff9 (fork develop pushed, rebased over a concurrent README commit 4d71c80). c2mir-vs-MIR distinction: c2mir=C->MIR front-end (IR gen), MIR=the IR machine (libmir gen/interp/regalloc); the lined-up bugs are mostly c2mir front-end (tractable+upstreamable); genuine MIR-machine work (SIMD vector types, __builtin_setjmp) stays LAST. 2026-06-02 AGGREGATE-INIT CLUSTER (the last cheap front-end cluster) LANDED [develop @ ec97689, PUSHED; gcc-torture 1547->1549, +2, ZERO regressions, SMAUG boots 'Realms of Despair ready ... port 4444', ZERO compiler warnings]: (1) pr98366 `(S[]){{.b=3,..}}` array compound-literal with a STRUCT element — the array path rendered the element type via append_type_specs() which emits N_INT for any struct, so c2mir saw `int[]` and rejected the brace-init as 'excess elements in scalar initializer'; extracted the scalar path's struct/typedef/anon spec-builder into CirBuilder::append_lit_type_spec() and reused it for the array path; parser propagates the element's typedef alias to the literal. (2) strlen-4 family `A3_28 *paa[]` (array of pointer-to-typedef'd-array) — var_decl's skip_tail correction (compensating for the parser flattening a typedef's dims into v->dims) only applies in the NON-pointer path; with a pointer prefix the parser leaves those dims in the pointee (peeled into ptr_array_dims) so v->dims holds only the var's own dims; gated skip_tail on !is_ptr so the outer `[]` is no longer dropped (was misparsing paa as a scalar pointer). NOTE strlen-4 itself still hits a deeper RUNTIME bug (pointer-to-array arithmetic SIGSEGV) — its declarator family is fixed regardless. The OTHER two tests the worklist listed under this cluster (pr109938/pr109986) are SIMD floor gaps (v4si vector initializers), NOT aggregate-init. Also cleaned ALL compiler warnings to zero (FuncDef ctor -Wreorder, MADC_COMPLEX_OPS ushort -Wnarrowing x11 via explicit (T) casts, 2 unused parser locals, snprintf 'p%zu' -Wformat-truncation). NEXT TRACK (user-chosen 2026-06-02): SMALL FLOOR CLUSTERS — float->int saturating conversion, __builtin_setjmp+alloca, scalar_storage_order (smaller per-fix, less fork divergence than the ~35-test SIMD raise-MIR track). 2026-06-01 PARITY CAMPAIGN START: measured gcc.c-torture (1685 execute tests, same runner) — asmjit/master=1645 (97.6%) vs CIR=1382->1547 (91.8%) this session (+165: stmt-expr+2, ptr-array+2, bitfield+15, struct-member-type+30, aggregate-init+9, varargs+20, integer-promotion+21, builtin-libm+10, _Complex+3, nested-functions+11, attributes+4, ternary-void+voidptr-arith+8, FAM-init+3, self-ref-typedef+3, K&R-unprototyped+6, label+block-extern+2 [5ca8492], union-layout+11 + stmt-expr-nested-lastval+3 [069fb8b] + fn-as-value-proto+3 [9ac7a1b] + [14fc16c def/ref + 187b135 typedef + f9d7566 return/var/extern tag-refs; emit union_layout?N_UNION:N_STRUCT in struct_def + type_list ref + typedef_decl; fixed member aliasing/type-punning AND union-by-value param passing; recovered pr86492/pr82524/20180131-1 +5 more]). UNION-LAYOUT [14fc16c]: CirBuilder::struct_def + the general type-spec reference path hardcoded N_STRUCT, so `union U{..}` was laid out as a struct (distinct offsets) -> member aliasing/type-punning gave wrong values. Both now emit union_layout?N_UNION:N_STRUCT (tag kind must match definition+refs or c2mir errors 'kind of tag unmatched'). Recovered pr86492/pr82524 +2 more. Remaining union cluster (separate, not layout): 20180131-1 (union-by-value param + short sign-ext), bitfld-3/5/6/7 (bitfield arithmetic). Also this session: legacy cir_translate path REMOVED (madc_cir.cpp ~1800->275; one backend, no drift) + test_cir repointed at live CirBuilder + make test capped (ulimit+timeout) + scripts/resume.sh rehydration preflight (handoff STEP 0) + rule no-parallel-implementations. 2026-06-01 LABEL+BLOCK-EXTERN CLUSTER LANDED [clean squash 5ca8492, PUSHED; 1527->1529]: labels are a PREFIX on the statement they name (TokenLabel carries `labeled`; parseStatement parses it; translate_stmt emits `label: stmt` in ANY context incl if/while/for body + switch case; translate_block peels chains) — recovers pr112581-1/20111208-1 (label-as-body was dropped-with-statement -> c2mir undefined label). Block `extern int v;` rebind recovers scope-1, GATED to a PLAIN SCALAR extern + NON-OWNING Variable view (clear vfALLOC, NULL data): the owning copy double-freed SMAUG's global storage (SIGABRT) AND non-scalar in-block externs emitted types c2mir rejected with 43 'incompatible types of declarations'; aggregates fall through to the existing return NULL. PRE-EXISTING HAZARD (cost a chaotic session): the `CIR: goto and labels` UNIT test HANGS under MIR_interp (the snippets run fine under bin/madc/MIR_gen; it hangs on the reverted baseline too, so NOT from 5ca8492) -> `make fulltest` ABORTS early before integration tests; use `bash scripts/run_tests.sh` for the integration gate, and ALWAYS wrap test runs in `( ulimit -t N; timeout M ... )` because a hung test_cir pegs a QNAP core (TRACKED: fix the interp goto/label hang). LESSON: orphaned background tasks SURVIVE /compact — a pre-compact verify-loop (b46iicx9x) + a cluster subagent (ac76ba07f7d1e8bd7) kept relaunching fulltest->hanging test_cir AND the subagent committed to the branch while the coordinator hand-edited it (commit/revert/reapply collision); ON RESUME ps-grep+kill survivors and check in-flight background tasks BEFORE git ops. K&R [ea61546]: NOW LANDED (was reverted earlier) — emit (void) only for is_void_params, bare K&R () unprototyped consistently across func_def/func_proto/fnptr_func_node + typed-zero bare return; SMAUG boots clean (coordinator-verified). CAVEAT: the earlier get_color crash root cause is NOT definitively pinned (the re-fix subagent's 'fork missing c40ed46' theory is DISPROVEN by the commit timeline; likely the FAM/self-ref-typedef commits incidentally fixed it) — re-soak SMAUG on any future prototype-emission change. FAM [ef0f1e8]: flexible-array member emits ARR(N_IGNORE) not ARR(0) so c2mir sizes the static object from its initializer. self-ref-typedef [137d230]: combined `typedef struct Tag{...} Tag;` recorded as the body-definition point (struct_body flag) so an earlier forward `typedef struct Tag *p;` emits STRUCT(tag,IGNORE) not the hoisted body; + `array`/`string` spelled by contextual_identifier_name. 14 clusters LANDED this session (K&R deferred). ternary/void* [24a5ca2]: void-returning libc dlsym protos (abort/exit/free/... -> dtVOID in dynamic_symbol_fallback_return_type, fixes void/comma-expr ternary 'incompatible types in cond-expr') + void* arithmetic lowered as char* (size 1, GNU C). K&R cluster ATTEMPTED+REVERTED (unprototyped-() emission regressed SMAUG get_color; patch tmp/knr-cluster-016eb2f.patch; deferred — see docs/parity/root-cause-worklist.md). LESSON: a subagent FALSELY reported SMAUG boot for K&R; coordinator MUST re-run the SMAUG soak (grep the literal 'ready at' line) before crediting. attributes [madc 7f597fd + fork 1fdf44d]: __attribute__((alias("t"))) lowered as C-identity (var_emit_name resolves the alias chain to the target symbol; MIR has no alias primitive); __attribute__((aligned(N))) member/tag alignment recorded on DataDefSTRUCT + emitted as _Alignas(N), c2mir set_type_layout now honors decl_spec.align (max with type_align). aligned(N>16) BLOCKED at MIR floor (allocator caps at 16-byte; Tier-3 raise). nested-functions [c65a57d]: GNU nested fns lowered via the lambda capture-by-reference machinery (each used enclosing var -> hidden T* param, body uses (*v), call sites forward &v); removed the old single-__env hidden param from BOTH nested-fns and lambdas. Recovered testnestedvarargs/testnestedvlaparam/testvlaglobalboundnested too. Deferred: a CAPTURING nested fn passed as a bare fn-ptr (GCC trampoline) -> clean error, not miscompile. _Complex [madc 5df19ef + MIR fork e0872fd]: emit NATIVE c2mir _Complex (not struct shim); fork fixed float-complex SysV ABI classify (MIR_T_D eightbyte), complex-return frame reservation, scalar->complex local init block-move, complex-constant node-leaf walk; madc lowers imaginary/f-suffix literals to N_CF/N_CD/N_CLD + registers complex builtin prototypes. Recovered the testcomplex* integration family + un-skipped testbuiltincomplexparts. REMAINING 3 torture (complex-6 int-complex, 20020411-1 cross-width, 20020227-1 unaligned-member) fail in stock c2m too = deeper c2mir codegen, scoped. builtin/libm [6c3ff17]: libm fns declared with real double/float return in include/madc/math.h (were dlsym-fallback long -> FP result read from int reg); __builtin_*_overflow width/signedness dispatch from dest type (va_helpers width helpers were dead); unary -0.0 sign preserved (was lowered 0-x); __builtin_constant_p + if(0) dead-branch DCE. integer-promotion [f2e9e83]: integer literals now lower with their lexer-recorded signedness/width (new CirBuilder::integer_typed picks N_I/N_U/N_L/N_UL from the literal's DataDef, was always-signed) so c2mir runs usual-arithmetic conversions from the right type (+testgccconversionprefix/testgccu32todouble/testuint32realcoerce). VARARGS fix included MIR FORK ABI fixes (/workspace/mir @ c40ed46: SysV register-save-area boundary off-by-one + SSE fp_offset + mixed-class struct va_arg slot stride) — LOCAL-ONLY, must push to origin/mir before any release relies on it (build.md). Plus a parser scoping fix (madc @ 33b0975: inner-scope redeclaration creates a fresh shadowing local instead of mutating the parent-scope symbol). The 265 asmjit-pass/CIR-fail regressions (4 compile + 260 runtime mislowering + 1 timeout) triaged (5 parallel agents) into ~16 ROOT-CAUSE CLUSTERS (docs/parity/root-cause-worklist.md + regression lists; asmjit reference worktree built at /workspace/madc-asmjit): ~15 fixable front-end clusters (~180 tests; top: struct/aggregate member-type resolution ~35, bitfield ~30, varargs ~21, aggregate-init nesting ~17) + ~45 deferred floor gaps (SIMD ~30/VLA/asm/wchar/int128). Fixes landed this session: (a) MADC_VERSION const-char* macro replacing the stale std::string `version` global, value threaded from ../VERSION via -DMADC_VERSION_STR (single source of truth); (b) STRING LITERALS ARE const char* (addLiteral->ddCHARptr, was wrongly the std::string CLASS ddSTRING — a falsely-'done' fix; dtSTRING tag was retired but ddSTRING-instance+addLiteral still mistyped literals) + completed const-char* ingestion (build_call_args resolves lambda/fn-ptr callee via call_target_funcdef; is_string_operator_plus char* RHS uses type()==dtCHARptr since rawtype() strips the +10000 ptr offset) [commit 22028b0]; (c) pointer-to-array declarators T(*p)[N] — un-shimmed the param path (was getPointerType approx dropping [N]) + fixed var_decl CArray render [f7f3f7e, +2]; (d) GNU statement-expressions ({...}) -> N_STMTEXPR [680f968, +2]; (e) BITFIELD load/store: member_node now carries the recorded :width into c2mir's N_MEMBER bit-field slot (was N_IGNORE) so c2mir does native mask/sign-zero-extend [095d633, +15 torture, +1 integration]. NEXT CLUSTERS (biggest leverage): struct/aggregate member-type resolution (~35: anon/inline/nested structs, constexpr array dims, incomplete/flexible members), then varargs (~21), aggregate-init nesting (~17). Method per cluster: reduce->gcc/asmjit-diff->deepest-layer fix->fulltest->torture re-run->SMAUG soak->commit. SMAUG boots + serpent passes throughout. 2026-06-01 STRING-LITERAL TYPING + MADC_VERSION (432->433, +1 teststrlitaddrsubscriptglobal, zero real regressions, gcc-canon + fulltest-gated + SMAUG boot & serpent-combat PASS, commit 22028b0): a bare string literal is now `const char *` (addLiteral->ddCHARptr), NOT the std::string CLASS. dtSTRING/dtSTRINGref (DataType enum tags) were retired but ddSTRING (the std::string DataDefSTRING *instance*) remained and addLiteral still typed every literal as it -> `*\"x\"` / `MADC_VERSION[0]` failed 'cannot dereference non-pointer'. std::string ingests a literal via its const-char* ctor/operator+/operator= (the CLASS path), not the literal's own type. Completed const-char* ingestion at 2 sites: build_call_args resolves the callee via call_target_funcdef so INDIRECT (lambda/fn-ptr DataDefFPTR->target) calls materialize a literal arg into a temp std::string for string-by-value params (was raw-pass SIGSEGV, testlambda); is_string_operator_plus char* RHS test fixed (rawtype() strips the +10000 ptr offset so rawtype()==dtCHARptr was DEAD code; use type()==dtCHARptr) so `s+\"lit\"` binds string_concat (teststringplus). Also: built-in `version` std::string -> MADC_VERSION const-char* macro (lexer define_map; value threaded from ../VERSION via -DMADC_VERSION_STR in Makefile DEFINES = single source of truth, tracks release 0.25.0). 2026-06-01 C-STABILITY PIVOT (410->428, zero regressions, each gcc-compared+fulltest-gated; SMAUG boots + serpent-combat run PASSES): cleared the entire CIR 'unhandled expression' cluster by adding translate_* cases in cir_builder.cpp — compound literals (T){...} (TokenStructLit, +empty-struct/anon-member fixes; commit 4d7a973), block-scope typedefs (TokenTypedefDecl; 34e772d) [NOTE: that commit ALSO rewrote typedef_decl's array peel which regressed SMAUG get_color -> bisected+reverted in 0bcb446; keep the original peel_carray_dims], __real__/__imag__ (TokenComplexPart->N_REALPART/IMAGPART; 2e5d7d0), computed goto &&label->N_LABEL_ADDR + goto*e->N_INDIRECT_GOTO (c2cbae4), fn-ptr arrays T(*a[N])(args) [var_decl passes v->dims as lead_dims + globals emitted AFTER fn prototypes so file-scope fn-ptr tables resolve] (794a51b). REMAINING fails: a VLA group = c2mir FLOOR GAP (no VLA support; Track 1.6 raise-MIR, SKIP) + fixable c2mir-check-error groups (test4/testdefer/testversion/testloop/testmultiret/teststruct2/testfstream/testcin/complex-arith/testlambda/teststaticlocalinit) to triage next. 2026-06-01 — P1.1b EXCEPTIONS NOW RUN ON MIR-JIT (the C-stability pivot's first item; 410->415, 0 regressions, failset identical to baseline modulo the flaky test): the long-standing exception/dtor-unwind SIGBUS was NOT 'setjmp returns-twice' (that recorded theory was disproven — it crashed at -O0 too). REAL root cause: cir_builder builds the cir_node tree BOTTOM-UP, so a nested N_BLOCK gets a SMALLER c2mir uid than its parent; c2mir's process_func_decls_for_allocation/decl_cmp sorted auto-locals by scope->uid (assuming nested=bigger uid, true only for its recursive-descent text parser) and OVERLAPPED a try-body std::string onto the live try-context jmp_buf -> the string ctor clobbered the saved registers -> longjmp restored a garbage frame pointer -> SIGBUS in the JIT epilogue. FIX (deepest correct layer, MIR fork 01f999b): decl_cmp now orders by ACTUAL scope depth (parent chain), a strict refinement (identical for parser-built trees; full c2mir suite incl. bootstrap stays green) that makes c2mir robust to ANY externally-built cir_node tree — serving the polyglot/external-tree vision. madc f54df38 removed the 5 .mir_skip files; testrethrow + testexcept_dtor_{string,order,nested,rethrow} all pass on the default MIR backend. NOTE the emit-C path (--emit=c11) is a SEPARATE renderer, NOT the c2mir feed (guard comment added to cir_emit_c.cpp). 2026-05-31 SESSION 2 CONT'D-2 — P2.14 dtSTRING ELIMINATED (grep-verified 0): the resilient dtSTRING/dtSTRINGref legacy tags (a crutch requested removed 1+ week ago, repeatedly partial-then-falsely-'done') are GONE — std::string now carries the generic dtRESERVED tag + is recognized purely by class identity like vector/map/set; typespec_t lets the builtin-registration ABI name string by DataDef*; MadValueKind gives the PHP-array union its own discriminator; canonical_string_class shim removed; P2.9 type-code overlap dissolved (commits ddcaa80..4c049cc, 219->0, 0 regressions). Also landed: P2.8b string& through operator dispatch, P1.1c exception RAII dtor-unwind (front-end; MIR setjmp-returns-twice bug P1.1b is the fork/C-stability work), P2.1b operator()/->/postfix++, P2.5b class default-private, P2.5c method access, P2.6 upcast, P2.7 range-for-by-ref, P1.3b this-binding. LESSON: 'done' for a removal = grep=0 + gate, coordinator-verified. 394 passed, 52 failed, 1 flaky (testfortypedcomma flips fail<->timeout), 58 skipped (CIR sole backend, branch feature/cir-stdstring-claude; unit tests green). 2026-05-31 SESSION 2 CONT'D — P1/P2 C++ FEATURES + VISION CRYSTALLIZED (386->394, 0 regressions, every fix invariant-checked + g++-compared + fulltest-gated): P1.1 exceptions Phase A (scalar try/catch/throw via the LIVE src/exception_runtime.cpp SJLJ runtime, 2444f85; follow-ups P1.1b MIR-nested-setjmp [rethrow .mir_skip'd, gcc-correct lowering, MIR-backend bug] + P1.1c dtor-unwind deferred); P1.2 lambda return-type deduction (01870da, deduce_expr_type helper); P1.3a register class self-type early so user-class methods/operators using the self type parse (e5ca90f) + scoped the tkAdd guard so USER operator+ dispatches through the same class_operator_call path as std:: (unified machinery payoff, g++-verified); P2.1 operator-set completeness (binary table + unary + prefix inc/dec dispatch, 707a5a4+b68dfe3; operator()/operator->/postfix++/same-name-overload are parser gaps P2.1b); P2.2 enum class scoped enums std-gated (f6ecd3f); P2.3 general auto deduction std-gated (3aac7af, reuses deduce_expr_type). ***ARCHITECTURE CRYSTALLIZED*** docs/plans/madc-vision-and-invariants.md (referenced from AGENTS.md): madc as a POLYGLOT TRANSPILER (read source language/std X, emit target Y, via the one cir_node/MC11-IR — chosen because most languages are implemented in C/C++, so a faithful C/C++ AST is their universal substrate; borrowed-language features are NATIVE in libmadc, not calls into those runtimes). 8 INVARIANTS (I1-I8) + a 'does this block the vision?' checklist now gate every change: one IR (dual-view), C/C++ floor, the --std=/LanguageStd enum on BOTH ends (input dialect gating + output/c2mir-target std) never hardcoded, every feature gated via a keyword/feature->standard REGISTRY (P2.11 keystone), one emitter, no special-casing, honest gating. Roadmap docs/plans/cpp-support.md: principle #6 (two-axis std) + P2.11 registry/gating + P2.12 de-hardcode c2mir target + P2.13 standards conversion + P3 polyglot + P2.6-P2.10 follow-ups. SEQUENCING (project_sequencing_cpp_then_c): finish core C++ (P2.4 const-enforce, P2.5 access-control next) THEN the C-stability pass (Track 1.3 ~52 fails + the std-subsystem/registry/gating). 2026-05-31 SESSION 2 — C++ CLASS-COMPLIANCE PUSH (376->386, 0 regressions, every fix diff-reviewed vs g++ + fulltest-gated): completed std::string's operator set (operator[] d565f9b, operator+ a4d76c0, == != < > <= >= cac3422 — all via the real-class machinery, mangled-libstdc++ or extern-C wrappers string_equals/string_concat/string_lt..ge) + the subscript-member KEYSTONE (v[i].method() be5b063 dot, v[i]->method() 811ba55 arrow) which de-workaround'd the map/set headers. Then cleared the P0 CORRECTNESS-CRASH TIER (now COMPLETE): P0.1 class by-value return garbage -> generalized the __retbuf struct-return ABI to non-trivial user classes (2839b56+7f51cf8); P0.2 class-reference param member access + a latent ref_params/__this alignment bug (82fbc92); P0.3 virtual-dispatch SIGSEGV = vptr never inited in a ctor-less `new` (edff1f7); P0.4 range-for over a raw C array hit the MadArray fallback -> SIGSEGV, added translate_foreach_carray (c23fcdd); P0.5 copy-assignment double-free -> synthesized memberwise operator= for non-trivial classes (84a839e); P0.6 arr[i]->method() arrow-chain (811ba55); P0.7 vector<T*> multi-element crash = double-pointer struct-member rendered as `int *data` (4-byte stride), fixed member_node to peel all ptr levels + emit dd_ptr_depth stars (50775a8). HYGIENE sweep removed dead dtVECTOR/MAP/SET/LIST tags+DataDefVECTOR/MAP classes+TokenOSTREAM orphan (90c1ef3/f4dc30a/bc974a2). ***NEW AUTHORITATIVE PLAN*** docs/plans/cpp-support.md rewritten as the C++23/C23 COMPLIANCE ROADMAP (north star + honest status table + prioritized P0->P1->P2; supersedes the stale asmjit-era 'compliance is not the goal' doc). P0 COMPLETE; NEXT = P1 exceptions (SJLJ runtime src/exception_runtime.cpp is already LIVE; curated cruft-free reference docs/plans/refs/exceptions-sjlj.md + the c2mir cleanup-in-stmt-expr gotcha) then lambdas; open follow-ups P2.6 (derived->base upcast warning) P2.7 (range-for by ref) P2.8 (tighten foreach MadArray catch-all; VLA range-for still crashes) P2.9 (DataDefPTR double-ptr vs ref type-code overlap). Prior milestone: std::-types-as-real-classes refactor — std::string BEHAVES as a real class (done, works) BUT NOT fully de-special-cased: the dtSTRING/dtSTRINGref TAGS remain (std::string recognized by a special tag at ~130 sites, UNLIKE vector/map/set which were fully de-tagged). 'COMPLETE' was overstated (behavioral, not full de-special-casing); completing it = P2.14 (retire dtSTRING tag, recognize by class-identity) — dissolves P2.9 + the canonical_string_class/dd_ptr_depth shims. std::string is now a real C++ class (decl/ctor/dtor/methods/operators/stream/members/pointers/return-by-value via the class model + mangled libstdc++ symbols bound through FuncDef::emit_symbol; struct string sized from sizeof(std::string)). std::vector/map/set are real #include-defined std:: templates instantiated through the class model. REMOVED: the vector/map/set/list keywords, the tkSTRING token, ns_stl.cpp, and the dtSTRING special-case lowering. vector<string> works; element dtors via __destroy; operator==/!= via string_equals wrapper. Plan: docs/superpowers/plans/2026-05-31-stdtypes-as-real-classes.md; restart guide: docs/superpowers/plans/2026-05-31-RESTART-HANDOFF.md.",
    "smaug_compile": "SMAUG-FIRST (priority #1). RUN-PATH (cir_node->c2mir): `cd /workspace/MadSMAUG/src; timeout 90 /workspace/madc/bin/madc SMAUG.mad 2>err`. CIR validity gate OPEN (0 untranslatable, since 7bbcd57 va_arg). The 85K tree reaches c2mir's checker. 2026-05-30: 159 -> 25 check errors via four type-erasure correctness fixes (all the same theme: madc was erasing array/pointer member/param types so c2mir rejected the malformed tree \u2014 NOT user errors): (1) 5221603 char-ptr-array string-init (is_char_array_element_type used rawtype, stripping pointer-ness) cleared 96 'excess elements'; (2) b858959 forward-typedef struct completion dropped member_dims, cleared 20 'subscripted value' on 2D members; (3) 667e9bd array-of-pointer struct members emitted (*m)[N] instead of *m[N], cleared 13 'assignment of incompatible value'; (4) d7ba8c2 array-parameter decay (char map[50][50]->char(*map)[50]) cleared 5 'subscripted value' on params. See docs/c2mir-error-catalog.md for the pre-catch strategy (c2mir is the reused semantic checker on madc's own tree w/ original source positions; most errors are madc bugs to fix, not user errors to pre-catch). 2026-05-30 cont'd: 25->4 via more fixes (df2cae4 extern preserves real type [bool/array]: an extern is a forward ref, emit the symbol's type not a re-derived one; f49e6b0 emit pre-case declarations in switch bodies; 218a6c5 __builtin_va_start takes 1 arg; 719744b varargs System V ABI). 2026-05-30 FINAL: SMAUG is c2mir-CHECK-CLEAN (0 errors, was 159 at session start). Last 4 cleared: a644505 strcpy-family return char* (ternary char*-vs-long) + return<void-expr> lowering; the 'repeated declaration' was an EMPTY-NAMED param (c2mir scope=N_FUNC) \u2014 root cause: param_decl emitted unnamed params as N_SPEC_DECL(IGNORE-id); c2m uses N_TYPE for abstract params. d2fecaf routes unnamed params through N_TYPE (also fixed a c2mir MIR-GEN CRASH on 2+ param fn-ptrs, and made fn-ptr params run: apply(op_add,20,22)==42). Also cleaned SMAUG.mad (removed bring-up artifacts: custom main, probes, services.c dup-main; comm.c's main is the entry). The fill_in_control_page MIR-gen 'wrong type memory' was a `.`-vs-`->` bug (fb3027b): `m_data[i].field` (m_data a pointer -> m_data[i] a struct VALUE) was emitted `m_data[i]->field`; gcc rejects it, c2mir-check is lenient, MIR-gen miscompiled it. Fixed by keying `.`/`->` off the subscript RESULT type (member translation ~cir_builder.cpp:1357). Confirmed via stock `c2m` on emitted C (and `-O0` unchanged -> it was a MIR-gen verify error, not optimizer). Added a guard rejecting `->` on a plain non-pointer struct value (arrays decay to pointers -> still OK). New tests/fidelity/structmemberaccess.c. -O<n> flag added (51937b4; default 1, MIR-gen level; level>=2 has a known SSA bug). 2026-05-30: MIR-LINK `import of undefined item spec_fun` ROOT-CAUSED + call-site fixed (7924e42). SMAUG uses fn-ptr-typedef members: `typedef bool SPEC_FUN(CHAR_DATA*); SPEC_FUN *spec_fun;` called `(*ch->spec_fun)(ch)`. cir_builder.cpp:1535 ignored TokenCallFunc::src_node (the member-access callee expr the parser sets for fn-ptr-member calls) and emitted a bare `spec_fun(ch)` \u2014 c2mir-check tolerated it as an implicit decl, MIR-link could not resolve it. Now emits `ch->spec_fun(ch)` (indirect, via src_node). This UNMASKED a pre-existing type gap: the prior `0 check errors` was illusory. SMAUG now = 35 honest check errors, all `called object is not a function or function pointer`/`invalid type argument of unary *` at the fn-ptr-member call sites (special.c/update.c/interp.c/build.c) \u2014 because fn-ptr-typedef members are still emitted as `long`. DataDefFPTR RETAINS the full signature in `->target` (FuncDef* w/ returns+params); the emitter just ignores it. NEXT (the deferred fn-ptr-typedef fix, finally on the critical path): (a) PARSER records the source `*` on fn-ptr-typedef var/member/param/return decls (wrap in DataDefPTR so explicit_star_count != 0; currently the `*` is dropped because a fn-ptr typedef already looks pointer-ish) \u2014 Task #5 prereq; (b) typedef/member/return EMITTERS render via fnptr_decl_pieces from `->target` + stars (unstash stash@{0} `WIP fnptr typedef+member emitter`); `DECLARE_SPEC_FUN`/`DECLARE_DO_FUN` (`SPEC_FUN fun` = a plain func decl) must still emit `bool fun(CHAR_DATA*)`. Reducer: tmp/specfun.c via `madc --emit=c11` (hidden flag). FIXED 2026-05-30 (49b79a1): unstashed+reconciled the fn-ptr-typedef emitter (DataDefFPTR.ptr_syntax Form1/Form2; fnptr_decl_pieces emit_pointer flag; typedef_decl/member_node/var_decl/param_decl render from ->target signature; fnptr_alias_stars). PARSER prereq solved without touching var.type (the expression parser's fn-ptr-call detection keys on var.type being DataDefFPTR): the implicit pointer star for a Form-1 fn-ptr-typedef use is added at EMIT time in explicit_star_count via fnptr_alias_stars. Plus the src_node call-site fix (7924e42). SMAUG 35->0 check errors; integration 316->322 (+6). THEN MIR-link surfaced `import of undefined item help_greeting` (tentative-def-vs-extern): a file-scope `char *help_greeting;` def + redundant `extern char *help_greeting;` share ONE Variable (no global shadowing), and vfEXTERN was set at the call site so the LAST-parsed decl won -> both emitted `extern` -> no definition. FIXED 2026-05-30 (62577a8): vfEXTERN is now set ONLY at variable creation in addVariable (never re-set on an existing var); the call site keeps only the converse definition/promotion logic. ***SMAUG NOW BOOTS*** through cir_node->c2mir->MIR->JIT: prints 'Booting Database', 'Boot Log', 'Loading commands' then exits 0 (needs its area/ data dir + a run cwd to proceed; runtime correctness is the new phase). Stack: lexer -> parser -> cir_node -> c2mir CHECK [0] -> MIR-GEN [ok] -> MIR-LINK [ok] -> JIT [BOOTS]. KNOWN PRE-EXISTING FLAKY TEST: tests/testfortypedcomma.mad \u2014 the 2nd declarator in a multi-declarator for-init (`for(int a=0,b=4;...)`) is left UNINITIALIZED (reads varying garbage each run), so it times out nondeterministically; reproducible on clean HEAD, unrelated to the fn-ptr/extern work. Worth a dedicated fix. Memory project_smaug_fnptr_and_159; KG Gap nodes mir_link_undefined_spec_fun + (new) mir_link_undefined_help_greeting. *** 2026-05-30 SMAUG IS OPERATIONAL: boots END-TO-END through cir_node->c2mir->MIR->JIT and comes up as a LIVE SERVER -- 'Realms of Despair ready at address madc-dev on port 4000.' Run from runtime/area: cd /workspace/MadSMAUG/runtime/area; /workspace/madc/bin/madc /workspace/MadSMAUG/src/SMAUG.mad 4000 -- boots fully then enters game_loop (a timeout-kill / exit 124 is the running server, NOT a crash). Unmodified SMAUG source. Two final fixes after the help_greeting one: (1) 2fbe5f0 VARARGS lowered directly to the c2mir intrinsic (va_start(ap,last)->__builtin_va_start(ap)) instead of the non-standard master-__va_args + struct-copy, which mis-set reg_save_area in large frames (SMAUG's bug() segfaulted in vsprintf); the lexer __builtin_va_start macro was dropped, parser registers it as a builtin (NULL ptr), cir skips referenced_funcs for __builtin_va_* so no shadowing proto. (2) 4011d95 SWITCH DEFAULT bug fixed (was project_cir_switch_default_bug): translate_switch only emitted the separate default when default_index<0 (parser always sets >=0), so EVERY switch dropped its default -> mprog_read_programs mis-dispatched speech_prog and exit(1)'d; now emits default at its recorded default_index. fulltest 325 pass, 0 timeouts. NEXT PHASE: runtime correctness/gameplay (connect a client to port 4000, exercise commands) + the data warnings during boot (duplicate help keywords, missing optional .dat files) are SMAUG's own, not madc bugs. *** 2026-05-30 GAMEPLAY: character creation + world navigation + COMBAT all work. Reproduced+fixed the 'serpent crash' (entering Newgate room 109, the serpent attacks): (1) feat 25c731a JIT-symbolizing crash handler (madc_jit_symbolize in madc_cir.cpp maps a faulting addr to MIR func via machine_code..+len; crash handler in madc.cpp prints `func+0xoff [JIT]`) \u2014 pinpointed the crash to learn_from_failure. (2) fix ac16d07 ROOT CAUSE = strcmp family declared as the long fallback (extern long strcmp()) instead of int; libc returns int in eax, so a negative result read as 64-bit long becomes huge -> SMAUG's bsearch_skill_exact `strcmp(name,x)<1` always took the wrong branch -> skill_lookup failed for 79 skills -> every combat gsn (dodge/parry/second_attack/dual_wield) unassigned -> learn_from_failure(ch, bad gsn) deref skill_table[bad] = SIGSEGV at 0x1132. Declared strcmp/strncmp/memcmp/strcasecmp as int in embedded string.h. ASSIGN_GSN failures 79->0; serpent fight survives. (Regression \u2014 was correct a month+ ago.) fulltest 325 pass/95 fail (+1)/1 flaky-timeout. Repro: cd /workspace/MadSMAUG/runtime/area; madc SMAUG.mad <port>; connect, `new`, create char, down, 2 north, say lighted-path phrase, 2 north into room 109. Scripts in /workspace/madc/tmp (serpent_full.py). NOTE harness tmpfs fills if a run_in_background server's game_loop streams to its task pipe \u2014 redirect server stdout to /workspace/madc/tmp and kill it promptly. Watch for OTHER int-returning libc fns hitting the long fallback (atoi/isdigit/etc) \u2014 same sign/width bug class; declare as needed. Debugging tip: c2mir's 'repeated declaration %s' (c2mir.c:6697) prints the symbol + scope; a temp fprintf of POS(def_node)+scope->code pinpoints empty-named/no-position synthetic nodes. FN-PTR STATUS: scalar fn-ptr vars+params declarators LANDED (2ec34ff) \u2014 spiral-rule renderer + fnptr_decl_pieces build `ret (*name)(params)`; fnptr.c reducer FIDELITY-OK. The fn-ptr TYPEDEF + struct-MEMBER emitter fix is STASHED ('WIP fnptr typedef+member emitter') because it regresses SMAUG (159->176): root cause is a PARSER gap \u2014 `DO_FUN *p` and `DO_FUN g` collapse to the same bare DataDefFPTR (0 recoverable stars), so the emitter cannot tell pointer-use (DO_FUN *do_fun member, need *) from function-decl-use (DECLARE_DO_FUN `DO_FUN do_north`, no *). Prerequisite = Task #5 (parser records source `*` on fn-ptr-typedef var/param decls); then unstash + re-gate. Stack: SMAUG.mad -> lexer -> parser -> cir_node OK [0 untranslatable] -> c2mir [159 check errors, 96 = 2D-array init] -> (then MIR -> JIT)."
  },
  "phases": {
    "language_core_phase_1_to_6": "complete (achieved on the now-removed asmjit backend; being re-established on CIR)",
    "phase_4_2_libmadc_api": "in_progress (security policy / structured diagnostics / engine-owned IO landed; in-process compile/exec/eval stubbed)",
    "cir_coverage": "ACTIVE \u2014 376 passed (Track 1.3, parity-to-master gate). std::string + std::vector/map/set now real class/template types (legacy shortcuts retired).",
    "gecko_parser_and_transpiler": "REMOVED (42e9b6e)",
    "asmjit_jit_codegen": "REMOVED (64f44b3)"
  },
  "deferred": [
    "eval/exec + REPL (mode-4): reimplement on CIR->c2mir->MIR (new piece = a madc_cir_execute variant returning the computed value). ~100 unit tests skipped as the future spec. Task #14.",
    "AOT native object/executable: low priority. Near-term native builds via emit-.c + an external compiler. save_object/save_executable stubbed with signatures kept for a later MIR-based revisit."
  ],
  "remaining_todo": [
    "std::-types follow-ups (see RESTART-HANDOFF step 4): 4A COMPLETE OPERATOR OVERLOADING (user priority) \u2014 bind std::string operator[] (s[i] fails today) and operator+ (a+b fails); extend binop_overload_symbol to the full operator set + add unary/()/-> dispatch; all function/lookup-based, overload ONLY when the class declares the operator (never by default). 4B three parser gaps (method/operator on a subscript element [keystone], pseudo-destructor (data+i)->~T(), unqualified sibling-method call in a method) \u2014 grammar risk, do deliberately.",
    "PRIMARY: drive the ~95 CIR integration failures to parity with master (Track 1.3) \u2014 this is the gate for promoting develop->master. Re-measure fresh (make -C src fulltest).",
    "SMAUG gameplay: continue past the serpent (kill it \u2014 its death_prog mpoload/mpechoat exercises mud_prog.c); exercise more commands over a port connection.",
    "Audit other signed-int-returning libc fns on the long fallback (atoi, etc.) and declare them int in embedded headers (same class as the strcmp sign bug ac16d07). See .claude/rules/embedded-headers.md.",
    "Fix flaky tests/testfortypedcomma.mad: 2nd declarator in a multi-declarator for-init (for(int a=0,b=4;...)) is left uninitialized (reads garbage; flaky timeout). Pre-existing, reproducible on clean HEAD.",
    "Write the cir_node -> .c / .mc11 renderer (single emitter; render target = the same --std= language enum: C11, MC11, C++, madc) + the gcc -S -fverbose-asm fidelity gate + cir_node-vs-`c2m -d` differential, to make the worklist mechanical.",
    "Debug/REPL tier (hybrid seam, ADR 0001): extend the JIT-symbolizing crash handler (25c731a) toward a madc-native debugger on MIR interp.",
    "Reimplement eval/exec + REPL on MIR (deferred). AOT via emit-C + external compiler (deferred).",
    "Backend trajectory is settled \u2014 see docs/adr/0001-cir-c2mir-backend.md and .claude/rules/backend-strategy.md. Don't re-litigate. Lowering-vs-raising decision rule: .claude/rules/lowering-vs-raising.md.",
    "SIMD (Track 1.6, KG Decision{simd_raise_mir_upstream}): add a minimal generic-vector extension to MIR (types+insns+per-target codegen+interp+ABI) and a c2mir vector_size front-end, DESIGNED FOR UPSTREAM to vnmakarov/mir (enables MIR's WASM->MIR future; all 5 targets have vector ISAs; lifts ~11 deferred SIMD torture tests). Interim: scalarize for JIT + emit-C->gcc/clang. TODO: write the full multi-phase plan doc docs/plans/2026-05-30-simd-mir-c2mir.md (deferred \u2014 context budget; phasing sketched in the ROADMAP Track 1.6 note + the KG node)."
  ],
  "known_issues": [
    "~95 CIR integration failures = the coverage worklist (classes: unhandled_expr / c2mir_rejected / runtime_mismatch) and the parity-to-master gate. Re-measure fresh per session.",
    "tests/testfortypedcomma.mad flaky: uninitialized 2nd declarator in a multi-declarator for-init -> reads varying garbage -> nondeterministic timeout. Reproducible on clean HEAD; unrelated to the fn-ptr/extern/strcmp work.",
    "libmadc in-process compile/exec/eval stubbed (asmjit-era machinery removed); ~100 unit tests skipped until CIR reimplementation."
  ],
  "canonical_refs": {
    "backend_decision_adr": "docs/adr/0001-cir-c2mir-backend.md (+ .claude/rules/backend-strategy.md; KG Decision{cir_c2mir_backend_adr}) \u2014 SETTLED, don't re-litigate",
    "ir_definition_rule": "docs/rules/mc11-ir.md (bare) + .claude/rules/mc11-ir.md",
    "ir_design_doc": "docs/superpowers/plans/2026-05-28-cir-node-unified-ast.md",
    "roadmap": "docs/plans/ROADMAP.md",
    "promote_gate": ".claude/rules/branching.md \u2014 develop->master gated on CIR parity with master",
    "agent_memory": "project_backend_decision, project_smaug_fnptr_and_159, feedback_promote_parity, project_madc_vision, project_mc11_ir (FalkorDB graph madc-knowledge + ~/.claude .../memory)"
  }
}