{
  "version": "0.25.0",
  "date": "2026-06-02",
  "snapshot_note": "Canonical current snapshot. Kept lean and valid -- deep session history lives in docs/superpowers/plans/2026-06-01-HANDOFF.md, docs/parity/root-cause-worklist.md, CHANGELOG.md, and the madc-knowledge graph / ~/.claude memory. (Rewritten fresh 2026-06-02 after the prior append-only blob accumulated an unescaped-quote JSON corruption.)",
  "head": "aa8b16a",
  "branch": "develop (PUSHED, == origin/develop). Parity feature work is cut as feature/cir-parity-claude off develop and ff-merged back per cluster.",
  "mir_fork": "/workspace/mir branch develop @ 8864a73, PINNED via repo-root MIR_COMMIT=8864a73 (fork HEAD == pin). 8864a73 adopts 5 proven community-fork bug fixes (op_nums spill-reload overflow, addr_regs-stale-after-SSA [O2-gated], jump_opt lref-label preservation, vararg-RET use-after-NULL, NULL teardown guards) on top of 4aa628b; each attributed inline ADOPTED-FROM <fork>@<sha>. Correspondence: MIR develop <-> madc develop; MIR master <-> madc master at parity. madc links libmir.a -- rebuild the fork if it is stale after a container rebuild.",
  "build_status": "green (make -C src clean && make -C src builds clean, zero compiler warnings)",
  "backend": "madc parser -> cir_node (MC11-IR) -> c2mir -> MIR is the SOLE backend. asmjit JIT and the Gecko parser/transpiler were both removed. --backend=mir aliases to cir; there is no --backend=jit.",
  "ir": "MC11-IR: the cir_node tree DERIVES FROM c2mir node_t (c2mir consumes the lowered C11 view) AND carries each node's originating lexed tokens + parse subtree + file/line/col (madc retains the high-level view). Deliberately BOTH. SET IN STONE -- see docs/rules/mc11-ir.md. --dump-cir shows the live backend tree; --emit=c11 is a SEPARATE renderer, never the c2mir feed.",
  "test_status": {
    "unit_tests": "green (doctest: 19+30 cases). ~100 eval/exec/compile cases skipped -- the libmadc in-process compile/exec/eval surface is stubbed pending CIR->c2mir->MIR reimplementation.",
    "integration_tests": "456 passed, 7 failed, 1 flaky (testfortypedcomma flips fail<->timeout, IGNORE), 55 skipped (make -C src fulltest). testmultiret now PASSES (multi-return ported 2026-06-02). The 7 remaining fails are FEATURE-AREA gaps, NOT basic breaks (names are misleading): IOSTREAM/FSTREAM cluster = testloop (actually file streams: ofstream.open/<</good/close -> `<<` lowered as integer shift), testfstream, testcin (cin>> stdin); STL range-for = testforeach2 (vector/map iteration SIGSEGV); isolated = testdefer (Go-style `defer`, unimplemented), teststruct2 (std::string MEMBER of a plain struct not default-constructed -> bob.name=\"..\" SIGSEGV), testlargesizeofquery ((1<<62)-elem array sizeof -> abort). Most tractable real bug: teststruct2 (string-member construction); biggest 3-for-1: wire ofstream/ifstream/cin operator<</>>.",
    "gcc_torture": "CIR 1565/1685 (92.9%) vs asmjit/master 1645 (97.6%), gap 80. Regression baseline: docs/parity/torture-failset-current.txt (91 FAIL+TIMEOUT basenames). asmjit reference oracle built at /workspace/madc-asmjit (--madc /workspace/madc-asmjit/bin/madc).",
    "smaug": "SMAUG 1.8 boots END-TO-END through cir_node->c2mir->MIR->JIT and comes up as a live server ('Realms of Despair ready at ... port N'), and a serpent-room combat run passes. Port lives in the separate MadSMAUG repo (symlinked at /workspace/madc/MadSMAUG). Soak: cd /workspace/MadSMAUG/runtime/area; timeout 50 /workspace/madc/bin/madc /workspace/MadSMAUG/src/SMAUG.mad PORT (exit 124 = survived = good; grep the literal 'Realms of Despair ready at')."
  },
  "current_phase": "PARITY CAMPAIGN (develop->master gate): drive CIR gcc-torture coverage to match the removed asmjit backend (master, full C89 parity). Directive: BUGS BEFORE FEATURES -- fix c2mir front-end bugs first; save MIR-machine/fork-floor work (SIMD, __builtin_setjmp, frame-address) for LAST. c2mir = the C->MIR front-end (IR gen; tractable + upstreamable); MIR (libmir) = the IR machine (gen/interp/regalloc; the floor gaps live here).",
  "in_progress": "OTHER-38 triage (the failset's uncategorized bucket), group A = VLA follow-ons. DONE this tick: 20221006-1 const-bound VLA (fcaea2d, torture 1564->1565). Remaining group A: VLA-in-struct (20040423-1, align-nest), N-bound VLA (pr22061-1/3/4), VLA+goto-dealloc (vla-dealloc-1), multidim-VLA-via-pointer (pr82210, pr71626-2). Then group B (genuine value-miscompiles), C (features), D (MIR floor + SIMD, last). Full map: docs/parity/root-cause-worklist.md section 'OTHER-38 TRIAGE'.",
  "method_per_fix": "reduce -> attribute (gcc + clang + stock /workspace/mir/c2m FILE -ei/-eg; gcc-pass & clang-pass & c2m-fail = genuine c2mir/MIR bug; for madc-path bugs use bin/madc --dump-cir, NOT emit-C) -> deepest-layer fix -> rebuild -> make -C src fulltest (cap every run: ( ulimit -t 120; timeout 180 ... ), ONE heavy job at a time) -> FULL torture failset-diff for ZERO regressions -> COORDINATOR re-runs the SMAUG soak himself -> commit -> push -> if fork changed: push fork + bump MIR_COMMIT in the SAME madc commit (pin discipline) -> sync mirrors (this file + README + CHANGELOG + ROADMAP + worklist + failset + KG + memory).",
  "recent_fixes_2026_06_02": [
    "MULTI-RETURN reimplemented on CIR (aa8b16a, madc cir_builder): Go-style `return a,b` / `x,y := f()` was asmjit-only, removed with asmjit in 64f44b3, never ported to CIR -> silently broken (return 0 + uninit) since the backend switch; testmultiret produced garbage at all opt levels. Ported the __retbuf ABI (void return + hidden long*__retbuf first param; return stores to __retbuf[i]; call site allocs long __mret[N], passes it, extracts). Integration 455->456 (testmultiret recovered), torture 1565 zero-regression, SMAUG boots. NOTE this corrected an off-by-one in the const-VLA fulltest count (it was 455 not 456; testmultiret was already failing).",
    "Adopted 5 proven community-fork MIR bug fixes (fork 8864a73, pin bumped 4aa628b->8864a73): see mir_fork + docs/parity/mir-fork-community-patches.md. O1-safe, torture 1565 zero-regression. The GVN->=O3 shim was NOT adopted.",
    "20221006-1 const-bound VLA (fcaea2d, madc parser): a const var folds to a constant array dim ONLY when read_constant_integer succeeds; a const with a runtime initializer is a VLA bound. +1 torture.",
    "VLA core (af75404 + predecessors, madc cir_builder): function-local VLA lowered to (T*)malloc(prod(dims)*sizeof(elem)) + __attribute__((cleanup(__madc_vla_free))) (NOT __builtin_alloca -- c2mir never reclaims it, goto-loops stack-overflow). Multidim = flat malloc + linearize nested TokenSubscriptExpr M[i][j]->M[i*n+j]. Param-bound side effects + runtime sizeof(vla) from the DataDefCArray chain. All 6 VLA integration tests pass; +5 torture. (Advanced VLA forms = OTHER-38 group A above.)",
    "Seven c2mir-grind fixes (20020320-1 struct stmt-expr copy-out, 950906-1 stmt-expr post-inc, zerolen-1 zero-length array member, pr53084 static-local init, 20020411-1 _Complex width conversion, pr85095 overflow-builtin u64 helper, 20050929-1 static-init compound-literal ordering). Fork commits caa6ff9..4aa628b; all gated, zero regressions."
  ],
  "known_pre_existing_gaps": [
    "1D-VLA sizeof returns pointer size (8) instead of the runtime byte count -- affects plain runtime int a[n] too, independent of the const-VLA fix. Multidim/typedef VLA sizeof (DataDefCArray chain) is correct.",
    "Global const used as an array dim (const int G=5; int g[G]) reads 0 -- var->data is calloc'd-to-0 at parse time but the constant initializer is not folded into it. Pre-existing; separate from VLA.",
    "tests/testfortypedcomma.mad flaky: the 2nd declarator in a multi-declarator for-init (for(int a=0,b=4;...)) is left uninitialized -> nondeterministic timeout. Reproducible on clean HEAD.",
    "libmadc in-process compile/exec/eval stubbed (asmjit-era machinery removed); ~100 unit tests skipped until CIR reimplementation."
  ],
  "phases": {
    "language_core_phase_1_to_6": "achieved on the removed asmjit backend; being re-established on CIR via the parity campaign.",
    "phase_4_2_libmadc_api": "in_progress (security policy / structured diagnostics / engine-owned IO landed; in-process compile/exec/eval stubbed).",
    "cpp_class_model": "std::string is a real class; std::vector/map/set are real #include-defined std:: templates instantiated through the class model. Legacy dtSTRING/tkSTRING/tkVECTOR/ns_stl shortcuts retired.",
    "gecko_parser_and_transpiler": "REMOVED",
    "asmjit_jit_codegen": "REMOVED"
  },
  "deferred": [
    "eval/exec + REPL (mode-4): reimplement on CIR->c2mir->MIR (a madc_cir_execute variant returning the computed value). ~100 unit tests skipped as the future spec.",
    "AOT native object/executable: low priority; near-term native builds via --emit=c11 + an external compiler. save_object/save_executable stubbed with signatures kept.",
    "P2.11-13 std-dialect subsystem (vision keystone): keyword/feature->standard REGISTRY -> gating -> de-hardcode the c2mir target std -> standards conversion. See docs/plans/cpp-support.md.",
    "SIMD (Track 1.6, KG Decision{simd_raise_mir_upstream}): add a generic-vector extension to MIR + a c2mir vector_size front-end, DESIGNED FOR UPSTREAM. Interim: scalarize for JIT + emit-C->gcc/clang. The dominant remaining floor gap (~35 torture)."
  ],
  "canonical_refs": {
    "handoff": "docs/superpowers/plans/2026-06-01-HANDOFF.md (authoritative rehydration entry; STEP 0 = bash scripts/resume.sh)",
    "parity_worklist": "docs/parity/root-cause-worklist.md (root-cause clusters + OTHER-38 triage)",
    "torture_failset_baseline": "docs/parity/torture-failset-current.txt (diff next run for regressions)",
    "backend_decision_adr": "docs/adr/0001-cir-c2mir-backend.md (+ .claude/rules/backend-strategy.md) -- SETTLED, don't re-litigate",
    "lowering_vs_raising": ".claude/rules/lowering-vs-raising.md",
    "ir_definition_rule": "docs/rules/mc11-ir.md + .claude/rules/mc11-ir.md",
    "vision": "docs/plans/madc-vision-and-invariants.md (invariants I1-I8) + docs/plans/cpp-support.md (compliance roadmap)",
    "roadmap": "docs/plans/ROADMAP.md",
    "promote_gate": ".claude/rules/branching.md -- develop->master gated on CIR parity with master",
    "agent_memory": "project_cir_parity_campaign, project_backend_decision, project_north_star_c23_cpp23, project_mc11_ir, feedback_promote_parity (FalkorDB graph madc-knowledge via scripts/kg_query.sh + ~/.claude/projects/-workspace-madc/memory)"
  }
}
