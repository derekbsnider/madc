{
  "version": "0.24.0",
  "date": "2026-05-29",
  "branch": "feature/cir-node",
  "build_status": "green",
  "backend": "madc parser -> cir_node (MC11-IR) -> c2mir -> MIR is the SOLE backend. The asmjit JIT and the Gecko parser/MIR-transpiler were BOTH removed (commits 42e9b6e + 64f44b3). There is no --backend=jit; --backend=mir aliases to cir.",
  "ir": "MC11-IR: the cir_node tree DERIVES FROM c2mir node_t (c2mir consumes the lowered C11 view) AND carries each node's originating lexed tokens + parse subtree + file/line/col (madc retains the high-level view). It is deliberately BOTH. SET IN STONE — see docs/rules/mc11-ir.md and docs/superpowers/plans/2026-05-28-cir-node-unified-ast.md.",
  "test_status": {
    "unit_tests": "green; ~100 eval/exec/compile cases skipped — the libmadc in-process compile/exec/eval surface is stubbed pending reimplementation on CIR->c2mir->MIR",
    "integration_tests": "316 passed, 104 failed, 1 timed out, 56 skipped (CIR sole backend). This session 302->316 via fn-ptr declarators + array/pointer type-erasure fixes + varargs System V ABI (719744b) + abstract-param N_TYPE fix (d2fecaf). FIDELITY-OK (gcc-asm match on emitted C) != integration-pass.",
    "smaug_compile": "SMAUG-FIRST (priority #1). RUN-PATH (cir_node->c2mir): `cd /workspace/MadSMAUG/src; timeout 90 /workspace/madc/bin/madc SMAUG.mad 2>err`. CIR validity gate OPEN (0 untranslatable, since 7bbcd57 va_arg). The 85K tree reaches c2mir's checker. 2026-05-30: 159 -> 25 check errors via four type-erasure correctness fixes (all the same theme: madc was erasing array/pointer member/param types so c2mir rejected the malformed tree — NOT user errors): (1) 5221603 char-ptr-array string-init (is_char_array_element_type used rawtype, stripping pointer-ness) cleared 96 'excess elements'; (2) b858959 forward-typedef struct completion dropped member_dims, cleared 20 'subscripted value' on 2D members; (3) 667e9bd array-of-pointer struct members emitted (*m)[N] instead of *m[N], cleared 13 'assignment of incompatible value'; (4) d7ba8c2 array-parameter decay (char map[50][50]->char(*map)[50]) cleared 5 'subscripted value' on params. See docs/c2mir-error-catalog.md for the pre-catch strategy (c2mir is the reused semantic checker on madc's own tree w/ original source positions; most errors are madc bugs to fix, not user errors to pre-catch). 2026-05-30 cont'd: 25->4 via more fixes (df2cae4 extern preserves real type [bool/array]: an extern is a forward ref, emit the symbol's type not a re-derived one; f49e6b0 emit pre-case declarations in switch bodies; 218a6c5 __builtin_va_start takes 1 arg; 719744b varargs System V ABI). 2026-05-30 FINAL: SMAUG is c2mir-CHECK-CLEAN (0 errors, was 159 at session start). Last 4 cleared: a644505 strcpy-family return char* (ternary char*-vs-long) + return<void-expr> lowering; the 'repeated declaration' was an EMPTY-NAMED param (c2mir scope=N_FUNC) — root cause: param_decl emitted unnamed params as N_SPEC_DECL(IGNORE-id); c2m uses N_TYPE for abstract params. d2fecaf routes unnamed params through N_TYPE (also fixed a c2mir MIR-GEN CRASH on 2+ param fn-ptrs, and made fn-ptr params run: apply(op_add,20,22)==42). Also cleaned SMAUG.mad (removed bring-up artifacts: custom main, probes, services.c dup-main; comm.c's main is the entry). The fill_in_control_page MIR-gen 'wrong type memory' was a `.`-vs-`->` bug (fb3027b): `m_data[i].field` (m_data a pointer -> m_data[i] a struct VALUE) was emitted `m_data[i]->field`; gcc rejects it, c2mir-check is lenient, MIR-gen miscompiled it. Fixed by keying `.`/`->` off the subscript RESULT type (member translation ~cir_builder.cpp:1357). Confirmed via stock `c2m` on emitted C (and `-O0` unchanged -> it was a MIR-gen verify error, not optimizer). Added a guard rejecting `->` on a plain non-pointer struct value (arrays decay to pointers -> still OK). New tests/fidelity/structmemberaccess.c. -O<n> flag added (51937b4; default 1, MIR-gen level; level>=2 has a known SSA bug). NEW FRONTIER = MIR-LINK: `import of undefined item spec_fun` (an undefined SPEC_FUN symbol at MIR link/import time). Stack now: SMAUG.mad -> lexer -> parser -> cir_node -> c2mir CHECK [0] -> c2mir MIR-GEN [ok] -> MIR-LINK [undefined spec_fun] -> (JIT). Debugging tip: c2mir's 'repeated declaration %s' (c2mir.c:6697) prints the symbol + scope; a temp fprintf of POS(def_node)+scope->code pinpoints empty-named/no-position synthetic nodes. FN-PTR STATUS: scalar fn-ptr vars+params declarators LANDED (2ec34ff) — spiral-rule renderer + fnptr_decl_pieces build `ret (*name)(params)`; fnptr.c reducer FIDELITY-OK. The fn-ptr TYPEDEF + struct-MEMBER emitter fix is STASHED ('WIP fnptr typedef+member emitter') because it regresses SMAUG (159->176): root cause is a PARSER gap — `DO_FUN *p` and `DO_FUN g` collapse to the same bare DataDefFPTR (0 recoverable stars), so the emitter cannot tell pointer-use (DO_FUN *do_fun member, need *) from function-decl-use (DECLARE_DO_FUN `DO_FUN do_north`, no *). Prerequisite = Task #5 (parser records source `*` on fn-ptr-typedef var/param decls); then unstash + re-gate. Stack: SMAUG.mad -> lexer -> parser -> cir_node OK [0 untranslatable] -> c2mir [159 check errors, 96 = 2D-array init] -> (then MIR -> JIT)."
  },
  "phases": {
    "language_core_phase_1_to_6": "complete (achieved on the now-removed asmjit backend; being re-established on CIR)",
    "phase_4_2_libmadc_api": "in_progress (security policy / structured diagnostics / engine-owned IO landed; in-process compile/exec/eval stubbed)",
    "cir_coverage": "ACTIVE — 227/193/56; drive cir_node -> c2mir -> MIR to full parity",
    "gecko_parser_and_transpiler": "REMOVED (42e9b6e)",
    "asmjit_jit_codegen": "REMOVED (64f44b3)"
  },
  "deferred": [
    "eval/exec + REPL (mode-4): reimplement on CIR->c2mir->MIR (new piece = a madc_cir_execute variant returning the computed value). ~100 unit tests skipped as the future spec. Task #14.",
    "AOT native object/executable: low priority. Near-term native builds via emit-.c + an external compiler. save_object/save_executable stubbed with signatures kept for a later MIR-based revisit."
  ],
  "remaining_todo": [
    "Drive the 193 CIR integration failures toward 0 (Track 1.3)",
    "Write the cir_node -> .c / .mc11 renderer (single emitter; render target = the same language enum as --std=: C11, MC11, C++, madc)",
    "Stand up the gcc -S -fverbose-asm transpiler-fidelity gate + the cir_node-vs-`c2m -d` differential to make the 193 mechanical/localizable",
    "Reimplement eval/exec + REPL on MIR (deferred)",
    "AOT native output on MIR (deferred, low priority)",
    "Finish syncing stale mirrors: CHANGELOG [Unreleased], TODO.md (task #17)"
  ],
  "known_issues": [
    "193 CIR integration failures = the coverage worklist (classes: unhandled_expr / c2mir_rejected / runtime_mismatch). Re-measure fresh per session.",
    "CIR switch `default:` returns 0 for any value (CIR-only; emit-c/MIR/JIT were correct). See agent memory project_cir_switch_default_bug.",
    "libmadc in-process compile/exec/eval stubbed (asmjit-era machinery removed); ~100 unit tests skipped until CIR reimplementation."
  ],
  "canonical_refs": {
    "ir_definition_rule": "docs/rules/mc11-ir.md (bare) + .claude/rules/mc11-ir.md",
    "ir_design_doc": "docs/superpowers/plans/2026-05-28-cir-node-unified-ast.md",
    "roadmap": "docs/plans/ROADMAP.md",
    "agent_memory": "project_madc_vision, project_mc11_ir, project_cir_triage (FalkorDB graph madc-knowledge + ~/.claude .../memory)"
  }
}
