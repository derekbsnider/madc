{
  "version": "0.97.0",
  "date": "2026-09-02",
  "snapshot_note": "session 123: c2mir registry page arena landed; packed Adventure -2.06% Ir with slightly lower peak heap",
  "head": "develop — the vector CALLING-CONVENTION arc landed (a 128-bit vector is a register-class value at every call boundary: c2mir v128_reg_class_p, the MIR vararg typing, x86-64 16-byte stack slots + whole-xmm saves, the aarch64 gather and S5 shims, the two recorded shim defects, the Apple vararg handoff; gated against gcc/clang-compiled code on x86-64, aarch64/qemu and the Mac, scripts/vector_abi_gate.sh in fulltest); dispatch #11 measured wave 9 exactly (arm64 1285/3, Intel 1286/3 = <list>); lanes green at 7e0098d1; owner dispatch #12 next (expect arm64 3 / Intel 3); then upstream PR branches, the Apple stack-packing wave; libc++ <list> = owner scope",
  "branch": "develop @ 47ef432d (+ ledger/status commit on top); master = v0.95.2 promotion line + release.yml synced @2056f871; master's darwin-probe.yml copy is BEHIND develop's (D4 suite step + domains) — sync both workflows at D4 green",
  "live_handoff_previous": "s155 part 30 (2026-09-05): THE aarch64 SIMD (MIR_T_V128) ARC, S1-S4, LANDED + the three vector FRONT-END gaps its S4 reducers surfaced. Dispatch #10 (run 33934880333 @df6c620e) measured wave 8 exactly as predicted: arm64 1279/6 (3 gccvector + 3 libc++ <list>), Intel 1283/3 (docs 8754377b). SIMD arc (plan docs/plans/2026-09-05-aarch64-simd-v128.md; branch feature/aarch64-simd-v128-claude, rebased and fast-forwarded into develop): S1 d123c3b0 — a V128 is an FP-class value, the LD-in-Q register shape: mir-aarch64.h target_hard_reg_type_ok_p / target_locs_num, target_call_used_hard_reg_p (V8-V15 callee-saved in the low 64 bits only), fp_class_type_p + stack_arg_16_p (one predicate per ABI fact, written per type so Apple's 8-byte long double never shares the vector's 16-byte slot) through get_arg_reg / both stack-argument arms / the result and return arms, mir-gen.c's get_move_code forward-declared as the one move-code owner, the mv/Mv pattern letters (scale 16), the five VMOV rows (the LDMOV words with the v letters), va_arg_builtin's vector arms (linux vr save area; Apple 16-byte-aligned slot pair), the NEON encoding oracle promoted into the subtree (aarch64-neon-encodings.md); S2 715dc569 and/orr/eor, add/sub .16b/.8h/.4s/.2d, mul .8h/.4s; S3 43b4f307 fadd/fsub/fmul/fdiv .4s/.2d; S4 797d88b8 cmeq, signed cmgt, fcmeq, fcmeq+mvn (VNEF), fcmgt/fcmge with SWAPPED operands (VLTF/VLEF), ushl/sshl per-lane shifts, neg+ushl|sshl for the per-lane right shifts, umov/[neg]/dup/ushl|sshl for the scalar-count shifts on the fixed temps X9 + V16 (the DSL's h<hex> literal). Every word from the encoding oracle; qemu gate ALL 8 reducers == aarch64 gcc at c2m default and -O2 (tmp/logs/simd-qemu-3.log: vec_not comb lit args float cmp shift varg). THE S4 REDUCERS EXPOSED THREE FRONT-END GAPS (D5: separate x86-64 gaps first, each its own commit + gcc==clang reducer): ef156cac parser — a SIMD-typed EXPRESSION is subscriptable (the general subscript fallback knew pointers only: `(a + b)[2]` fell to the lambda introducer 'Expecting ] in lambda expression'), TokenBand/Bor/Xor type by the usual arithmetic conversions (C11 6.5.10-12; they were bare int — a suite-wide retyping, JIT lane 1302/0 green), TokenBSL/BSR vector arm, a comparison with a vector operand typed at reduce time (resolve_object_operator_type -> Program::simd_comparison_type = gcc's opaque signed-lane vector), Program::simd_type the one owner of anonymous DataDefSIMD (tests/testgccvectorexprsubscript); 15126633 c2mir — a vector comparison's type is gcc's OPAQUE vector of SIGNED lanes (struct vector_type::opaque_p, vector_cmp_result_type, opaque_vector_convertible_p at assignment/init/arg/return + the conditional, opaque_vector_compatible_elements_p for binary operands; the lowerings key signedness on the OPERAND type) — was SILENT: (ua == ub)[0] widened to 4294967295, and LOUD for a double compare into long long lanes (tests/testgccvectorcmpresult); 90990307 c2mir — vector << scalar count of ANY integer type (the count ran through the lane-fit test; tests/testgccvectorshiftcount). Mac (arm64, cross release build staged at ~/madc-s154): 1285 pass / 3 fail / 23 skip (tools/ and examples/ staged) — the 3 libc++ <list> tests only (testforeachiter, testphpdumpiter, testptrcmpupcast); the 17 vector tests, the 3 formerly-failing gccvector tests and the 3 new front-end reducers included, 17/0 (tmp/logs/simd-mac.log); release-arm64-macos 835 units, forest_pack_gate OK, verify_macho OK. Lanes at 797d88b8: linux battery jit 1302/0/9skip exe 1243/0 obj 1243/0 packed 1302/0/9skip headerless 1271/0/40skip, fulltest rc=0 with every gate in the chain GREEN (check-c-abi-surface OK: 1090 extern-C exports classified, 43 listed internal; warning ratchet GREEN, 0 warnings over 1311 compiled; tag-arithmetic 0/0; tsubst flag-on GREEN; forest_pack_gate linux 68 = baseline; forest_bind_gate all OK); wine64 1248/0/63skip + verify_pe_release OK (234 units) + win64 pack 68 = baseline; c-testsuite 220/220, 0 outside baseline; macos cross release both arches 835 units, darwin pack 48 = baseline both (mir-blob-skips 1/1), verify_macho OK both (tmp/logs/w9-battery.log). DISPATCH #11 MEASURED (run 33942662074 @0e121dd3, code 797d88b8, 2026-09-05 03:44 UTC): arm64 1285/3/23skip, Intel 1286/3/22skip — the libc++ <list> trio on both arches, the prediction exactly (docs commit records it; triage tmp/d4-triage/d4-33942662074). NEXT: THE VECTOR CALLING-CONVENTION ARC, found while opening S5 and superseding the shim-only slice — the reducers were SELF-CONSISTENT (c2m caller and c2m callee), so the qemu gate never compared the ABI against gcc-compiled code; a gcc-compiled shared object on one side (tmp/mirq/libvecnative.c + vec_ffcall.c, oracle = the same pair built by the platform compiler) shows: (1) x86-64, madc INCLUDED: a vector through `...` is garbage (silent, exit 0: bin/madc tmp/mirq/vec_varg.c prints 1073677189 for 19) — c2mir lowers va_arg(ap, v4si) as a two-eightbyte va_block_arg (reads TWO xmm slots) while the caller passes a V128 register operand that mir-gen-x86_64.c/mir-gen-aarch64.c/mir-interp.c type as I64 (value_mode MIR_OP_VECTOR is not in their vararg lists), and mir-x86_64.c va_arg_builtin has no vector arm; (2) x86-64, madc INCLUDED: a NINTH vector argument is demoted to a BLK by cx86_64-ABI-code.c process_aggregate_arg (register exhaustion) while target_gen_gather_arg still VMOVs it as a register — LOUD `vmov ... Got int, expected vector` (bin/madc tmp/mirq/vec_args.c); (3) aarch64: c2mir passes EVERY vector as blk0:16 (two X regs) and returns it through the x8 hidden pointer (simple_add_arg_proto/simple_add_res_proto treat a vector as an aggregate) — the S1 V-register convention is never exercised by c2m-compiled code; wrong against clang/gcc (AAPCS64 Short Vector: v[NSRN], result v0). FIX PLAN (each its own commit, x86-64 first): x86 process_aggregate_arg never demotes a V128; the generic simple_* helpers treat a 128-bit vector as a register-class value (V128 proto/call/gather/result/return; v128_gen_gather_arg shared with x86); va_block_type_p excludes v128 (non-win64: MS x64 passes __m128 varargs by reference — recorded, kept on the block path) and the va_arg lowering gains a V128 arm; the three machinizers/interp type MIR_OP_VECTOR varargs as V128; x86 va_arg_builtin gains the vector arm (FP save area, 16-aligned overflow); then the aarch64 S5 shims + Gap 1 (sizeof(MIR_val_t) stride) + Gap 2 (Apple interp shim imm12). GATES: madc x86-64 (.mad reducers testgccvectorvararg / testgccvectorargs9 with gcc==clang .expect; the interop pair via obj/mir/host/c2m -eg/-ei vs the gcc-built oracle — to become scripts/vector_abi_gate.sh in fulltest), qemu aarch64 (the pair with an aarch64-gcc .so), the Mac (Apple clang dylib + a cross arm64-macos c2m; also probes Apple stack-argument PACKING: iapply10 — Apple packs sub-8-byte stack args by natural size, MIR uses 8-byte slots). -> S5 of the SIMD arc (ff_call / interp shims + the two recorded shim defects mir_ff_call_shim_val_stride_long_double and mir_aarch64_apple_interp_shim_stack_fp_arg_offset_unencoded; the Apple va_arg arm's oracle through an arm64-macos c2m) -> #474 win64 spill space (perf) -> libc++ <list> two-phase completion (owner scope) -> candidate upstream PRs #472/#473 (owner review gates submission; the vector arc is fork-only until the x86-64 vector support is upstreamed as one series). TRAPS: a kg_query.sh argument with a backtick RUNS it; c2m wants the source file BEFORE -eg; the qemu driver builds from a COPY of the container's synced third_party/mir with the worktree's aarch64 files overlaid (tmp/simd-qemu.sh) — a c2mir change must be synced first; the runner's exe lane is advisory until D5 ships the dylib.",
  "live_handoff": "s156 part 31 (2026-09-05): THE VECTOR CALLING-CONVENTION ARC LANDED (ten code commits; plan docs/plans/2026-09-05-aarch64-simd-v128.md, section 'The vector calling convention'). Dispatch #11 (run 33942662074 @0e121dd3) measured wave 9 exactly as predicted: arm64 1285/3, Intel 1286/3 = the libc++ <list> trio on both arches (docs c1537bfa). Opening S5 exposed that every vector-ABI gate had compared c2m against c2m (self-consistent: a lane test, never an ABI test); with ONE side compiled by the platform compiler (tests/abi/libvecnative.c + vec_ffcall.c) the vector ABI was wrong on x86-64, madc INCLUDED (a vector through `...` = garbage, silent — bin/madc tmp/mirq/vec_varg.c printed 1073677189 for 19; a ninth vector argument = loud vmov mode error; no 16-byte stack slot ever 16-aligned — long double too, a movdqa fault; the vararg prologue saved xmm low eightbytes only) and on aarch64 everywhere (c2mir passed every vector as blk0:16 and returned it via x8). ONE RULE, v128_reg_class_p (c2mir.c): a 128-bit vector is a register-class value at every call boundary. Commits: e85f6c68 c2mir (v128_reg_class_p; va_block_type_p + a V128 MIR_VA_ARG arm; simple_* V128 lane; v128_gen_gather_arg shared x86/generic; x86 process_aggregate_arg never demotes V128), 464d0a4d mir (MIR_OP_VECTOR varargs typed V128 in mir-gen-x86_64.c / mir-gen-aarch64.c vararg_value_type / mir-interp.c call(); x86 va_arg_builtin vector arm; vsave whole-xmm register save area), 4fbffc77 mir x86-64 (stack_arg_16_p: caller arg_stack_size / callee mem_size / ff_call sp_offset / va_arg overflow 16-aligned) + tests/testgccvectorvararg.mad + tests/testgccvectorargs9.mad (gcc==clang), 4486c513 c2mir aarch64 gather -> simple_gen_gather_arg, b70903ed sizeof(MIR_val_t) ff_call stride (Gap 1, verified on the Mac: c2m -ei hello2), 75648579 Apple interp shim imm12 (Gap 2, verified: interop dapply9), 62e86e2d S5 aarch64 shims V128 (fp_class_type_p/stack_arg_16_p in mir-aarch64.h = one owner), fdaa33b4 Apple interp shim hands the caller's stack (x9) to an interpreted vararg function as its va_list (the start-at-8 contiguity fold cannot hold with 16-byte slots; probes tmp/mac-vabi/vec_varg2.c), 0661a666 scripts/vector_abi_gate.sh in fulltest (c2m -eg / -ei / madc via #load, 17 lines == host cc). GATES: x86-64 tmp/logs/vabi-x86-4.log (5 reducers x madc/eg/ei == gcc, interop pair identical, subsets 172/0, sret/unprototyped OK); qemu aarch64 tmp/logs/vabi-a64-2.log (every S1-S4 reducer -O0/-O2/-ei, both .mad reducers, the pair -eg/-ei == aarch64-gcc); Mac tmp/logs/vabi-mac-3.log (cross arm64-macos c2m: every reducer -eg/-ei == Apple clang; the pair identical except iapply10 = KG Gap mir_aarch64_apple_stack_arg_packing — Apple packs sub-8-byte non-variadic stack args by natural size, MIR uses 8-byte slots; NEXT WAVE: mir-gen-aarch64.c both stack arms + both shims, reducer = the pair's iapply10 + a c2m-caller twin). Also recorded: KG Gap mir_win64_vector_abi_by_reference (MS x64 passes __m128 by reference; varargs already on the block path = by pointer). Lanes at 7e0098d1 (tmp/logs/w10-battery.log): linux battery jit 1304/0/9skip exe 1245/0 obj 1245/0 packed 1304/0/9skip headerless 1273/0/40skip, fulltest rc=0 with every gate in the chain GREEN (vector_abi_gate OK: c2m -eg, c2m -ei and madc byte-identical to the host compiler; check-c-abi-surface OK: 1090 extern-C exports classified, 43 listed internal; warning ratchet GREEN; tag-arithmetic 0/0; tsubst flag-on GREEN; forest_pack_gate linux 68 = baseline); wine64 1250/0/63skip + verify_pe_release OK (234 units) + win64 pack 68 = baseline, the two new vector reducers included, and the interop pair under wine == mingw-gcc (tmp/w10-vabi-win.sh); c-testsuite 220/220, 0 outside baseline; macos cross release both arches 835 units, darwin pack 48 = baseline both (mir-blob-skips 1/1), verify_macho OK both (tmp/logs/w10-battery-2.log). Mac full suite on the content: 1287 pass / 3 fail / 23 skip (tools/ and examples/ staged) — the 3 libc++ <list> tests only (testforeachiter, testphpdumpiter, testptrcmpupcast); the 19 vector tests (the two new reducers testgccvectorvararg and testgccvectorargs9 included) 19/0 (tmp/logs/w10-mac.log; the cross release-arm64-macos build of fdaa33b4 — 7e0098d1 differs only under #if defined(_WIN32)). NEXT: owner dispatch #12 (`gh workflow run darwin-probe.yml -R derekbsnider/madc --ref develop -f suite_gate=false`; `bash tmp/d4-triage.sh <run-id>`; expect arm64 3 = the libc++ <list> tests, Intel 3) -> upstream PR branches (owner asked; owner review gates submission): #472 0a2d3e8e, #473 49741295, the Apple interp imm12 75648579 (upstream mir-aarch64.c:606 has it), the aarch64 _Complex HFA 93010447, canon_type in new_func_reg 7d63083a, the x86-64 16-byte stack slot alignment for long double (the non-vector half of 4fbffc77), the aarch64 ff_call LD first-pass rounding — clean branches off upstream/master on derekbsnider/mir, each built + tested on the container -> the Apple stack-packing wave -> #474 win64 spill space (perf) -> libc++ <list> two-phase completion (owner scope; offered to take it). TRAPS: an ABI reducer both sides of which madc compiles proves nothing — the oracle compiles ONE side (tmp/vabi-x86.sh / vabi-a64.sh / vabi-mac.sh are the three stage drivers; the Mac cross c2m needs CC=clang-18 -target arm64-apple-macos12 --sysroot /workspace/sdk/MacOSX.sdk AR=llvm-ar-18 LDFLAGS=-fuse-ld=lld); a kg_query.sh argument with a backtick RUNS it; a pathspec git stash leaves untracked files out.",
  "mir_fork": "RETIRED AS A DEPENDENCY (2026-08-11, ADR 0002): MIR lives IN-TREE at third_party/mir — ordinary madc source, no pin, no lockstep, no fork release. It carries native _Complex, cleanup attribute, scope-depth layout fix, <=16-byte SIMD, the Mach-O executable writer, and the ABI fixes the CIR backend depends on. Build products in obj/mir/<variant>. github.com/derekbsnider/mir = frozen historical downstream + upstream-PR transport (vnmakarov/mir is upstream; remotes: mir-upstream, mir-pr). Last standalone state = tag madc-pre-subtree-migration = v1.0-madc.0.76.0.",
  "build_status": "green",
  "backend": "madc parser -> cir_node (MC11-IR) -> c2mir -> MIR is the SOLE backend. asmjit JIT and the Gecko parser/transpiler were both removed. --backend=mir aliases to cir; there is no --backend=jit.",
  "ir": "MC11-IR: the cir_node tree DERIVES FROM c2mir node_t (c2mir consumes the lowered C11 view) AND carries each node's originating lexed tokens + parse subtree + file/line/col (madc retains the high-level view). Deliberately BOTH. SET IN STONE -- see docs/rules/mc11-ir.md. --dump-cir shows the live backend tree; --emit=c11 is a SEPARATE renderer, never the c2mir feed.",
  "test_status": "Wave-10 battery on develop @7e0098d1 (container, 2026-09-05, tmp/logs/w10-battery.log): linux battery jit 1304/0/9skip exe 1245/0 obj 1245/0 packed 1304/0/9skip headerless 1273/0/40skip, fulltest rc=0 with every gate in the chain GREEN (vector_abi_gate OK: c2m -eg, c2m -ei and madc byte-identical to the host compiler; check-c-abi-surface OK: 1090 extern-C exports classified, 43 listed internal; warning ratchet GREEN; tag-arithmetic 0/0; tsubst flag-on GREEN; forest_pack_gate linux 68 = baseline); wine64 1250/0/63skip + verify_pe_release OK (234 units) + win64 pack 68 = baseline, the two new vector reducers included, and the interop pair under wine == mingw-gcc (tmp/w10-vabi-win.sh); c-testsuite 220/220, 0 outside baseline; macos cross release both arches 835 units, darwin pack 48 = baseline both (mir-blob-skips 1/1), verify_macho OK both (tmp/logs/w10-battery-2.log). Full arm64 Mac suite on the same content: 1287 pass / 3 fail / 23 skip (tools/ and examples/ staged) — the 3 libc++ <list> tests only (testforeachiter, testphpdumpiter, testptrcmpupcast); the 19 vector tests (the two new reducers testgccvectorvararg and testgccvectorargs9 included) 19/0 (tmp/logs/w10-mac.log; the cross release-arm64-macos build of fdaa33b4 — 7e0098d1 differs only under #if defined(_WIN32)). Vector ABI gates: x86-64 tmp/logs/vabi-x86-4.log, qemu aarch64 tmp/logs/vabi-a64-2.log, Mac tmp/logs/vabi-mac-3.log (all == the platform compiler except the recorded Apple stack-packing probe). Darwin runner measurement #11 @0e121dd3: arm64 1285/3/23skip, Intel 1286/3/22skip — dispatch #12 measures wave 10 (expect arm64 3, Intel 3).",
  "current_phase": "php dump intrinsics: pointers + enums DONE (session #103). ONE shape left — std::map/set/list. NOT because madc lacks begin()/end() (a hand-written iterator loop over a std::map works today) but because madc has ONE shared \"is this iterable\" recognizer, class_index_iteration_protocol, which knows only the POSITIONAL size()+operator[] shape — so there is no GENERATED iterator loop for either the range-for or the dumper. No release cut: the owner ruled no version bump until the arc is done.",
  "in_progress": "TRACK 7 PHASE 1 GREEN on feature/track7-hub-projections-claude @6ff79adf (pushed, UNMERGED — merge timing = owner call, UNRELEASED — OWNER LAW: releases mark COMPLETE features; RELEASE BOUNDARY = Colossal Cave Adventure TESTED FULLY PLAYABLE, and that release comes WITH a three-platform MASTER PROMOTION batching v0.93+v0.94+Track 7; VERSION stays 0.94.0). Wave battery: fulltest rc=0, 1106/0/0TO/9skip (+2 pilot tests), EXE 1065/0, release+packed 1106/0/0/9. Shipped: madcdis hub/verbs/uinode/prose/render_text/projection/world_text/adventure headers + <ns_ui> (first new namespace since mangled-direct, zero parser changes) + tests/adventure.world pilot + 2 negative-controlled fulltest gates + 4 doctest batteries (185 asserts). NEXT (owner-directed): examples/adventure = the ORIGINAL Colossal Cave Adventure on madc::ui+madc::dis (esr/open-adventure BSD-2 = data source + transcript oracle; advent.cpp/advent.py = idea mines); OWNER RULING: level-0 text render is dis-internal, external UI render libs are dat-side providers. READ the HANDOFF block atop docs/plans/2026-08-20-track7-phase1-text-adventure.md. Background: upstream wave 2 (#469, PRs #470/#471) awaiting Vladimir; v0.93/v0.94 promotion deferred.",
  "method_per_fix": "reduce -> attribute with GCC + Clang + the in-tree third_party/mir/c2mir path (for madc-path bugs use bin/madc --dump-cir, not emit-C) -> deepest-layer fix -> focused reducer -> make -C src fulltest -> authoritative remote fulltest/libcxx/EXE/OBJ as scope requires -> hosted/domain gates for target changes -> commit with Hypothesis/Layer/Searched/Oracle trailers -> push -> update KG first and mirror status/README/CHANGELOG/ROADMAP. MIR is ordinary in-tree source; there is no fork pin or lockstep release.",
  "known_pre_existing_gaps": [
    "SCRIPT `value` IS NOT YET THE 32-BYTE madc_value STRUCT (recorded 2026-08-17, owner: not to be taken on now). Symptom: `value f()` returned BY VALUE emits `int f()` and silently returns nothing (exit 0, empty output) — func_def's return-type arm -> type_list -> append_type_specs has no dtARRAY case and falls through to int. That is why php::print_r returns madc::value & rather than madc::value. CORRECTED PREMISE: a C type DOES exist — madc_value (include/madc_api.h:52) is a tagged 32-byte aligned(16) struct, and madc::value merely CONTAINS it plus two unique_ptr members (32+8+8 = 48 = the emitted long long[6]); the opaque-buffer lowering exists because of those OWNING C++ members, not a missing type. MEASURED: _array/_object are private with no friends and all 40 refs are in ONE file (src/madc_value.cpp) — 12 mutations, 28 reads; the cell finalizer is already wired and called from generated code (cir_builder.cpp:25789/25797); the is_array_object sites would mostly RETIRE. HARD PART: the class deep-copies the containers while the cell path shares, so the migration needs copy-on-write at those 12 sites — a semantic change with its own oracled tests. Full write-up: docs/plans/2026-06-12-type-table-value-abi-design.md section 9. Sibling defect, also open: assigning to a `value &` PARAMETER is wrongly rejected ('assignment of incompatible value').",
    "FIXED 2026-06-12 (this session): multi-declarator ctor-init hang (`Q a(1), b(2);` — ctor-syntax decl path now does the standard comma-continuation; tests/testctorcomma.mad); VLA-variable sizeof returned 8 for 1D AND multidim AND typedef VLA vars (runtime dims now captured into hidden __madc_vla_dim_* locals at declaration, sizeof(var) builds the runtime product; tests/testvlasizeof.mad); global const array dim read 0 (parse-time-known const scalar int initializers now baked into var->data + vfCONSTBAKED admits the CIR read-fold so `const int H=G+3;` emits a constant file-scope initializer; tests/testconstdim.mad); testfortypedcomma flake not reproducible at live HEAD (25 consecutive green runs) — resolved by intervening parser work.",
    "VLA sizeof in CONSTANT-required contexts (e.g. `int b[sizeof(vla_a)]`) still folds to 8 via the constant evaluate_type_query path (the expression-path fix doesn't reach it); alignof(vla_var) still reports pointer alignment 8 (gcc: element alignment). Same family, much rarer.",
    "VLA NOT DEALLOCATED ON BACKWARD GOTO: a `goto` jumping back to before a VLA's declaration must deallocate the VLA (gcc/C semantics; gcc-torture 20040811-1.c). madc lowers VLAs as malloc + __attribute__((cleanup)), which frees only on SCOPE EXIT, not on the backward goto re-entering the scope — so a backward-goto loop over a VLA LEAKS one allocation per iteration. 20040811-1.c leaks ~2GB over 1e6 iterations; it passes only when memory is available (it OOM-SIGSEGV'd when the MIR fork's 128MB code-holder reservation reduced headroom — see mir_fork; arch-gated off x86-64, so it passes again). Proper fix: free the VLA at the backward goto. Separate future slice.",
    "MadArray/MadValue namespacing is an open API/ABI follow-up: moving the real C++ types to madc::array and madc::value would give PHP array helpers a normal namespace type surface, but compatibility aliases alone will not preserve old C++ mangled names.",
    "Embedded <iostream> output remains partially staged: cout/cerr/clog still use header-defined stdio-backed ostream bodies. Follow-up is to replace that output surface with real libstdc++ declarations/operators, matching the std::cin direction; do not add stdio.h/FILE* stream bridges.",
    "Real system-header PCH regeneration is deferred until PCH preserves include-guard/macro state; broad regenerated blobs currently duplicate transitive typedefs/declarations.",
    "Explicit --std=c++ real iostream preprocessing reaches c2mir but fails on unresolved class/member aliases such as char_type, iter_type, iostate, __streambuf_type, __ostream_type, and __ios_type. Fix this generically through class/member alias resolution, not per-std shims.",
    "libmadc in-process compile/exec/eval stubbed (asmjit-era machinery removed); ~100 unit tests skipped until CIR reimplementation."
  ],
  "phases": {
    "language_core_phase_1_to_6": "achieved on the removed asmjit backend; being re-established on CIR via the parity campaign.",
    "phase_4_2_libmadc_api": "in_progress (security policy / structured diagnostics / engine-owned IO landed; in-process compile/exec/eval stubbed).",
    "data_channel_streaming_process_flow": "done @cd1f19c6 - lazy typed flow, dependency-free channels including TCP/UDP/UDS, explicit process pumping, DSV native streaming, and ABI-compatible extension seams.",
    "cpp_class_model": "std::string, streams, std::sstream, std::vector/map/set/list-style containers, and user classes flow through the same parsed-header/user-defined object model: generic overload resolution, mangling, ctor/dtor, and retbuf handling. Legacy dtSTRING/tkSTRING/tkVECTOR/ns_stl shortcuts are retired; the finish-line gate is 0 on develop.",
    "gecko_parser_and_transpiler": "REMOVED",
    "asmjit_jit_codegen": "REMOVED"
  },
  "deferred": [
    "Optional madcdat service providers: libcurl-backed HTTP/HTTPS/REST/FTP/S3 as appropriate, mail protocols, and dependency-backed database/API endpoints. Core TCP/UDP/UDS transports are already in madcdis.",
    "Native streaming migration: DSV is incremental; migrate suitable FLR/VLR/external drivers and future shipped SourceAdapters through the optional extension seams while preserving legacy vector APIs.",
    "eval/exec + REPL (mode-4): reimplement on CIR->c2mir->MIR (a madc_cir_execute variant returning the computed value). ~100 unit tests skipped as the future spec.",
    "P2.11-13 std-dialect subsystem (vision keystone): keyword/feature->standard REGISTRY -> gating -> de-hardcode the c2mir target std -> standards conversion. See docs/plans/cpp-support.md.",
    "SIMD (Track 1.6, KG Decision{simd_raise_mir_upstream}): in progress on /workspace/mir feature/simd-vector-support-codex @ 2ffebff. Checkpoints now cover the first memory-backed c2mir vector_size frontend slice, a partial MIR v128 floor with i32 add/sub/bitwise/signed comparisons, unsigned v4u32 equality/inequality/ordering, same-size v128 vector casts, v4i32/v4u32 vector shifts, v4i32/v4u32 multiply/divide/modulo, __builtin_convertvector/__builtin_shufflevector on v128 vectors with 4- and 8-byte arithmetic lanes, GCC __builtin_shuffle on same-type v128 sources with signed/unsigned runtime mask modulo handling, v128 integer vectors with 1-, 2-, 4-, and 8-byte lanes, uint16x8_t coverage, Clang-style __builtin_vectorelements, width-changing __builtin_shufflevector results from v128 sources, non-v128 source-width support for __builtin_shufflevector via memory-backed scalar lane copies, generic non-v128 GCC __builtin_shuffle support through memory-backed scalar lane copies, floating-lane arithmetic/comparisons for v128 and non-v128 float/double vectors through memory-backed scalar lane lowering, x86-64 v128/v64/v32/v16/v8 integer-vector parameter/return ABI support, packed MIR v128 f32/f64 arithmetic and comparison opcodes for vector_size(16) float/double operations, and packed MIR v128 i8/i16/i32 add/sub and comparison opcodes, packed MIR v128 i64 add/sub opcodes, plus packed MIR v128 i8/i16/i32 multiply coverage, packed MIR v128 i8/i16/i32 scalar-count shift coverage, packed MIR v128 i64 scalar-count shift coverage, and packed MIR v128 lane-count shift coverage, qword vector comparison scalar-fallback masks, packed v128 i64 equality/order, scalar-condition vector conditional expressions, and same-element-count __builtin_convertvector across supported vector widths, non-v128 integer vector operations through scalar-lane lowering, and non-v128 same-size vector casts through memory-backed block copies, same-size integer scalar/vector reinterpret bitcasts, GNU declaration-spec and leading type-name vector attributes, expression-valued vector_size attributes, __builtin_abort, __builtin_memcmp, __builtin_copysignf, and __builtin_nan lowering, all 37 GCC c-torture execute vector-construct files checked in and passing under C2MIR -ei/-eg, including exact pr92618/pr94524-{1,2}/pr53645/pr53645-2/pr109040/simd-{1,2,4,5,6}/pr65427/pr60960/pr70903/pr85169/pr108292/pr105613/pr72824-2/fp-cmp-cond-1 runtime coverage, empty GNU asm barrier parsing, narrow address-taken register rvalue extension, union-alias preservation through array subscripts, one-lane unsigned __int128 vector equality broadened to one-lane __int128 vector arithmetic/bitwise/unary/comparison/shift/compound/inc-dec lowering including signed/unsigned div/mod helper-call lowering, mixed-signedness vector shift-count type compatibility, and mixed-source-width __builtin_shufflevector support, and GCC vector inc/dec lowering, plus Clang ext_vector_type support including odd lane counts, and MIR_T_V128 text/binary data I/O round-trip support, and MIR v128 lane-count shift opcodes with direct MIR/C frontend coverage. No known <=16-byte SIMD gap remains after one-lane __int128 vector div/mod helper-call lowering; later work is AVX/YMM register ABI for 32-byte-and-larger external vector boundaries plus the broader generic-vector extension to MIR + vector ops/registers/interpreter/codegen, DESIGNED FOR UPSTREAM. Interim: scalarize for JIT + emit-C->gcc/clang."
  ],
  "canonical_refs": {
    "data_channel_plan": "docs/plans/2026-08-07-data-channel-streaming-process-flow-plan.md (implemented guideline; actual code preserves legacy vtables through optional extensions)",
    "handoff": "docs/plans/2026-07-16-CODEX-HANDOFF-pattern-lane-perf-rungs.md (Codex hand-back @b2b2cf5e; R3 stashed; exact-HEAD gates listed)",
    "forest_perf_result": "docs/plans/2026-07-14-CODEX-HANDOFF-forest-perf-instantiate-cirbuild.md (Mission 1 result @cbcb79b6, merged + independently verified)",
    "parity_worklist": "docs/parity/root-cause-worklist.md (root-cause clusters + OTHER-38 triage)",
    "torture_failset_baseline": "docs/parity/torture-failset-current.txt (diff next run for regressions)",
    "backend_decision_adr": "docs/adr/0001-cir-c2mir-backend.md (+ .claude/rules/backend-strategy.md) -- SETTLED, don't re-litigate",
    "lowering_vs_raising": ".claude/rules/lowering-vs-raising.md",
    "ir_definition_rule": "docs/rules/mc11-ir.md + .claude/rules/mc11-ir.md",
    "vision": "docs/plans/madc-vision-and-invariants.md (invariants I1-I8) + docs/plans/cpp-support.md (compliance roadmap)",
    "roadmap": "docs/plans/ROADMAP.md",
    "promote_gate": ".claude/rules/branching.md -- develop->master gated on CIR parity with master",
    "agent_memory": "project_cir_parity_campaign, project_backend_decision, project_north_star_c23_cpp23, project_mc11_ir, feedback_promote_parity (FalkorDB graph madc-knowledge via scripts/kg_query.sh + ~/.claude/projects/-workspace-madc/memory)"
  },
  "current_focus": "RELEASED as v0.86.0 ON DEVELOP (not pushed). The session-#104 handoff's three turn-3 items are all landed: the libc fallback signatures (both halves), the count/size ruling, and the range-for auto element. OPEN, recorded: (a) the c++locale.h ARGUMENT-typing residual (typeof declaration -> __locale_t as integer; plan doc names it), (b) `for (value v : a)` does not compile (loud, recorded in the auto plan doc), (c) `puts` registered as returning void where C says int (noted in the libc plan doc, own blast radius), (d) handoff §4 leftovers: raw-pointer end() tightening, D1 (value f() emits int f()), D2 (value & parameter assignment), _Alignas on a struct member in c2m, std::unordered_map does not parse (__int_traits), for (int v : plain_struct) diagnostic quality, two macOS MIR-blob causes.",
  "last_update": "2026-08-28 v0.96.0 release (s141)",
  "arc_rollup": "ALTITUDE MAP (rungs -> arcs -> tasks; maintained at milestones). [1] FOREST/TRACK-B INFRASTRUCTURE — COMPLETE: pack-time drain (rung 1, v0.34.0), per-segment zstd compression 101->9.26MB (#37), MIR module cache rungs 1-3 (#42), class-pattern live-capture R1-R5 (#39-41,#43), multi-TU --project milestone (@94120962, project_gate in fulltest), bind lane byte-identical to live (forest_bind_gate 18/18). The packed suite IS the arbiter for everything since. [2] CORRECTNESS BURNDOWN (what exercising the packed state exposed) — (a) VIRTUAL-INHERITANCE ARC, one family #35->#36(s1,s2)->#47->#49: member-subobject construction, dynamic Itanium vbase offsets + vcall convention + vbase groups, member access through vbase views + member_vbase freeze v32, ref-bind/ref-return unwraps, prologue-only vtables + plain-base RTTI. CLOSED — #48 landed @def0db36 (manipulator save-the-lhs). (b) one-off kills: #33 istringstream shift, #34 family-D drain gaps, #44/#45 loop scope, #46 cast-arrow. Arbiter trajectory across the burndown: 697->704 (growth = new oracle tests locking fixes). [2c] EMIT-C + CONSTRUCTION COMPLETENESS: #50 landed @04635221 (extern-global cleanup gate + typed dtor externs -> iostream TUs gcc-clean, old #20 item closed); #51 landed @cf73c3ad (complete-object construction assembler: new[]/delete[] Itanium cookie, stack class arrays, ctorless user-ctor base chains, heap/member vbase ctors; arbiter 706->708). [2d] LANGUAGE-SURFACE BURNDOWN (batch queue #52-#54, all landed 2026-07-18): #52 pure virtual (@6fbadabe), #53 explicit dtor calls + placement-new convergence (@a0d82a83), #54 #if evaluator ?:/##/typeof-casts -> strict --std=c++11 lane serves real headers (@afc85776); arbiter 710->713. [3] OPEN LEDGER: CLEARED 2026-07-18 (batch #55-#59: SFINAE-quiet @1f382e8c, array dtors @3339e04d, dtor names @1b12c1bb; #58 C1 @bdd129fe; #59 A3 verified already-landed). Unfiled polish only: sizeof(template-id) parse gap; instantiation-diagnostic file/line misattribution (cosmetic). [4] NEXT ALTITUDE: the governing plan docs/plans/2026-07-04-data-substrate-first-customer-PLAN.md track order — B4b-B4d (forest default-mode design) then A2 snapshot:// driver / C2-C4 feature tracks. | [3a] data-substrate track C OPENED: C1 madcdis core-ification landed @bdd129fe (task #58) — interface headers at their permanent madcdis home, substrate core in both configure modes; C2-C4 follow after track B. | [5] AOT/ELF ARC (docs/plans/2026-07-19-mir-aot-elf-plan.md, owner-directed 2026-07-19/20): R0 fork survey (#81) -> R1 madc -g source-level gdb on the JIT incl. .debug_frame CFI (#82, fork @b6a411fa) -> R2 object-capture -> linkable ELF .o, library API at both layers, object lane 1139/0 (#83, fork @dc01374b, madc @c5741b27; R3 writer unification largely closed by construction). R4 LANDED @e86ac538 (fork @5c461803): madc -c/-o DIRECT native output — MIR assembles ET_EXEC itself (no cc/ld; owner: external linking was only ever a TEST), --exe lane live 709/729. R4 RUNG COMPLETE 2026-07-20 (task #85 closed): direct ET_DYN -shared landed (cir_run_link DELETED — zero external-toolchain product paths; fork @c0cb2b47), --project native emission (per-TU .o + whole-program -o/-shared; NATIVE SMAUG: 51 TUs -> 5.0MB MIR-assembled ELF, boots + serves TCP), exe burndown to CLEAN 717/0 (void-main->int lowering + exe_skip fixture). NEXT AOT RUNGS (tasks not yet filed): R4b execute-.o-as-cache (master parity), R5 DWARF in .o, R6 PIC; Mach-O/PE assemblers later behind the same MIR_object seam.",
  "promote_gate": ".claude/rules/branching.md — develop->master gated on the gcc-torture class-(a) burndown (met; stamp in docs/parity/failset-classification.md). PROMOTED 2026-08-07: master = v0.69.0 @034cfd28 (tag v0.69.0 + GitHub Release --latest; README conflict vs owner's direct @75bf5b7d resolved to develop's rework per owner). Fork master FF'd to 4573a0f3 in lockstep.",
  "validation": "v0.91.0 battery 2026-08-19: fulltest 1096/0/0TO/9skip rc=0; EXE 1056/0; OBJ 1056/0; census 1105 tests / zero warnings; release rc=0; packed 1096/0/0/9; headerless 1069/0/36skip (testiomanip carries a headerless_skip: <iomanip> is a documented pack-corpus blocker — forest_pack_headers.txt v3); version_flag_gate 0.91.0"
}
