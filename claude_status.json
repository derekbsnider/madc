{
  "version": "0.24.0",
  "date": "2026-05-29",
  "branch": "feature/cir-node",
  "build_status": "green",
  "backend": "madc parser -> cir_node (MC11-IR) -> c2mir -> MIR is the SOLE backend. The asmjit JIT and the Gecko parser/MIR-transpiler were BOTH removed (commits 42e9b6e + 64f44b3). There is no --backend=jit; --backend=mir aliases to cir.",
  "ir": "MC11-IR: the cir_node tree DERIVES FROM c2mir node_t (c2mir consumes the lowered C11 view) AND carries each node's originating lexed tokens + parse subtree + file/line/col (madc retains the high-level view). It is deliberately BOTH. SET IN STONE — see docs/rules/mc11-ir.md and docs/superpowers/plans/2026-05-28-cir-node-unified-ast.md.",
  "test_status": {
    "unit_tests": "green; ~100 eval/exec/compile cases skipped — the libmadc in-process compile/exec/eval surface is stubbed pending reimplementation on CIR->c2mir->MIR",
    "integration_tests": "272 passed, 148 failed, 56 skipped (CIR sole backend). Up from 227 via fidelity-suite-driven cir_builder fixes (pointer-depth, _Complex spec, array-dims, struct bodies, multi-level ptr base, for-init decls, deref-step, etc.). The 148 are the active CIR coverage worklist; remaining hard clusters = fn-pointer vars (need parenthesized declarators), VLA, va_arg, complex-arithmetic (see the cir_fidelity suite + task #22)."
  },
  "phases": {
    "language_core_phase_1_to_6": "complete (achieved on the now-removed asmjit backend; being re-established on CIR)",
    "phase_4_2_libmadc_api": "in_progress (security policy / structured diagnostics / engine-owned IO landed; in-process compile/exec/eval stubbed)",
    "cir_coverage": "ACTIVE — 227/193/56; drive cir_node -> c2mir -> MIR to full parity",
    "gecko_parser_and_transpiler": "REMOVED (42e9b6e)",
    "asmjit_jit_codegen": "REMOVED (64f44b3)"
  },
  "deferred": [
    "eval/exec + REPL (mode-4): reimplement on CIR->c2mir->MIR (new piece = a madc_cir_execute variant returning the computed value). ~100 unit tests skipped as the future spec. Task #14.",
    "AOT native object/executable: low priority. Near-term native builds via emit-.c + an external compiler. save_object/save_executable stubbed with signatures kept for a later MIR-based revisit."
  ],
  "remaining_todo": [
    "Drive the 193 CIR integration failures toward 0 (Track 1.3)",
    "Write the cir_node -> .c / .mc11 renderer (single emitter; render target = the same language enum as --std=: C11, MC11, C++, madc)",
    "Stand up the gcc -S -fverbose-asm transpiler-fidelity gate + the cir_node-vs-`c2m -d` differential to make the 193 mechanical/localizable",
    "Reimplement eval/exec + REPL on MIR (deferred)",
    "AOT native output on MIR (deferred, low priority)",
    "Finish syncing stale mirrors: CHANGELOG [Unreleased], TODO.md (task #17)"
  ],
  "known_issues": [
    "193 CIR integration failures = the coverage worklist (classes: unhandled_expr / c2mir_rejected / runtime_mismatch). Re-measure fresh per session.",
    "CIR switch `default:` returns 0 for any value (CIR-only; emit-c/MIR/JIT were correct). See agent memory project_cir_switch_default_bug.",
    "libmadc in-process compile/exec/eval stubbed (asmjit-era machinery removed); ~100 unit tests skipped until CIR reimplementation."
  ],
  "canonical_refs": {
    "ir_definition_rule": "docs/rules/mc11-ir.md (bare) + .claude/rules/mc11-ir.md",
    "ir_design_doc": "docs/superpowers/plans/2026-05-28-cir-node-unified-ast.md",
    "roadmap": "docs/plans/ROADMAP.md",
    "agent_memory": "project_madc_vision, project_mc11_ir, project_cir_triage (FalkorDB graph madc-knowledge + ~/.claude .../memory)"
  }
}
