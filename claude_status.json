{
 "version": "0.96.0",
 "date": "2026-08-28",
 "snapshot_note": "session 123: c2mir registry page arena landed; packed Adventure -2.06% Ir with slightly lower peak heap",
 "head": "v0.96.0 on develop \u2014 the variadic-class arc: embed_hello.cpp compiles+RUNS (both example legs gated); libmadc embedding fixes; forest rebound-ctor local_emit_name stamp + husk canonical-path re-include",
 "branch": "develop @ v0.96.0 (merged feature/variadic-class-tpl-claude --no-ff @55d4a69e); master = v0.92.1\u2192v0.95.2 promotion line",
 "live_handoff_previous": {
  "session": 141,
  "date": "2026-08-28",
  "branch": "develop @c18f3a45 (v0.96.0 released @3cecdf2a; feature branch merged @55d4a69e) \u2014 session queue COMPLETE through the win-VT recon",
  "head": "S141 BATTERY GATE COMPLETE \u2014 FULL PUSH-GATE GREEN @e41acd91: fulltest rc=0 (all gates), JIT 1199/0/0/9 (suite 1225), EXE 1150/0, OBJ 1150/0, release rc=0, packed 1199/0, headerless 1172/0/36. THE FIRST BATTERY CAUGHT 3 BLOCKERS, all root-caused, own commits: (1) @93fcb312 the [tsubst-alias] probe's local_emit_name read takes the drift gate's allowed-exception marker (debug print, not symbol build). (2) @a19863b4 PRE-EXISTING (>= v0.95.2, proven on the shipped rpm binary): the basic-class-pattern rebind (forest instance materialization) registered ranked ctors under correct __oN Variable names but NEVER stamped FuncDef::local_emit_name (the ctor arm's skip condition pattern_emit_symbol==symbol is ALWAYS true for ctors) \u2014 ctor_call_symbol's FuncDef-only emitters (memberwise retbuf copy/shims/global-construct) degraded to canonical Class__Class: struct-with-vector by-value return called the DEFAULT ctor symbol with copy args -> c2mir 'too many arguments' under ANY forest bind. Fix = live-parity stamp at the rebind registration (symbol != canonical). Reducer tests/testvecmembercopy.mad; testvartplsigchain/-ns were the first suite coverage of the shape. DIAGNOSIS TOOLING: MADC_COPY_PROBE prints memberwise-copy ctor selection + EVERY ctor-set push site (matrec/mtmpl-skel/mtmpl-inst/mtmpl-reg/ool-def/import). (3) @e41acd91 DEVELOP REGRESSION (green v0.95.2+08-24, red at base 2e5f9a35): the missing-content-husk fallback re-included by BASENAME; <stat.h> is ambiguous (bits/linux/sys) and the s140 corpus growth (339->345 units) reordered the grove lookup onto linux/stat.h -> masked <sys/stat.h> consumers lost __S_IFMT (teststat headerless). Fix = re-include husks by CANONICAL PATH (resolve_include_path passes absolute verbatim; find_unit exact arm; read_resolved_include serves pack raw source). WHY bits/stat.h husks at all: the pack TU includes <fcntl.h> before <sys/stat.h>, so glibc's '#if !defined _SYS_STAT_H && !defined _FCNTL_H' guard recorded the _FCNTL_H disjunct a sys/stat.h-only consumer never reproduces. DEBUG METHOD BANKED: v0.95.2 rpm on the container = the instant pre-existing-vs-regression oracle (python newc-cpio extractor in tmp/v0952); /workspace/madc-base worktree (@2e5f9a35, built) = the base-bisect binary; tmp/hl_one.sh / hl_dm.sh = single-test headerless mask runners; -dM under the mask = the macro-table oracle; -v husk diagnostics name the branch dep. MIRRORS: test-status.md + CHANGELOG updated @ this commit. NEXT: (1) merge --no-ff -> develop, push; (2) /release + pull per the banked cadence (complete feature: the arc + embedding fixes + the two forest artifact fixes); (3) THEN Windows VT recon (docs/plans/2026-08-27-madcide-ide-controls.md section 'Windows console TUI', 7 items banked); (4) THEN Windows Run copy-AST-to-child (recon item 6 = the owner-call fork: cygwin-style copy vs forest-serialization-over-a-pipe; NOT a TUI blocker). BACKLOG unchanged: chained-get parser SIGSEGV (eh_red46), eh_red1 implicit-copy pack arm, eh_red8 const-ref operator[], captured-guest-output public API (owner), red36 pointer-cast warning; container strays: /workspace/madc-base worktree + /workspace/madc-base-stale-20260828 (old broken worktree moved aside) \u2014 both disposable.",
  "settled": [
   "name lookup finds the TEMPLATE, never a specialization \u2014 the ns-map write is gated at the registration tail; the forest-restore first-wins door remains (int-flavor get standalone still binds a forest instance by lookup \u2014 benign while the ranker owns concrete calls)",
   "multi-flavor TUs are THE reducer shape for instantiation bugs; single-flavor reducers pass straight through them",
   "the runner's pass criterion is .expect, not rc \u2014 probe with the runner, classify rc only against the oracle's rc",
   "splitting co-located fixes: scratch copy + git stash + Edit replay + diff-verify IDENTICAL, never checkout over uncommitted work"
  ],
  "validation": "Final content @349c8a92: the 4 battery failures all pass; embed gate both legs OK; embed_hello.cpp prints both lines; 22 container reducers at expected values; test*templ* 25/25, testenum* 11/11, test*vector*/stream/str/map/set green; libmadc-smoke green (negative control vs old lib exits 10); tsubst ratchet green. FULL BATTERY RE-RUN PENDING (first run caught the 4; fixes landed after) \u2014 IT IS THE NEXT ACTION, then EXE+OBJ lanes, then mirrors sync (CHANGELOG [Unreleased] already updated, uncommitted!), dupaudit families done, merge --no-ff, release+pull.",
  "next": "(1) OWNER RULING NEEDED: Windows Run copy-AST-to-child \u2014 the fork is banked in docs/plans/2026-08-27-madcide-ide-controls.md \u00a7'Windows Run \u2014 copy the AST to a child': (a) cygwin-style self-copy vs (b) forest-serialization-over-a-pipe (recommendation: b \u2014 rides cir_freeze, LOADED==parsed, packed-launch cost class). (2) The win TUI implementation slice per \u00a7'Recon findings (2026-08-28)': one win term_target twin under _WIN32 + taskio console probe/wait arms (PRE-REQ FINDING: io_probe_readable's _WIN32 arm is pipe-only \u2014 PeekNamedPipe FAILS on a console handle and returns 'readable' \u2192 busy-wake; the console arm = GetConsoleMode-classify + GetNumberOfConsoleInputEvents, [validate-win] non-key records). (3) /promote to master when the owner wants the v0.96.0 line public (3-platform gate). (4) Backlog: chained-get parser SIGSEGV (eh_red46), eh_red1 implicit-copy pack arm, eh_red8 const-ref operator[], captured-guest-output public API (owner), red36 pointer-cast warning."
 },
 "live_handoff": {
  "session": 149,
  "date": "2026-08-31",
  "branch": "feature/madcide-project-claude (push after this commit, tracking origin) on top of develop @c7c620f7 \u2014 develop itself is AHEAD OF ORIGIN by the php::dirname merge + a status commit (UNPUSHED, deliberate: rides the next merge wave; origin/develop = caa2fd94). Tree clean; owner strays donut.c/test.mad/testsort.mad untouched. Container tree synced at this content; NO engine change this session (bin/madc + the PE unchanged since s148 \u2014 QNAP binaries current).",
  "head": "S149c (same session, owner steering LIVE \u2014 three rulings banked): (1) OWNER LAW: madc code must be MORE ELEGANT than C++ (memory feedback_more_elegant_than_cpp \u2014 .c_str() key ceremony + kind-split walls = elegance debt; PHP/JS terseness is the dialect bar). (2) RULED: PHP's empty append accessor `rows[] = expr;` is wanted. (3) RULED \u2014 THE TARGET SPELLING for madcide's row pattern: `rows[] = {\"this\": \"that\", \"that\": \"this\"};` (collapses the var r1; r1[k]=v; rows.push(r1) dance madcide is FILLED with). ALSO RULED BY IMPLICATION: rows.push({...}) bare braced-list arguments should work (C++ [over.ics.list] shape, no array() wrapper). S149b (same session, after the vi slice): KEYED VALUE LITERALS + the HANG CLASS, owner-driven. The owner asked whether one-shot associative init existed \u2014 probing found `{ \"a\": 1 }` HANGS the parser (3 contexts) and `{ \"a\" 1 }` compiled SILENTLY to `{ 1 }`. Landed, in order: (1) @16e2f284 the hang-class fix \u2014 parseExpression pushes a bare ':' back (ternary/case/range-for convention) and every caller loop re-popped the same token; walls at BOTH violated contracts: the brace-list/ctor-args element loop refuses a non-','/close terminator, and parseExpression refuses a ':' that HEADS a parse (both stacks empty \u2014 kills the class for every caller; elvis ?: is pre-checked so no legal colon-head exists). Reducers testcolonheadexpr (.expect_err). (2) @9bc9ec75 the juxtaposition fix \u2014 the finalize returned exStack.top() dropping the rest silently; now 'Malformed expression: N operands...'; string-literal concat is LEXER-level so unaffected; reducer testexprjuxtapose. FULL BATTERY green on the walls (1243/0/9skip + all unit suites). (3) @cb419c3b the FEATURE: keyed elements `var m = { \"a\": 1 }` \u2014 parser records key:value pairs (TokenDecl::ctor_arg_keys parallel vector), array_list_init_call lowers a keyed element to carrier_slot_call (the ONE value-lvalue lane; madc_array_key_index = the one key-vs-index owner) + the registered operator= rows via carrier_row_def_for (generalized from carrier_push_def_for \u2014 push and operator= share the ONE element classification). SEMANTICS = the carrier's kind model, NOT PHP's unified array: string keys vivify OBJECT (map), int keys/positional vivify ARRAY (vector), mixing the two kinds = COMPILE-TIME refusal (the runtime kind refusal stays the catchable backstop); bare var key = element INDEX (documented subscript rule, .c_str() to key). First attempt seeded make_array for keyed lists \u2014 WRONG (key_slot vivifies null->OBJECT and refuses ARRAY); the kind separation is the deeper truth. Tests: testvalueinitkeyed (positive, 17-JIT/14-EXE/14-OBJ targeted set green incl. arrayslot + the ternary family), testvalueinitkeyedmix (.expect_err). CHANGELOG [Unreleased] carries all three entries. NOTE for /dupaudit (banked in commit @16e2f284): flush_forest_pending_globals' ctor-args mini-loop (parser.cpp ~23753) is a semantic COPY of the args-list parse (trusted pack tokens only today). S149: vi MODES-AS-SCOPES LANDED (the owner's twice-asked neovim.keys) + a standing prompt defect fixed. (1) THE VI SLICE @1664b9ad: profiles/neovim.keys = the MODAL personality (vim control chords in the main table; @normal/@insert = the vi mode tables \u2014 ALL data through the one parse_keys). parse_keys now stores scoped keys AS WRITTEN and scoped_action matches exact-then-lowercase: the vi case alphabet (a/A i/I o/O x/X) is DATA; lowercase-only scopes (every existing profile) stay case-insensitive. IdeSession::vi_event owns the GRAMMAR (session surface, per the design doc): counts multiply (2d3w=6), d/c pend + double to line-wise (dd/cc), motions = the existing shared action vocabulary run through apply_ide_event RECURSION (one dispatch, zero duplicated arms \u2014 kind 'action' never re-enters the vi arm), count+G = the :N landing rule, @normal ':' enters the ONE colon line, esc clears pending. A profile carrying @normal data LOADS IN NORMAL MODE (data decides \u2014 no name ladder); the ^N palette's new 'v' row (baked @modes v vimode) toggles modal editing anywhere, refusing with the road (^T Keymap -> neovim) where no @normal data exists. %M = showmode (-- NORMAL/INSERT --), %k doubles as showcmd (bag 'vipend'). State bags: vimode/vicount/viop/viopn/vipend. Honest absences documented in the profile, never pretended: no registers/put, no text objects, no repeat, no gg (1G is the door), BYTE-wise operator spans (dj = char span), w/b are the shared end-of-word motions (dw = vim's de shape). Gate 3 got a two-hop swap (joe->neovim->pico): the modal hop pins %M lighting, the pico hop pins the identical-tree clause + mode clearing. (2) FIX-WHAT-YOU-FIND @35c4e09d (own commit): the slice's live pty probe caught that a prompt opened from a FOCUSED pane row (tabwidth/theme/keymap) could NEVER COMMIT \u2014 the engine's focused-choice contract hands enter back as a CHOOSE and prompt_event ignored choose entirely (reproduced on plain joe, zero vi involvement; actions are deliberately inert under a prompt so the session looked stuck). Fix at the modal contract: prompt_event routes a choose through the same @prompt data as the enter key (pcommit -> finish_prompt); confirm keeps y/n only. Pinned headlessly (prompt-choose-commit) with the exact engine event shape. VALIDATION (all container): testmadcide headless GREEN + empty stderr; wine win64 domain GREEN on the s148 PE (WINEDEBUG=-all); check-madcide-seam + check-madcide-single-owners GREEN; live pty probes GREEN \u2014 palette v row + joe refusal, AND a REAL modal session (options-pane swap joe->neovim, -- NORMAL --, i -> -- INSERT --, typing, esc, :q! exit, alt screen restored). PROBE LESSON banked: a cell-DIFF renderer never re-emits unchanged cells, so pty byte-stream checks must strip escapes and search short changed runs ('INSERT'), or model the screen \u2014 the control probe's 'FAIL commit' after the fix was THIS artifact (its clean exit proved the commit).",
  "next": "ON REHYDRATE: scripts/resume.sh, this handoff FULLY, memory feedback_more_elegant_than_cpp, then EXECUTE the APPEND+LITERAL-EXPRESSION slice (design converged, coordinates scouted, NOT started \u2014 no code written): (A) RUNTIME madc_mir_backend.cpp: add `madarray_append_slot(void*)` beside madarray_push_target (~line 411): { auto &vec = madarray_push_target(v); vec.push_back(madc::value()); return &vec.back(); } \u2014 frozen/kind errors ride push_target; slot lifetime = until next growth (the vector contract, same as index_slot). (B) PARSER empty-[]: the subscript arm parser.cpp ~36827-36865 (`if !exStack.empty() && top is ttVariable` block): when peek after '[' is IMMEDIATELY ']' AND the receiver is carrier (is_madc_array, ref-unwrapped like the existing sub_recv code) -> TokenSubscript with NULL index (or an append flag member) typed madc_array_subscript_type(); non-carrier empty [] stays an error. TokenSubscriptExpr base too if cheap (r[\"k\"][] chains \u2014 optional). (C) CIR carrier_slot_call (cir_builder.cpp ~12102): append arm FIRST (index==NULL -> madarray_append_slot, one void* arg, ret {N_CHAR} ptr like the others); the key/index arms unchanged. is_carrier_keyed_subscript + the translate arms (~20386/~20452) must tolerate NULL index (they pass tsub->index through \u2014 guard before translate_expr). THEN `rows[] = 42;` etc. falls out via the parser's registered-operator= assign resolution (value_init_assign comment explains: the parser resolves carrier assignment to the operator= rows over the slot lvalue). (D) THE LITERAL AS AN EXPRESSION (the ObjTemp route \u2014 DECIDED over direct-into-slot: ONE mechanism serves assignment, rows[]=, AND call args; cost = one value copy per literal, PHP copy semantics, O(elements) unchanged): (D1) TokenObjTemp (include/tokens.h:1800) gains ctor_arg_keys + a carrier-list flag; (D2) parse_objtemp_ctor_arguments (parser.cpp 11978) reads key:value for a carrier obj_class \u2014 CONSOLIDATE the element+key reading with the decl loop's (parser.cpp ~65460, the keyed arm landed @cb419c3b) into ONE shared helper (dupaudit: 3 args-list loops exist now \u2014 decl, objtemp, flush mini-loop @~23753); (D3) the `= {` RHS hook: parseExpr_operatorArm's mid-expression tkOpBrc \u2014 when the pending opStack op is '=' AND exStack.top() datadef is_madc_array -> respell_braced_list_for_target(&ddARRAY, tb) (the class arm at 46718 pushes TokenDataType+'{' -> the functional route try_parse_functional_ctor 12180 builds the ObjTemp); (D4) respell_braced_list_call_argument (46748) becomes OVERLOAD-AWARE: today it reads the FIRST signature only \u2014 probes tmp/blarg.mad+blarg2.mad show rows.push({1,2}) refusing 'the parameter is not a class or aggregate' because push's first row is scalar; search the callee's overload set for a class param at idx (cir's class_method_def_by_param is the iteration precedent) -> respell to that class -> push({...}) works; (D5) CIR ObjTemp materialization (cir_builder.cpp 19669-19694, the __madc_objtmp_%d + m_pending_stmts pattern): add the CARRIER branch \u2014 array_storage_decl (carries the madarray_destruct cleanup attr) + array_ctor_call + the element loop EXTRACTED from array_list_init_call (@cb419c3b) into a shared helper(receiver-name, args, keys, origin); yields id(otname) like the class branch. The assign path then likely FALLS OUT: TokenAssign(slot, ObjTemp) resolves to the operator= value& row, object_arg_addr's ObjTemp arms (6312/6471/5377) materialize the temp. VERIFY the kind-mix compile wall (@cb419c3b) also runs for ObjTemp literals (share it in the extracted helper). (E) TESTS: new testvalueappend.mad (rows[]=scalar; rows[]={keyed}; rows[]={positional}; rows.push({...}); m={...} whole-replace; one-level nesting m[\"k\"]={...} via the slot; .expect from probed output; JIT+EXE+OBJ targeted set like @cb419c3b's). (F) THE MADCIDE SWEEP (the owner's motivating ask): madcide_core.inc row builders (modes_show, project_rows, build-cmd seeds, compose row pushes \u2014 grep `rows.push(r` and `var r[0-9]`) collapse to rows[] = {...}; testmadcide's EXISTING pins are the gate (rows content unchanged). Validation: targeted battery + wine testmadcide (WINEDEBUG=-all); fulltest rides the merge wave. OWNER-OPEN (flag, do NOT implement unruled): (i) kind-dispatched var keys \u2014 `{ kn: kv }` without .c_str(): needs runtime kind-dispatch AND the subscript-read rule change (a[v] element-read is owner-documented parser.cpp ~2800; literal-vs-subscript drift is the tension); (ii) THE UNIFIED MIXED CONTAINER (PHP array semantics: int+string keys in one value \u2014 dissolves the object/array kind split my compile wall enforces): substrate arc touching madarray_key_slot/index_slot, freeze, iteration, dump, php:: parity \u2014 the elegance endpoint, owner ruling + its own arc. EARLIER NEXT-ARC ITEMS STAND (merge wave when called \u2014 s149/s149b/s149c all ride it; madcide-as-binary owner-open; vi residues). ON REHYDRATE: scripts/resume.sh, this handoff, then read docs/plans/2026-08-31-madcide-gateway-and-code-server.md FULLY. NEXT ARC: (0) the s149b commits ride the same merge wave (fulltest already run ONCE on the wall content; the feature ran the targeted battery \u2014 the wave re-validates merged content anyway);  (a) the MERGE WAVE when the owner calls it \u2014 merge feature/madcide-project-claude into develop, FULL battery + all 4 push-gated lanes on merged content (wine lane: export WINEDEBUG=-all), record via scripts/lane_ledger.sh, THEN push develop (it carries the unpushed dirname merge); the repo-wide var-over-value tests/ sweep rides this wave; pre-merge: /dupaudit scoped to the madcide/texteditor subsystem (AGENTS.md rule). (b) OWNER-OPEN: the madcide-as-binary lane decision (design doc Open questions \u2014 libmadc link seam is the real slice). VI RESIDUES (demand-driven, documented in neovim.keys \u2014 refinements, not debt): w-as-next-word-start (vim w vs our de-shaped dw), gg, registers/put (needs a paste vocabulary), text objects, repeat, line-wise dj/dk spans. GATEWAY RESIDUES STAND: bag->member state migration rides later slices; commands-with-arguments = slice 3 (modal key->name lookup moves client-side there); presence/tiers then transports (LSP early). EARLIER RESIDUES STAND: project emit-lane cancellation chain (Stop pre-start only); frozen-project twin of --run-frozen; genuine-Win validation of project_run's child-of-self arm; emacs Meta + C-SPC engine key spellings; a future project-close verb must proj_touch. PARKED: macOS full-suite lane arc; upstream MIR va_block_arg PR (owner go). SESSION TRAPS: wine needs WINEDEBUG=-all; pty probes vs diff renderers (above); splitting a fix into its own commit = lift hunks with Edit, commit, restore byte-identically (md5 against the validated container copy), NEVER --unidiff-zero."
 },
 "mir_fork": "RETIRED AS A DEPENDENCY (2026-08-11, ADR 0002): MIR lives IN-TREE at third_party/mir \u2014 ordinary madc source, no pin, no lockstep, no fork release. It carries native _Complex, cleanup attribute, scope-depth layout fix, <=16-byte SIMD, the Mach-O executable writer, and the ABI fixes the CIR backend depends on. Build products in obj/mir/<variant>. github.com/derekbsnider/mir = frozen historical downstream + upstream-PR transport (vnmakarov/mir is upstream; remotes: mir-upstream, mir-pr). Last standalone state = tag madc-pre-subtree-migration = v1.0-madc.0.76.0.",
 "build_status": "green",
 "backend": "madc parser -> cir_node (MC11-IR) -> c2mir -> MIR is the SOLE backend. asmjit JIT and the Gecko parser/transpiler were both removed. --backend=mir aliases to cir; there is no --backend=jit.",
 "ir": "MC11-IR: the cir_node tree DERIVES FROM c2mir node_t (c2mir consumes the lowered C11 view) AND carries each node's originating lexed tokens + parse subtree + file/line/col (madc retains the high-level view). Deliberately BOTH. SET IN STONE -- see docs/rules/mc11-ir.md. --dump-cir shows the live backend tree; --emit=c11 is a SEPARATE renderer, never the c2mir feed.",
 "test_status": "v0.96.0 push-gate battery on final content @e41acd91 (container, 2026-08-28): fulltest rc=0 (all gates), JIT 1199/0/0/9 (suite 1225), EXE 1150/0, OBJ 1150/0, release rc=0, packed 1199/0/0/9, headerless 1172/0/0/36",
 "current_phase": "php dump intrinsics: pointers + enums DONE (session #103). ONE shape left \u2014 std::map/set/list. NOT because madc lacks begin()/end() (a hand-written iterator loop over a std::map works today) but because madc has ONE shared \"is this iterable\" recognizer, class_index_iteration_protocol, which knows only the POSITIONAL size()+operator[] shape \u2014 so there is no GENERATED iterator loop for either the range-for or the dumper. No release cut: the owner ruled no version bump until the arc is done.",
 "in_progress": "TRACK 7 PHASE 1 GREEN on feature/track7-hub-projections-claude @6ff79adf (pushed, UNMERGED \u2014 merge timing = owner call, UNRELEASED \u2014 OWNER LAW: releases mark COMPLETE features; RELEASE BOUNDARY = Colossal Cave Adventure TESTED FULLY PLAYABLE, and that release comes WITH a three-platform MASTER PROMOTION batching v0.93+v0.94+Track 7; VERSION stays 0.94.0). Wave battery: fulltest rc=0, 1106/0/0TO/9skip (+2 pilot tests), EXE 1065/0, release+packed 1106/0/0/9. Shipped: madcdis hub/verbs/uinode/prose/render_text/projection/world_text/adventure headers + <ns_ui> (first new namespace since mangled-direct, zero parser changes) + tests/adventure.world pilot + 2 negative-controlled fulltest gates + 4 doctest batteries (185 asserts). NEXT (owner-directed): examples/adventure = the ORIGINAL Colossal Cave Adventure on madc::ui+madc::dis (esr/open-adventure BSD-2 = data source + transcript oracle; advent.cpp/advent.py = idea mines); OWNER RULING: level-0 text render is dis-internal, external UI render libs are dat-side providers. READ the HANDOFF block atop docs/plans/2026-08-20-track7-phase1-text-adventure.md. Background: upstream wave 2 (#469, PRs #470/#471) awaiting Vladimir; v0.93/v0.94 promotion deferred.",
 "method_per_fix": "reduce -> attribute with GCC + Clang + the in-tree third_party/mir/c2mir path (for madc-path bugs use bin/madc --dump-cir, not emit-C) -> deepest-layer fix -> focused reducer -> make -C src fulltest -> authoritative remote fulltest/libcxx/EXE/OBJ as scope requires -> hosted/domain gates for target changes -> commit with Hypothesis/Layer/Searched/Oracle trailers -> push -> update KG first and mirror status/README/CHANGELOG/ROADMAP. MIR is ordinary in-tree source; there is no fork pin or lockstep release.",
 "known_pre_existing_gaps": [
  "SCRIPT `value` IS NOT YET THE 32-BYTE madc_value STRUCT (recorded 2026-08-17, owner: not to be taken on now). Symptom: `value f()` returned BY VALUE emits `int f()` and silently returns nothing (exit 0, empty output) \u2014 func_def's return-type arm -> type_list -> append_type_specs has no dtARRAY case and falls through to int. That is why php::print_r returns madc::value & rather than madc::value. CORRECTED PREMISE: a C type DOES exist \u2014 madc_value (include/madc_api.h:52) is a tagged 32-byte aligned(16) struct, and madc::value merely CONTAINS it plus two unique_ptr members (32+8+8 = 48 = the emitted long long[6]); the opaque-buffer lowering exists because of those OWNING C++ members, not a missing type. MEASURED: _array/_object are private with no friends and all 40 refs are in ONE file (src/madc_value.cpp) \u2014 12 mutations, 28 reads; the cell finalizer is already wired and called from generated code (cir_builder.cpp:25789/25797); the is_array_object sites would mostly RETIRE. HARD PART: the class deep-copies the containers while the cell path shares, so the migration needs copy-on-write at those 12 sites \u2014 a semantic change with its own oracled tests. Full write-up: docs/plans/2026-06-12-type-table-value-abi-design.md section 9. Sibling defect, also open: assigning to a `value &` PARAMETER is wrongly rejected ('assignment of incompatible value').",
  "FIXED 2026-06-12 (this session): multi-declarator ctor-init hang (`Q a(1), b(2);` \u2014 ctor-syntax decl path now does the standard comma-continuation; tests/testctorcomma.mad); VLA-variable sizeof returned 8 for 1D AND multidim AND typedef VLA vars (runtime dims now captured into hidden __madc_vla_dim_* locals at declaration, sizeof(var) builds the runtime product; tests/testvlasizeof.mad); global const array dim read 0 (parse-time-known const scalar int initializers now baked into var->data + vfCONSTBAKED admits the CIR read-fold so `const int H=G+3;` emits a constant file-scope initializer; tests/testconstdim.mad); testfortypedcomma flake not reproducible at live HEAD (25 consecutive green runs) \u2014 resolved by intervening parser work.",
  "VLA sizeof in CONSTANT-required contexts (e.g. `int b[sizeof(vla_a)]`) still folds to 8 via the constant evaluate_type_query path (the expression-path fix doesn't reach it); alignof(vla_var) still reports pointer alignment 8 (gcc: element alignment). Same family, much rarer.",
  "VLA NOT DEALLOCATED ON BACKWARD GOTO: a `goto` jumping back to before a VLA's declaration must deallocate the VLA (gcc/C semantics; gcc-torture 20040811-1.c). madc lowers VLAs as malloc + __attribute__((cleanup)), which frees only on SCOPE EXIT, not on the backward goto re-entering the scope \u2014 so a backward-goto loop over a VLA LEAKS one allocation per iteration. 20040811-1.c leaks ~2GB over 1e6 iterations; it passes only when memory is available (it OOM-SIGSEGV'd when the MIR fork's 128MB code-holder reservation reduced headroom \u2014 see mir_fork; arch-gated off x86-64, so it passes again). Proper fix: free the VLA at the backward goto. Separate future slice.",
  "MadArray/MadValue namespacing is an open API/ABI follow-up: moving the real C++ types to madc::array and madc::value would give PHP array helpers a normal namespace type surface, but compatibility aliases alone will not preserve old C++ mangled names.",
  "Embedded <iostream> output remains partially staged: cout/cerr/clog still use header-defined stdio-backed ostream bodies. Follow-up is to replace that output surface with real libstdc++ declarations/operators, matching the std::cin direction; do not add stdio.h/FILE* stream bridges.",
  "Real system-header PCH regeneration is deferred until PCH preserves include-guard/macro state; broad regenerated blobs currently duplicate transitive typedefs/declarations.",
  "Explicit --std=c++ real iostream preprocessing reaches c2mir but fails on unresolved class/member aliases such as char_type, iter_type, iostate, __streambuf_type, __ostream_type, and __ios_type. Fix this generically through class/member alias resolution, not per-std shims.",
  "libmadc in-process compile/exec/eval stubbed (asmjit-era machinery removed); ~100 unit tests skipped until CIR reimplementation."
 ],
 "phases": {
  "language_core_phase_1_to_6": "achieved on the removed asmjit backend; being re-established on CIR via the parity campaign.",
  "phase_4_2_libmadc_api": "in_progress (security policy / structured diagnostics / engine-owned IO landed; in-process compile/exec/eval stubbed).",
  "data_channel_streaming_process_flow": "done @cd1f19c6 - lazy typed flow, dependency-free channels including TCP/UDP/UDS, explicit process pumping, DSV native streaming, and ABI-compatible extension seams.",
  "cpp_class_model": "std::string, streams, std::sstream, std::vector/map/set/list-style containers, and user classes flow through the same parsed-header/user-defined object model: generic overload resolution, mangling, ctor/dtor, and retbuf handling. Legacy dtSTRING/tkSTRING/tkVECTOR/ns_stl shortcuts are retired; the finish-line gate is 0 on develop.",
  "gecko_parser_and_transpiler": "REMOVED",
  "asmjit_jit_codegen": "REMOVED"
 },
 "deferred": [
  "Optional madcdat service providers: libcurl-backed HTTP/HTTPS/REST/FTP/S3 as appropriate, mail protocols, and dependency-backed database/API endpoints. Core TCP/UDP/UDS transports are already in madcdis.",
  "Native streaming migration: DSV is incremental; migrate suitable FLR/VLR/external drivers and future shipped SourceAdapters through the optional extension seams while preserving legacy vector APIs.",
  "eval/exec + REPL (mode-4): reimplement on CIR->c2mir->MIR (a madc_cir_execute variant returning the computed value). ~100 unit tests skipped as the future spec.",
  "P2.11-13 std-dialect subsystem (vision keystone): keyword/feature->standard REGISTRY -> gating -> de-hardcode the c2mir target std -> standards conversion. See docs/plans/cpp-support.md.",
  "SIMD (Track 1.6, KG Decision{simd_raise_mir_upstream}): in progress on /workspace/mir feature/simd-vector-support-codex @ 2ffebff. Checkpoints now cover the first memory-backed c2mir vector_size frontend slice, a partial MIR v128 floor with i32 add/sub/bitwise/signed comparisons, unsigned v4u32 equality/inequality/ordering, same-size v128 vector casts, v4i32/v4u32 vector shifts, v4i32/v4u32 multiply/divide/modulo, __builtin_convertvector/__builtin_shufflevector on v128 vectors with 4- and 8-byte arithmetic lanes, GCC __builtin_shuffle on same-type v128 sources with signed/unsigned runtime mask modulo handling, v128 integer vectors with 1-, 2-, 4-, and 8-byte lanes, uint16x8_t coverage, Clang-style __builtin_vectorelements, width-changing __builtin_shufflevector results from v128 sources, non-v128 source-width support for __builtin_shufflevector via memory-backed scalar lane copies, generic non-v128 GCC __builtin_shuffle support through memory-backed scalar lane copies, floating-lane arithmetic/comparisons for v128 and non-v128 float/double vectors through memory-backed scalar lane lowering, x86-64 v128/v64/v32/v16/v8 integer-vector parameter/return ABI support, packed MIR v128 f32/f64 arithmetic and comparison opcodes for vector_size(16) float/double operations, and packed MIR v128 i8/i16/i32 add/sub and comparison opcodes, packed MIR v128 i64 add/sub opcodes, plus packed MIR v128 i8/i16/i32 multiply coverage, packed MIR v128 i8/i16/i32 scalar-count shift coverage, packed MIR v128 i64 scalar-count shift coverage, and packed MIR v128 lane-count shift coverage, qword vector comparison scalar-fallback masks, packed v128 i64 equality/order, scalar-condition vector conditional expressions, and same-element-count __builtin_convertvector across supported vector widths, non-v128 integer vector operations through scalar-lane lowering, and non-v128 same-size vector casts through memory-backed block copies, same-size integer scalar/vector reinterpret bitcasts, GNU declaration-spec and leading type-name vector attributes, expression-valued vector_size attributes, __builtin_abort, __builtin_memcmp, __builtin_copysignf, and __builtin_nan lowering, all 37 GCC c-torture execute vector-construct files checked in and passing under C2MIR -ei/-eg, including exact pr92618/pr94524-{1,2}/pr53645/pr53645-2/pr109040/simd-{1,2,4,5,6}/pr65427/pr60960/pr70903/pr85169/pr108292/pr105613/pr72824-2/fp-cmp-cond-1 runtime coverage, empty GNU asm barrier parsing, narrow address-taken register rvalue extension, union-alias preservation through array subscripts, one-lane unsigned __int128 vector equality broadened to one-lane __int128 vector arithmetic/bitwise/unary/comparison/shift/compound/inc-dec lowering including signed/unsigned div/mod helper-call lowering, mixed-signedness vector shift-count type compatibility, and mixed-source-width __builtin_shufflevector support, and GCC vector inc/dec lowering, plus Clang ext_vector_type support including odd lane counts, and MIR_T_V128 text/binary data I/O round-trip support, and MIR v128 lane-count shift opcodes with direct MIR/C frontend coverage. No known <=16-byte SIMD gap remains after one-lane __int128 vector div/mod helper-call lowering; later work is AVX/YMM register ABI for 32-byte-and-larger external vector boundaries plus the broader generic-vector extension to MIR + vector ops/registers/interpreter/codegen, DESIGNED FOR UPSTREAM. Interim: scalarize for JIT + emit-C->gcc/clang."
 ],
 "canonical_refs": {
  "data_channel_plan": "docs/plans/2026-08-07-data-channel-streaming-process-flow-plan.md (implemented guideline; actual code preserves legacy vtables through optional extensions)",
  "handoff": "docs/plans/2026-07-16-CODEX-HANDOFF-pattern-lane-perf-rungs.md (Codex hand-back @b2b2cf5e; R3 stashed; exact-HEAD gates listed)",
  "forest_perf_result": "docs/plans/2026-07-14-CODEX-HANDOFF-forest-perf-instantiate-cirbuild.md (Mission 1 result @cbcb79b6, merged + independently verified)",
  "parity_worklist": "docs/parity/root-cause-worklist.md (root-cause clusters + OTHER-38 triage)",
  "torture_failset_baseline": "docs/parity/torture-failset-current.txt (diff next run for regressions)",
  "backend_decision_adr": "docs/adr/0001-cir-c2mir-backend.md (+ .claude/rules/backend-strategy.md) -- SETTLED, don't re-litigate",
  "lowering_vs_raising": ".claude/rules/lowering-vs-raising.md",
  "ir_definition_rule": "docs/rules/mc11-ir.md + .claude/rules/mc11-ir.md",
  "vision": "docs/plans/madc-vision-and-invariants.md (invariants I1-I8) + docs/plans/cpp-support.md (compliance roadmap)",
  "roadmap": "docs/plans/ROADMAP.md",
  "promote_gate": ".claude/rules/branching.md -- develop->master gated on CIR parity with master",
  "agent_memory": "project_cir_parity_campaign, project_backend_decision, project_north_star_c23_cpp23, project_mc11_ir, feedback_promote_parity (FalkorDB graph madc-knowledge via scripts/kg_query.sh + ~/.claude/projects/-workspace-madc/memory)"
 },
 "current_focus": "RELEASED as v0.86.0 ON DEVELOP (not pushed). The session-#104 handoff's three turn-3 items are all landed: the libc fallback signatures (both halves), the count/size ruling, and the range-for auto element. OPEN, recorded: (a) the c++locale.h ARGUMENT-typing residual (typeof declaration -> __locale_t as integer; plan doc names it), (b) `for (value v : a)` does not compile (loud, recorded in the auto plan doc), (c) `puts` registered as returning void where C says int (noted in the libc plan doc, own blast radius), (d) handoff \u00a74 leftovers: raw-pointer end() tightening, D1 (value f() emits int f()), D2 (value & parameter assignment), _Alignas on a struct member in c2m, std::unordered_map does not parse (__int_traits), for (int v : plain_struct) diagnostic quality, two macOS MIR-blob causes.",
 "last_update": "2026-08-28 v0.96.0 release (s141)",
 "arc_rollup": "ALTITUDE MAP (rungs -> arcs -> tasks; maintained at milestones). [1] FOREST/TRACK-B INFRASTRUCTURE \u2014 COMPLETE: pack-time drain (rung 1, v0.34.0), per-segment zstd compression 101->9.26MB (#37), MIR module cache rungs 1-3 (#42), class-pattern live-capture R1-R5 (#39-41,#43), multi-TU --project milestone (@94120962, project_gate in fulltest), bind lane byte-identical to live (forest_bind_gate 18/18). The packed suite IS the arbiter for everything since. [2] CORRECTNESS BURNDOWN (what exercising the packed state exposed) \u2014 (a) VIRTUAL-INHERITANCE ARC, one family #35->#36(s1,s2)->#47->#49: member-subobject construction, dynamic Itanium vbase offsets + vcall convention + vbase groups, member access through vbase views + member_vbase freeze v32, ref-bind/ref-return unwraps, prologue-only vtables + plain-base RTTI. CLOSED \u2014 #48 landed @def0db36 (manipulator save-the-lhs). (b) one-off kills: #33 istringstream shift, #34 family-D drain gaps, #44/#45 loop scope, #46 cast-arrow. Arbiter trajectory across the burndown: 697->704 (growth = new oracle tests locking fixes). [2c] EMIT-C + CONSTRUCTION COMPLETENESS: #50 landed @04635221 (extern-global cleanup gate + typed dtor externs -> iostream TUs gcc-clean, old #20 item closed); #51 landed @cf73c3ad (complete-object construction assembler: new[]/delete[] Itanium cookie, stack class arrays, ctorless user-ctor base chains, heap/member vbase ctors; arbiter 706->708). [2d] LANGUAGE-SURFACE BURNDOWN (batch queue #52-#54, all landed 2026-07-18): #52 pure virtual (@6fbadabe), #53 explicit dtor calls + placement-new convergence (@a0d82a83), #54 #if evaluator ?:/##/typeof-casts -> strict --std=c++11 lane serves real headers (@afc85776); arbiter 710->713. [3] OPEN LEDGER: CLEARED 2026-07-18 (batch #55-#59: SFINAE-quiet @1f382e8c, array dtors @3339e04d, dtor names @1b12c1bb; #58 C1 @bdd129fe; #59 A3 verified already-landed). Unfiled polish only: sizeof(template-id) parse gap; instantiation-diagnostic file/line misattribution (cosmetic). [4] NEXT ALTITUDE: the governing plan docs/plans/2026-07-04-data-substrate-first-customer-PLAN.md track order \u2014 B4b-B4d (forest default-mode design) then A2 snapshot:// driver / C2-C4 feature tracks. | [3a] data-substrate track C OPENED: C1 madcdis core-ification landed @bdd129fe (task #58) \u2014 interface headers at their permanent madcdis home, substrate core in both configure modes; C2-C4 follow after track B. | [5] AOT/ELF ARC (docs/plans/2026-07-19-mir-aot-elf-plan.md, owner-directed 2026-07-19/20): R0 fork survey (#81) -> R1 madc -g source-level gdb on the JIT incl. .debug_frame CFI (#82, fork @b6a411fa) -> R2 object-capture -> linkable ELF .o, library API at both layers, object lane 1139/0 (#83, fork @dc01374b, madc @c5741b27; R3 writer unification largely closed by construction). R4 LANDED @e86ac538 (fork @5c461803): madc -c/-o DIRECT native output \u2014 MIR assembles ET_EXEC itself (no cc/ld; owner: external linking was only ever a TEST), --exe lane live 709/729. R4 RUNG COMPLETE 2026-07-20 (task #85 closed): direct ET_DYN -shared landed (cir_run_link DELETED \u2014 zero external-toolchain product paths; fork @c0cb2b47), --project native emission (per-TU .o + whole-program -o/-shared; NATIVE SMAUG: 51 TUs -> 5.0MB MIR-assembled ELF, boots + serves TCP), exe burndown to CLEAN 717/0 (void-main->int lowering + exe_skip fixture). NEXT AOT RUNGS (tasks not yet filed): R4b execute-.o-as-cache (master parity), R5 DWARF in .o, R6 PIC; Mach-O/PE assemblers later behind the same MIR_object seam.",
 "promote_gate": ".claude/rules/branching.md \u2014 develop->master gated on the gcc-torture class-(a) burndown (met; stamp in docs/parity/failset-classification.md). PROMOTED 2026-08-07: master = v0.69.0 @034cfd28 (tag v0.69.0 + GitHub Release --latest; README conflict vs owner's direct @75bf5b7d resolved to develop's rework per owner). Fork master FF'd to 4573a0f3 in lockstep.",
 "validation": "v0.91.0 battery 2026-08-19: fulltest 1096/0/0TO/9skip rc=0; EXE 1056/0; OBJ 1056/0; census 1105 tests / zero warnings; release rc=0; packed 1096/0/0/9; headerless 1069/0/36skip (testiomanip carries a headerless_skip: <iomanip> is a documented pack-corpus blocker \u2014 forest_pack_headers.txt v3); version_flag_gate 0.91.0"
}