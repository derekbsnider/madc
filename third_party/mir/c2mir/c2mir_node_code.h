/* c2mir_node_code.h — Node code enum for c2mir AST.
   Extracted from c2mir.c for use by external AST builders.
   Part of MIR project (madc fork).

   Values MUST match the node_code_t enum in c2mir.c exactly.
   Generated by expanding c2mir.c's REP macros.
*/

#ifndef C2MIR_NODE_CODE_H
#define C2MIR_NODE_CODE_H

typedef enum {
  /* Literals */
  N_IGNORE = 0, N_I, N_L, N_LL, N_U, N_UL, N_ULL, N_F,     /* 0-7 */
  N_D, N_LD, N_CH, N_CH16, N_CH32, N_STR, N_STR16, N_STR32, /* 8-15 */
  /* Identifiers and special operators */
  N_ID, N_COMMA, N_ANDAND, N_OROR, N_STMTEXPR,               /* 16-20 */
  /* Comparison and assignment operators */
  N_EQ, N_NE, N_LT, N_LE, N_GT, N_GE, N_ASSIGN, N_BITWISE_NOT, /* 21-28 */
  /* Logical, bitwise, shift operators */
  N_NOT, N_AND, N_AND_ASSIGN, N_OR, N_OR_ASSIGN, N_XOR, N_XOR_ASSIGN, N_LSH, /* 29-36 */
  N_LSH_ASSIGN, N_RSH, N_RSH_ASSIGN, N_ADD, N_ADD_ASSIGN, N_SUB, N_SUB_ASSIGN, N_MUL, /* 37-44 */
  N_MUL_ASSIGN, N_DIV, N_DIV_ASSIGN, N_MOD, N_MOD_ASSIGN, N_IND, N_FIELD, N_ADDR, /* 45-52 */
  /* Unary, ternary, inc/dec, sizeof */
  N_DEREF, N_DEREF_FIELD, N_COND, N_INC, N_DEC, N_POST_INC, N_POST_DEC, N_ALIGNOF, /* 53-60 */
  N_SIZEOF, N_EXPR_SIZEOF, N_CAST, N_COMPOUND_LITERAL, N_CALL, N_GENERIC, N_GENERIC_ASSOC, N_IF, /* 61-68 */
  /* Control flow */
  N_SWITCH, N_WHILE, N_DO, N_FOR, N_GOTO, N_INDIRECT_GOTO, N_CONTINUE, N_BREAK, /* 69-76 */
  N_RETURN, N_EXPR, N_BLOCK, N_CASE, N_DEFAULT, N_LABEL, N_LABEL_ADDR, N_LIST, /* 77-84 */
  /* Declaration specifiers */
  N_SPEC_DECL, N_SHARE, N_TYPEDEF, N_EXTERN, N_STATIC, N_AUTO, N_REGISTER, N_THREAD_LOCAL, /* 85-92 */
  N_DECL, N_VOID, N_CHAR, N_SHORT, N_INT, N_LONG, N_FLOAT, N_DOUBLE, /* 93-100 */
  /* Type qualifiers and specifiers */
  N_SIGNED, N_UNSIGNED, N_BOOL, N_STRUCT, N_UNION, N_ENUM, N_ENUM_CONST, N_MEMBER, /* 101-108 */
  N_CONST, N_RESTRICT, N_VOLATILE, N_ATOMIC, N_INLINE, N_NO_RETURN, N_ALIGNAS, N_FUNC, /* 109-116 */
  /* Declarators and misc */
  N_STAR, N_POINTER, N_DOTS, N_ARR, N_INIT, N_FIELD_ID, N_TYPE, N_ST_ASSERT, /* 117-124 */
  N_FUNC_DEF, N_MODULE, N_ASM, N_ATTR, /* 125-128 */
  /* _Complex support (madc fork) */
  N_COMPLEX, N_REALPART, N_IMAGPART, N_CF, N_CD, N_CLD,      /* 129-134 */
  /* madc extensions */
  N_DEFER, N_CLASS, N_METHOD, N_ASM_OPERAND, N_ASM_STMT,       /* 135-139 */
  N_INT128,                                                     /* 140 */
} c2mir_node_code_t;

/* Alias so c2mir.c internals still compile */
#ifndef C2MIR_INTERNAL
typedef c2mir_node_code_t node_code_t;
#endif

#endif /* C2MIR_NODE_CODE_H */
