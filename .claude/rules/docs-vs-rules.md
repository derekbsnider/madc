# Documentation vs Rules — Separation of Concerns

- Rules (`.claude/rules/`) contain only rules — prescriptive instructions for how to work
- Docs (`docs/`) explain the reasons behind rules and document features/systems
- Don't duplicate content — cross-reference instead
- When adding new features: rules for the constraints, docs for the explanation
- Consider `docs/rules/` for rule-related documentation that explains the "why"
