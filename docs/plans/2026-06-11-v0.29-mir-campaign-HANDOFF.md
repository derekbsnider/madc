# HANDOFF — v0.28.0 + v0.29.0 released; MIR/C2MIR campaign complete; next = `===` / eval C / promote backlog

**Date:** 2026-06-11 (late evening) · **Supersedes**
`docs/plans/2026-06-11-spaceship-track-complete-HANDOFF.md` as the cold-restart
contract (its mechanism map for the `<=>` work remains valid — read it for `<=>`
internals).

## STEP 0 — rehydrate

```bash
bash scripts/resume.sh
```

Then trust the LIVE repo over any doc, including this one
(`feedback_verify_over_stale_handoff`).

## State at handoff

- **`develop` @ `65d9dd9` = v0.29.0, RELEASED + PUSHED** (plus this handoff commit,
  local-only — never push develop without a `/release`). Two releases shipped today:
  - **v0.28.0** (`c14c69e`) — the C++20 `<=>` compliance track complete
    (P2.15: gating, real `<compare>`, hidden-friend bodies, token lowering,
    rewritten candidates, `= default` synthesis).
  - **v0.29.0** (`65d9dd9`) — backend correctness: **MIR-gen `-O2` = `-O1`
    gcc.c-torture parity** (1567 byte-identical failsets; was 1559) + fork synced
    with upstream.
- **MIR fork @ `9ab36fb`** (github.com/derekbsnider/mir, develop, PUSHED;
  `MIR_COMMIT` pinned same-commit). Ten commits over the old `2ffebff` pin:
  - PR #430 cherry-picks (LADDR OUT_FLAG; jmpi → simplified RA).
  - **Five self-root-caused O2-parity fixes** (each first reproduced on stock
    `c2m -eg`/`-ei`): `6c83111` VA_BLOCK_ARG missing from GVN
    `fixed_place_insn_p` (struct va_arg CSE → SSA corruption → copy_prop SIGSEGV);
    `058893f` **lost-copy** out-of-SSA hazard (self-loop block's back-edge phi
    copy precedes the tail branch → `while (i-- > 0)` one short);
    `7a90960` **union alias-conflict relation** (new MIR core API
    `MIR_add_alias_conflict`/`MIR_alias_conflict_p`; c2mir registers
    union-class ↔ member-leaf-class conflicts; also fixed alias_ctx malloc size);
    `bcdd0c5` `optimize("-fno-strict-aliasing")` honored per-function (c2mir sema
    flag → `get_type_alias` returns 0, survives MIR inlining);
    `cc74fef` `_MIR_get_ff_call` XMM slot over-counting for mixed-class block
    varargs (Win64 copy-paste; interp FFI read stale XMM slots).
  - PR #432/#433/#434 cherry-picks (GVN narrow-reload extension; jump_opt
    laddr/lref label liveness; aarch64 LD `%16` rounding).
- **madc front-end legs of the campaign** (in v0.29.0): lexer attribute allowlist
  gains `optimize` (`src/lexer.cpp` ~3802); `Program::consume_gnu_attributes`
  detects `optimize("…no-strict-aliasing…")` → `pending_no_strict_aliasing`
  (Program member, cleared in `_tokenizer_init`); `parseDeclaration` consumes a
  GNU attribute BETWEEN specifiers and declarator; the function parse applies
  pending → `FuncDef::no_strict_aliasing`; `CirBuilder::func_def` forwards an
  `N_ATTR(optimize, N_STR)` in the FUNC_DEF specs (vector_size/cleanup convention).
- **Gates all green at handoff** (fork `9ab36fb` × madc `65d9dd9`): fulltest
  **572/0/0/18** exit 0 both check gates GREEN; torture **O1 1567 byte-identical**
  to `tmp/failset_lsq.txt` AND **O2 = O1 byte-identical** (`tmp/failset_round2_*`);
  MIR `make test` exit 0 (+3 new regression tests, 1124/2248 interp / 1128/2256
  gen); SMAUG soak green.

## Attribution policy (user ruling 2026-06-11 — ALWAYS trace patches to origin)

Four layers, documented in `docs/parity/mir-fork-community-patches.md`:
author-preserving cherry-picks (verbatim messages for upstream patch-id dedup) +
**git notes** `refs/notes/commits` on the fork (`ADOPTED-FROM: upstream PR
vnmakarov/mir#NNN`) + `ADOPTED-FROM` source comments for hand-applied patches +
the narrative doc. cyanogilvie's MIR activity is driven by **slimcc's mir-backend**
(github.com/cyanogilvie/slimcc/tree/mir-backend) — WATCH that tree and upstream
`vnmakarov/mir` issues/PRs for more fixes; PRs #418/#420 were already carried via
the theMackabu backport.

## NEXT QUEUE (in order)

1. **`===` STD_MADC dialect operator** — strict equality over `madc::value`
   (queue item 4; gate behind STD_MADC via the std-floor machinery, see
   `test3waygate` precedent for std-gated tokens).
2. **eval package C** — the 38 `test_libmadc_program` skips
   (`docs/plans/2026-06-10-libmadc-eval-on-cir-plan.md`: register_function,
   get/set_global, string call marshalling, fork/limits, AOT save/load).
3. **41 class-(a) promote backlog** — K&R old-style definitions ×23 is the
   biggest lever (std-gated < C23, never C++); gate = ≥1608/1652
   (`docs/parity/failset-classification.md`, root-cause worklist in
   `docs/parity/root-cause-worklist.md`).
4. Non-blocking: `<=>` precedence corner; **O2 perf evaluation** (O2 is now
   *correct* — measure whether it beats O1 before considering flipping the
   default `madc_opt_level` 1 → 2).

## KNOWN PRE-EXISTING GAPS (don't re-discover)

- `Q a(1), b(2);` ctor-arg multi-declarator HANGS the parser (split declarations).
- `<string>` at `--std=c++20` fails `Unknown namespace ranges` (untriaged).
- `testfortypedcomma` flaky; 1D-VLA sizeof = 8; global-const array dim reads 0.
- Upstream #426: lref data can't round-trip `m2b → mir-bin-run` ("A label not
  from any function in lref") — pre-existing, in-process JIT unaffected; it's why
  `c-tests/mir/issue424.mir` only passes via the suite runner.

## Gotchas (new this session + still-live)

- **c2m flag order**: everything AFTER `-ei`/`-eg`/`-el` becomes the PROGRAM's
  argv — put `-O2`/`-dg2` BEFORE the exec flag (`c2m f.c -O2 -dg2 -eg`). This
  cost an hour chasing "empty debug output".
- Torture at O2: `scripts/run_gcc_testsuite.py --madc tmp/madc_O2.sh` (a 2-line
  exec wrapper; the runner has no -O passthrough).
- Debug c2m with asserts + gen DEBUG: build recipe in
  `tmp/c2m-debug/` (gcc -O0 -g of mir.c mir-gen.c c2mir.c c2mir-driver.c).
- Valgrind on c2m distinguishes generator UAFs from generated-code crashes
  (JIT frames show as `???`; gen frames show symbol names).
- `make` in /workspace/mir does NOT relink `mir-bin-run` reliably — `make` full
  target or check binary mtimes (cost one phantom-failure loop).
- madc `make -C src` does NOT relink `bin/test_*`; NAS mtime trap; capped runs
  `( ulimit -t 120; timeout 180 … )`; ONE heavy job at a time; SMAUG manifest is
  `/workspace/MadSMAUG/compile_commands.json` (NOT src/); deep-header errors
  mis-attribute with a VIRTUAL line (`-v` + `-E`); reducers carry original flags.

## Method per fix (unchanged)

reduce → attribute (gcc + clang + stock `/workspace/mir/c2m` `-ei`/`-eg`; for
madc-path bugs `--dump-cir`, NOT emit-C) → deepest-layer fix → rebuild →
fulltest → FULL torture failset-diff (O1; run O2 too when touching mir-gen) →
SMAUG soak → commit → push → fork changed? push fork + bump `MIR_COMMIT`
same-commit → sync mirrors (claude_status + README + CHANGELOG + ROADMAP + KG +
memory).
