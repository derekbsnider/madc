# HANDOFF — after the three-platform release (session #97, 2026-08-17)

**Read this fully before acting. Assume a cold start.** Run `bash scripts/resume.sh`
first: it prints live git state and orphaned background jobs, which a compaction
summary cannot. Then read `claude_status.json` (canonical) and this file.

The previous handoff, `docs/plans/2026-08-16-three-platform-release-HANDOFF.md`,
is **CLOSED** — its §2 chain is superseded (see "the correction" below). Do not
work from it.

---

## 1. Where things stand

**v0.82.0 SHIPPED** — the first release carrying working Linux, macOS and
Windows artifacts together.

- `develop` and `master` are both at v0.82.0; `master` was v0.76.0 before it.
- Tag `v0.82.0`, GitHub release published `--latest`, **six assets** attached
  and checksum-verified (`sha256sum -c SHA256SUMS` → 5×OK):
  `madc_0.82.0-1_amd64.deb`, `madc-0.82.0-1.x86_64.rpm`,
  `madc-0.82.0-macos-arm64.tar.gz`, `madc-0.82.0-macos-x86_64.tar.gz`,
  `madc-0.82.0-windows-x86_64.zip`, `SHA256SUMS`.
- Release binary archived both hosts: `tmp/release-bins/madc-release-v0.82.0`.

Since the release commit, on `develop`, **not yet released**:

```
e9e92af9  madc answers --version     (src/madc.cpp, new scripts/version_flag_gate.sh, wired into fulltest)
```

⚠️ **Per the owner's release-cadence rule, a feature merge to `develop` comes
WITH a release.** `e9e92af9` is a src/ change, so the next session should either
cut v0.83.0 for it or fold it into the next feature's release. It is verified
(flag, both spellings, cross-target line, gate + negative control) but has NOT
had a suite run — the owner explicitly waived one for it.

## 2. Validation of record (v0.82.0, the exact released tree)

| lane | result |
|---|---|
| Linux fulltest (JIT) | 1054 / 0 / 0TO / 9 skip |
| Linux exe · obj | 1021 / 0 each |
| Linux packed · headerless | 1054/0/0/9 · 1028/0/0/35 (mask verified) |
| Windows wine · headerless-win | 1011/0/0/52 · 1011/0/0/52 (mask verified) |
| macOS both arches | `forest_pack_darwin` OK (835 units) · `verify_macho_release` OK |
| macOS on hardware | **mac_battery 7 passed / 3 failed** — exact v0.77.0 parity |
| gates | `forest_bind_gate` GREEN 25/25 · `macho_exe_dylib_gate` OK · trailers 431/0 · warnings 0/0/0 |

The 3 remaining Mac failures are **pre-existing and also shipped in v0.77.0** —
C++ groves (`ostringstream::str`), the `madc::value` intrinsic (SIGSEGV 139),
and the `exec://` channel. Do **not** chase them as regressions.

Mac host: `derek@192.168.1.79`, macOS 15.3.2 arm64, **`LC_ALL=C`**. Copy the
tarball + `scripts/mac_battery.sh` to `/tmp` and run the battery against the
extracted `bin/madc`.

## 3. The correction that matters (#64)

The pre-compaction chain said the darwin forest held only `<iosfwd>`'s **forward
declaration** of `ios_base`. **That was wrong.** `ios_base`'s full definition was
recorded from `.../v1/ios` all along; only the FIRST `--dump-forest` rows are
`iosfwd`, and the earlier read stopped there.

What it actually was: a **class-nested enum tag was STAMPED a forest type-id but
never RECORDED**. `[basic.scope.class]/1` makes such a tag a member, so
`TokenENUM::parse` deliberately keeps it out of `datatype_map` (the guard that
stopped `money_base::part` leaking `part` to file scope) — and the freeze's
enum-recording walk read exactly that map. `forest_serialize_type_id` had
already stamped the tag an id as a cross-reference, so the id resolved to a
`DK_NONE` record. `std::ios_base` carries `event_callback *__fn_` whose
signature takes `ios_base::event`; at bind the fn-ptr could not be rebuilt,
`__fn_` would not swizzle, and pass 2's `if (!ok) continue;  // defensive: the
closure should have dropped it` dropped the aggregate **silently**. Members
flatten from bases → **11 aggregates** died on one member.

**Lesson worth keeping: two optimistic assumptions met one silent failure.**
`arena_chain_ok` returns true for a `DK_FPTR` chain unconditionally ("the target
signature never gates member layout"), so the closure never dropped it; and the
fill's own bail had no diagnostic. Neither was wrong alone.

## 4. What to do next (recommended order)

1. **#63 — gate the pack's silent degradation.** The measurement now exists
   (this release added it). Two halves, different strictness:
   - pack-time parse errors → **ratchet** against a baseline of **58** (NOT
     339/317 — those came from a loose `grep -c error` that also matched
     `filesystem_error` in the deliberate `pack drop:` lines; `grep -c 'error:'`
     gives 58 in ~17 classes, a finite burndown: 15 lambda-parsing, 10
     `__atomic_is_lock_free`, 8 facet member lookup, 6 `__ninp_`, ~19 tail).
   - load-side counters → **hard zero, no baseline**: `materialize fill:
     DROPPED` and `materialize derived: UNRESOLVED … kind=0`. A `kind=0` is a
     `DK_NONE` — an id stamped but never recorded — which has no legitimate
     instance. Both are at 0 today. **Had this gate existed, #64 would have
     failed the pack the day it was introduced instead of shipping.**
2. **#60 — the macOS headerless cell** (`sandbox-exec` denying the SDK subpath;
   no `/usr/include` since 10.14). It is #63's consumer-side proof: on a Mac
   with Xcode a degraded unit is silently rescued by live-parsing the SDK, which
   is exactly why this class stays invisible.
3. Then: #25 per-unit lazy decompression, #61 pointer-to-member-function
   remainder, #56 follow-ons (class element types in a braced list, braced lists
   as call arguments, range-for), #55, #49 branch housekeeping.

## 5. Process lessons paid for this session

- **A stale binary reads as a compiler bug.** `headerless-win` reported a real
  1010/1 failure against a PE built eight hours earlier, before the fix under
  test. Fixed the mechanism (`be78c47e`): `packed`, `headerless` and
  `headerless-win` each run their own make target first, and `release-win` got a
  stage. Freshness is the Makefile's job, not the caller's memory.
- **Check the file before "fixing" a stale skill.** `/promote`'s obsolete MIR
  fork step was gone from `.claude/commands/promote.md` (removed in `7fbfa061`);
  what carried the stale text was this session's pre-compaction copy of the
  skill. A restart picks up the file. Nothing needed changing.
- **Count with the right pattern.** `grep -c error` vs `grep -c 'error:'` was a
  5.5× overstatement that would have made #63 look unfinishable.
- **A `continue` with no diagnostic is where a week goes.** The three added this
  release (`materialize fill: DROPPED`, `materialize derived: UNRESOLVED`,
  `forest_restore_decls: SKIPPED`) turned the hunt into minutes.

## 6. Standing constraints (do not relearn these)

- The **QNAP NAS never builds or tests**. Everything goes through
  `scripts/remote_build.sh` (container, `ssh -p 2299 dev@localhost`). One heavy
  container job at a time — the script refuses concurrent runs.
- **Push only to `derekbsnider/*`** — verify the remote URL first.
- No `&&` chains or shell variable substitution in a single Bash tool call.
- Never `git add -A`; untracked files in this tree are usually leftovers
  (`donut.c`, `test.mad`, `testsort.mad` are present and are NOT ours to commit).
- Scratch/reducer files go in `tmp/` (gitignored).
- Every `src/`/`include/` commit carries `Hypothesis:` / `Layer:` / `Searched:` /
  `Oracle:`; `scripts/check-rule-trailers.sh` gates it (432/0 as of `e9e92af9`).
- **Do not re-run suites on already-green content** — but note the exception
  this release hit: `b27c8ed4` changed the freeze FORMAT
  (`DF_ENUM_CLASS_NESTED`), so every forest had to be re-packed and no earlier
  green run covered the new tree. A format change invalidates prior greens.
