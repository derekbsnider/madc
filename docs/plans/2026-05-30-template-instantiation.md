# madc Template Instantiation — design (2026-05-30)

**Status:** design / research grounded. NOT yet implemented. This supersedes any
"retire the container wrappers → call mangled libstdc++ symbols" note — that is
IMPOSSIBLE for vector/map/set (see Finding 2) and was a wrong turn.

## Findings (researched 2026-05-30, gcc canon)

1. **How g++/clang instantiate (Borland model).** Parse the template definition
   once; on each concrete use, *monomorphize* — substitute the concrete type
   through the template AST → a concrete instantiation → codegen per-TU as a
   weak/COMDAT symbol; the linker dedups. No subprocess, no wrappers — the body
   that lives in the header is instantiated. (Cfront's older "repository model"
   was abandoned as slow/fragile — see docs/research/cfront-template-instantiation.md.)

2. **libstdc++ export reality (verified via `nm -D` + `g++ -O0 -S`).**
   - `std::__cxx11::basic_string` is **explicitly instantiated** in libstdc++ →
     its members ARE exported (`_ZNKSt7__cxx11…4sizeEv` etc.). madc calls them
     DIRECT via mangled symbols (the dlsym(_Z) resolver binds them). DONE
     (commit 947dd96, generated by the substitution-aware mangler).
   - `std::vector<T>`/`map`/`set` members are **inline templates** — NOT exported
     (`nm` count 0 for `vector::push_back`/`operator[]`). Only type-erased leaves
     are exported (`_Znwm` operator new, `_ZdlPv`, `_ZSt…__throw_*`). So they
     **cannot** be dlsym'd; they MUST be instantiated by madc.
   - `v.push_back(x)` (x an lvalue) → `…9push_backERKl` (const T&); a literal
     rvalue → `…9push_backEOl` (T&&). `operator[]`/`at` return `T&` (a pointer →
     deref for read, store for write). These ABI facts matter when emitting bodies.

3. **madc has NO template support today.** `template` is only a reserved word
   (tokens.h). `vector<int>` is a hardcoded TokenVECTOR keyword that builds a
   DataDefVECTOR (sized `sizeof(std::vector)`) and registers `ns_stl` C++ method
   functions. There is no `include/madc/vector` header. `ns_stl.cpp` wraps a real
   `std::vector` — but per the rule "wrappers are ONLY for libmadc's own
   functions, not for wrapping stdlib," that is the wrong vehicle for the user's
   `vector<T>`.

## Design — implement real template instantiation in madc

### A. Front-end: parse template definitions
Parse `template<typename T[, ...]> struct/class Name { members...; methods...; }`
in madc source into a template AST node that carries its type parameter(s) and
the un-substituted member/method bodies (tokens + parse subtree, per MC11-IR).
Keep it minimal first: single type param, class/struct with data members + methods.

### B. Instantiation on use (Borland monomorphize)
On `Name<ConcreteT>`: look up the template, substitute the type parameter(s)
through a CLONE of the member/method parse subtrees (T → ConcreteT everywhere:
member types, param types, locals, sizeof, casts), producing a concrete
DataDefCLASS-like type `Name<ConcreteT>` + concrete FuncDefs for each method.
Cache by mangled instantiation name (dedup). This reuses the class-model lowering
already on CIR (class → struct, method → ClassName__method(&this,args), ctor/dtor
via cleanup attr) — an instantiation is just a generated class.

### C. The standard container templates live in madc-dialect headers
Create `include/madc/vector`, `.../map`, `.../set` (embedded, like the existing
`include/madc/string`, `.../iostream`) written in the madc dialect — a SIMPLE,
self-contained implementation (e.g. `T* data; size_t len, cap;` + push_back via
realloc, `operator[]` returns `data[i]`, etc.) that c2mir can compile after
instantiation, calling exported leaves (malloc/realloc/free or operator new,
memmove, __throw). This is madc's own STL — NOT libstdc++'s vector, NOT a wrapper.
`#include <vector>` pulls in the template; `vector<int> v;` instantiates it.

### D. Retire the container keyword/ns_stl path
Once instantiation works, the hardcoded TokenVECTOR/MAP/SET keywords + ns_stl.cpp
are replaced by: `<vector>` defines `vector<T>` as a normal madc template; the
generic template-instantiation path handles it. ns_stl.cpp is deleted (it was a
stdlib wrapper, disallowed). Keep tests green throughout (testvector/map/set/
subscript/foreach + testmadc_ns) — convert incrementally, type by type.

## Sequencing (careful, validated, no regression from 367)
1. Template parse (template<typename T> class) — parser + a template registry. TDD.
2. Instantiation engine: substitute + clone → concrete class + FuncDefs; cache.
   Reuse class-model CIR lowering for the generated class.
3. `include/madc/vector` (madc-dialect) → instantiate `vector<int>`/`<string>`;
   make testvector pass via the template path (parallel to ns_stl, behind a guard).
4. map/set headers; flip the keyword path off; delete ns_stl. Validate each step.

## What stays mangled-direct (NOT templated by madc)
std::string (and ostream/istream/cout/cin) — libstdc++ exports them; madc calls
them directly via the mangler. See [[project_cpp_mangled_direct]].
