# Release v0.32.0 — 2026-07-01

The rung-1 interning capstone: madc's most numerous token (bare identifiers) no
longer carries a per-token `std::string` — spelling lives in the interned
`madc::dis` pool as a 4-byte id. This session also root-caused the dominant
template-instantiation (tsubst) re-parse fallback, retargeting the path toward
deleting re-parse (the g++ parse-once parity milestone).

## Highlights

- **Dropped `TokenIdent::str`** — bare identifiers carry a 4-byte interned
  `spelling_id` (via `TokenBase::_active_strpool`) instead of a 32-byte
  `std::string`. `spelling()` is virtual; content/alias subclasses keep their own
  `str`. Measured **−43% token `std::string` constructors, −3.3% total instructions**.
- **Scope-lookup + finalize interning caches** (`Variable::name_sid`, per-file
  `file_id`) — **−6.6% instructions, hashing −64%** (deterministic callgrind).
- All gated: fulltest **673/0/0/16**, gcc-torture failset byte-identical to the
  51-name baseline, production byte-identical throughout.
- **tsubst burndown insight** — the dominant `[why: template-id '<' in body]`
  fallback class (78%) is a *first-blocker* tally masking the dependent-member-type
  (`::type` / rebind) resolution gap; gdb pinned it to `std::__and_`'s dependent
  base degrading to `"auto"`. Retargeting recorded in
  `docs/plans/2026-07-01-templateid-gate-insight-HANDOFF.md`.

See [CHANGELOG.md](../../CHANGELOG.md) for full details.
