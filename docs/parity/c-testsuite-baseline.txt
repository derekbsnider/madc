# c-testsuite known-fail baseline (scripts/c_testsuite_lane.sh ratchet).
# One test filename per line; # comments allowed. The lane is RED on any
# failure NOT listed here, and reports loudly when a listed test passes so
# this file only ever shrinks. Seeded 2026-08-28 from the first-ever run:
# 198/220 passed at feature/win-vt-tui-claude @1add3d11 (bin/madc, JIT).
#
# Triage classes (signatures from the seeding run):
#
# -- wrong output / silent-wrong-answer class (highest priority follow-up) --
# 00204: FIXED 2026-08-28 — MIR va_block_arg case 2 (all-SSE block) had no
#        register-exhaustion check; gate tests/testvastructfpexhaust.mad.
# 00210: FIXED 2026-08-28 — the generic expression-fptr call arm now accepts
#        a TokenCast receiver (any ((T(*)())p)() shape, attr'd or not);
#        gate tests/testcastfnptrcall.mad.
# 00205: FIXED 2026-08-28 — three stacked defects: exponential operator
#        datadef() recursion (the hang; gate tests/testaddchaindepth.mad),
#        unary plus under a cast, and flat-initializer struct-array element
#        count (both gated by tests/teststaticinitflat.mad).
#
# -- parse gaps: declarators --
# 00089: typedef struct S * (*fty)();  (fn-ptr typedef returning struct ptr)
00089.c
# 00124: int (* (*p)(int a, int b))(int c, int d) = f1;  (fn-ptr returning fn-ptr)
00124.c
# 00130: char arr[2][4], (*p)[4], *q;  (array-pointer declarator mid-list)
00130.c
# 00162: FIXED 2026-08-29 — skip_param_array_qualifiers (both param
#        bracket scans) + the plain parenthesized pointer param arm
#        (gate: testparamarrayqual)
# 00170: FIXED 2026-08-29 — `enum TAG` elaborated-specifier arm in
#        resolve_declared_type_token + parseFnPtrParams enum arm
#        (gate: testenumtagtype)
# 00209: FIXED 2026-08-29 — abstract fn-type params (6.7.6.3p8
#        adjustment) at both param grammars, array-of-fnptr typedef +
#        abstract param (one CArray shape), CArray-of-FPTR typedef
#        emission (gate: testabstractfnparam)
#
# -- parse gaps: statements/expressions --
# 00038: if (sizeof 0 < 2)  (sizeof unary-expr without parens)
00038.c
# 00051: switch (x) case 0: ...  (switch with non-compound body)
00051.c
# 00103: return **(int**)bar;  (double-deref of cast)
00103.c
# 00120: enum { X } x;  (anonymous enum declarator in struct scope)
00120.c
# 00143: FIXED 2026-08-28 — nested case labels become dispatch-only entries
#        (in_place_label) with a top-of-switch goto jump table; gate
#        tests/testduffdevice.mad.
# 00150: {[0] = 1, 1+1}  (designated initializer then positional)
00150.c
# 00152: #error inside a skipped conditional group still diagnosed
00152.c
# 00213: the Duff-shaped part is FIXED (nested labels + pre-case statements
#        parse); remaining root is DIFFERENT: a statement expression whose
#        last statement is a labeled while-loop inside a dead ternary branch
#        — c2mir check "last statement in statement expression is not an
#        expression" (00213.c:17). Stmt-expr value-semantics corner.
00213.c
# 00219: _Generic with function designators / const-qualified association
00219.c
#
# -- CIR/c2mir rejections --
# 00053: c2mir_compile_tree: 3 check errors
00053.c
# 00095: c2mir_compile_tree: 2 check errors
00095.c
# 00202: c2mir_compile_tree: 2 check errors
00202.c
# 00216: cir error: unhandled expression: TokenBase (token type 0)
00216.c
