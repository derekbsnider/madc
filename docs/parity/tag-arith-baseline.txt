3
# tag-arithmetic retirement burndown baseline (scripts/tag_arith_burndown.sh --check).
# Count of EXTERNAL raw-tag sites (rtPtr/rtRef/rtDePtr/rtDeRef construction +
# raw 10000/20000 comparisons) outside the core encoding in include/datadef.h.
# Lower this number in the SAME commit that migrates a site. When it reaches 0,
# a final commit removes the core encoding from datadef.h and this gate becomes
# a finish-line check. Plan: docs/plans/2026-06-30-tag-arithmetic-retirement-plan.md
#
# 25 (2026-06-30, Phase 0): initial baseline.
# 3  (2026-06-30, Cluster A done): the 18 builtin-signature rtPtr() sites
#    migrated to ptr_of(ddX) (structural DataDef + RefType, resolved via
#    getPointerType — representation-identical to the dtXptr tag). Remaining 3 =
#    Cluster B (same_representation: rtDeRef @ 9055/9057, raw >= 20000 @ 13127),
#    the === comparator built around the tag encoding's corner cases — coupled to
#    the final core-removal phase (it compares tag-only types), NOT independent.
