# Forest-pack degradation ratchet baseline (task #63).
# Enforced by scripts/forest_pack_gate.sh, which each pack script calls on its
# OWN run: scripts/forest_pack.sh (linux), forest_pack_windows.sh (win64),
# forest_pack_darwin.sh (darwin, once per arch against the same key).
#
# Format: <count> <profile> <metric>
#
#   pack-errors    anchored madc diagnostics (': error: ' on the ANSI-stripped
#                  stream) in ONE pack/freeze run. Every one is a header
#                  construct madc could not parse; the gate prints the class
#                  breakdown, which IS the burndown list.
#   dk-none        'materialize derived: UNRESOLVED … kind=0' in that profile's
#                  -v load probe: a project id whose record is DK_NONE, i.e.
#                  stamped by the freeze and never written. A CENSUS, because
#                  most instances are legitimate — a pointer to a template
#                  parameter has no concrete record by construction, and the
#                  consumer re-instantiates from the pattern. A CONCRETE type
#                  here is task #64's shape and is a defect.
#   mir-blob-skips 'blob skipped' in the pack log: the MIR module-cache blob
#                  failed to compile and was dropped with "consumers fall
#                  back". Correctness survives — which is why the pack exits 0 —
#                  but every consumer compile then pays full price. linux and
#                  win64 have no line here, so they are gated at ZERO (they
#                  pack 467KB / 497KB blobs). darwin is at 1 per arch and both
#                  causes are known; see the plan doc §8.
#   closure-drops  'materialize closure: DROPPED' in the same probe — records
#                  whose chain arena_chain_ok refuses to record. Designed
#                  behaviour, so it ratchets; the container carrying LESS than
#                  it did is the regression.
#
# A MISSING line means ZERO allowed, so a new profile or metric cannot appear
# with room to spare. Counts may only go DOWN: lower them in the same commit
# that fixes a class. The gate prints the exact new number when a run comes in
# under baseline.
#
# NOT here, deliberately: 'materialize fill: DROPPED' and
# 'forest_restore_decls: SKIPPED' are HARD ZERO with no baseline. Each is a
# record the consumer ADMITTED and then could not use, a bound unit is reported
# SERVED either way, and neither has a legitimate instance. `fill: DROPPED` is
# the counter task #64 would have tripped.
#
# darwin has pack-errors only: a cross-freeze cannot run its own consumer, so
# its load probe lives in scripts/mac_battery.sh leg 3c (hard-zero halves
# gated, census reported) and its numbers are recorded in
# docs/plans/2026-08-17-pack-degradation-gate.md.
#
# Measured 2026-08-17 (task #63, at v0.83.0 content + the gate):
#   linux  93 pack-errors — 51 __cerb, 10 max_size, 12 _M_dispose, tail
#   win64  93 pack-errors — same corpus (mingw ships libstdc++ 13.2)
#   darwin 58 pack-errors — identical on arm64 and x86-64: 15 lambda parsing,
#          10 facet member lookup, 4 static_cast tail (libc++, a different
#          corpus from the two above — they share almost no classes, which is
#          why one number for all three would have been 35 free slots)
#   dk-none 55 on linux AND win64, all of them DEPENDENT operands (_Up/_Tp1/
#          _Tp/_ValueT). It was 57 before this gate landed: the other 2 were
#          `ptr long double*` and they were REAL — slot 18 stayed unpinned
#          after real long double landed, so a struct with a `long double`
#          member LOST that member at bind. Pinning it also cleared every
#          closure drop (linux 1 -> 0, win64 3 -> 0): those dropped records
#          were the long-double-bearing ones, so the container now carries
#          MORE than it did. Gated by forest_bind_gate [ldouble].
68 linux pack-errors
45 linux dk-none
0 linux closure-drops
68 win64 pack-errors
46 win64 dk-none
0 win64 closure-drops
# darwin 64 -> 58 (2026-08-24): the six libc++ <filesystem> "no matching
# overload ... error_code**" entries (raised 58 -> 64 on 2026-08-23 when the
# strict no-viable-overload refusal replaced their silent wrong-function
# fallback) are FIXED at the type's origin: '&x' over a reference-typed
# operand now types as pointer-to-referent (Program::addressof_result_type,
# [expr.unary.op]p3), so `__f(__p, &__ec)` from an `error_code& __ec`
# parameter ranks as error_code* and binds the right overload. Reducer:
# tests/testaddrofrefoverload.mad (g++/clang++ oracle). KG Gap
# libcxx_fs_error_code_overload_shape CLOSED.
#
# darwin 58 -> 64 (2026-09-01, the 2026-08-23 precedent again): six
# "Malformed expression: 2 operands with no operator between them" —
# the s149b operand-juxtaposition wall made a PRE-EXISTING
# instantiation-body misparse LOUD. The shape: re-parsing a saved
# pattern body containing `TemplateId<...>()(args)` (libc++
# `__murmur2_or_cityhash<size_t>()(&__u, sizeof(__u))` in
# __functional/hash.h __scalar_hash<_Tp,2>::operator(), and five
# unique_ptr instantiation bodies) mis-consumes the empty ctor parens,
# leaving the TYPE and the parenthesized call-args as two stranded
# operands; the instantiated type name even absorbs the UNSUBSTITUTED
# defaulted non-type arg (…_sizeof__Size_P8). Before the wall this
# SILENTLY resolved to the top operand — the grove body computed
# sizeof(__u) instead of the hash — so loud-plus-fallback is strictly
# better than what 58 was hiding. Burndown item: fix the
# instantiation-body re-parse of TemplateId<...>()(args) at the
# template machinery layer, then lower this to 58. Repro: a darwin
# cross-freeze of `#include <memory>` (bin/madc-arm64-macos
# --freeze-append) shows the hash.h entry; the six appear in the full
# manifest freeze. KG Gap: instantiation_body_templateid_ctor_call.
# darwin, NATIVE self-freeze (darwin-host port D2, 2026-09-02): 54. Same
# libc++ text (pinned: scripts/fetch_libcxx_headers.sh), same 835 units with
# identical token counts — the 10-error gap is the CROSS freezer's own: its
# undeclared-identifier fallback probes the BUILD HOST's dynamic symbols, and
# the Linux host has no __atomic_is_lock_free (libatomic is not loaded) where
# libSystem does; ten __atomic_base<T>::is_lock_free instantiations are error
# husks in a container-frozen forest and resolve in a natively frozen one (KG
# Gap cross_freeze_host_dlsym_leak). The baseline stays the CROSS number
# while the container lane is canonical; a native lane reports IMPROVED.
# darwin 64 -> 66 (2026-09-02, darwin-host D4 wave 2): the long/long long
# IDENTITY fix (ddINT64/ddUINT64 carry their target C spelling as canonical
# where their display name is another type's source spelling; the class-scope
# typedef arm compares the source spelling to the dd's IDENTITY spelling, not
# its display name). Measured by freezing the same corpus with the pre-fix
# freezer (a HEAD worktree) and the post-fix one and diffing the error
# histograms: ONE line differs — `__atomic_is_lock_free` 10 -> 12. That is the
# cross-freezer dlsym leak documented above (KG Gap cross_freeze_host_dlsym_leak),
# reached by two more instantiations: __atomic_base<long>::is_lock_free and
# __atomic_base<unsigned long>::is_lock_free are now DISTINCT from the
# long long ones instead of aliasing them, exactly as the type model says.
# A native darwin freeze (libSystem has the symbol) reports lower. Every
# other class is byte-identical to the 64 run.
# linux 93 -> 68, dk-none 55 -> 45; darwin 66 -> 52 (2026-09-02, darwin-host
# D4 wave 3, the wchar_t identity slice — five commits): wchar_t/char16_t/
# char32_t are DISTINCT dds (the ctype<wchar_t> facet class: 4 linux + 6
# darwin); the served C headers' wchar_t typedef is guarded #ifndef
# __cplusplus (embedded stddef.h; the darwin prelude generator restores
# Apple's guard the -x c flatten resolved away) and a typedef may not
# redeclare a keyword type — the shadow that keyed every DERIVED wchar_t
# spelling int32_t* (basic_ostream<int32_t, char_traits<wchar_t>>,
# numpunct_int32_t, the reverse_iterator shared by basic_string_view<wchar_t>
# and vector<int>: mixed wide keys linux 87 -> 0, darwin 113 -> 0); the
# overload scorer compares scalar identity, not rawtype; and an explicit
# specialization on a pointer to a builtin (N<int*>) keys through the one
# builtin table. win64 measured by its own release-win pack run in the wave-3
# battery (2026-09-03): 93 -> 68 pack-errors, dk-none 55 -> 46 — the same
# libstdc++ corpus as linux (mingw ships 13.2), the same classes cleared.
# darwin 49 -> 52 within the same wave (the 64 -> 66 precedent again): once
# the one builtin table names long double and __int128 by their own dds,
# hash<long double>, hash<__int128> and hash<unsigned __int128> stop
# aliasing hash<double> / hash<long> / hash<unsigned long> and instantiate
# their OWN bodies — the 16-byte `__scalar_hash<_Tp, 2>` partial
# specialization (hash.h:273), whose `__murmur2_or_cityhash<size_t>()(...)`
# is the KNOWN instantiation-body misparse (KG Gap
# instantiation_body_templateid_ctor_call, 5 -> 8 instances). The three new
# instances are correct type identity meeting an old burndown item, not a
# new class; every other line is identical to the 49 run. The cross freezer
# gives long double the Linux host's 16 bytes — a native darwin arm64 freeze
# (8-byte long double) reports one fewer.
# darwin 52 -> 48 (2026-09-03, darwin-host D4 wave 6 close-out): the four
# "'X' is a private member of 'X'" entries are FIXED at the access rule —
# a lambda (a hoisted nested function) written inside a member has that
# member's access ([class.access]/2; access_context_class walks the compound
# chain to the enclosing member), so libc++ bodies whose mem-init lambdas
# read private members (basic_string's move constructor `__s.__r_`) now
# freeze. Every other class is identical to the 52 run; the four "Expecting
# ] in lambda expression" lines are the capture forms madc does not parse
# yet (`[=]`, `[this]`, named captures — KG Gap lambda_capture_forms), the
# next lambda burndown item. Reducer: tests/testlambdamemberaccess.mad.
48 darwin pack-errors
1 darwin mir-blob-skips
