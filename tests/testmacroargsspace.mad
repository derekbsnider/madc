#!/../bin/madc
#include <stdio.h>
#include "macro_args_space_outer.mah"

char *get_hint(int level)
{
    if ( level > 0 )
	return "high";
    return "low";
}

int main()
{
    printf("%s\n", get_hint(1));
    return 0;
}
