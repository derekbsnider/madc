pins -stdlib=libc++ in .flags, and this lane serves only the pack, whose flavor is libstdc++ (the Linux and Windows artifacts). Wrong lane, NOT a libc++ limitation: the macOS artifact packs libc++, and these must PASS on its headerless lane.
