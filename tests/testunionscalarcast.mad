typedef unsigned long int st;
typedef unsigned long long dt;
typedef union
{
	dt d;
	struct
	{
		st h, l;
	} s;
} t_be;

int main()
{
	t_be x;
	x = (t_be)0x100000000ULL;
	if (x.d != 0x100000000ULL)
		abort();
	puts("ok\n");
	return 0;
}
