#include <stdio.h>

_Static_assert(sizeof(int) == 4, "int should be 4 bytes (GCC parity)");
static_assert(alignof(double) == 8);
static_assert((1 << 4) == 16);

int main()
{
    printf("static-asserts-ok\n");
    return 0;
}
