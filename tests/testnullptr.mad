#include <stdio.h>

int main()
{
    char *name = nullptr;
    int *nums = nullptr;

    if ( !name )
        printf("name-null\n");

    if ( !nums )
        printf("nums-null\n");

    name = "ok";
    printf("%s\n", name);

    return 0;
}
