libc++ domain (linux -stdlib=libc++ lane and the darwin suite): the map-of-map insert `mm[1][7] = 70` (std::map<int, std::map<int,int>>) hits a pre-existing gap in the parse-once deferred-construction relowering of libc++'s allocator::construct — a pair<const int,int> element meets a selected ctor whose formal is int64 ([RELOWER-UNSUP] elem-formal-mismatch; "tsubst: class deferred-construction object argument pack" at __memory/allocator.h:172), fails at 779bb654 too, and is unrelated to the <list> completion site this test also exercised; KG Gap libcxx_nested_map_value_type_construct_relower, reducer in docs/plans/2026-09-01-darwin-host-port.md (WAVE 12); the fix follows this wave, then the fixture goes.
