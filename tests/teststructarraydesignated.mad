#include <stdio.h>

struct s {
	int a[3];
	int c[3];
};

struct s s1 = {
	.c = { 1, 2, 3 }
};

struct s s2 = {
	c: { 4, 5, 6 }
};

int main(void)
{
	printf("%d %d %d %d %d %d\n",
		s1.a[0], s1.a[1], s1.c[0],
		s2.a[0], s2.c[0], s2.c[2]);
	return 0;
}
