#include <iostream>
using namespace std;
long addr_of(int &r) { return (long)&r; }   // &r must be the referent's address
int main() {
    int x = 5;
    cout << (addr_of(x) == (long)&x) << endl;   // 1
    return 0;
}
