Presence enables the check: the JIT run must produce EMPTY stderr. A generated
iterator loop that mis-declares its iterator local, calls the POSTFIX
operator++ (the same spelling with a dummy int), or dereferences a discarded
reference result would warn here rather than fail an output comparison.
