Structurally JIT-only: the test spawns the LANE'S OWN madc as the external MCP server ({madc} -> madc::compiler_path()). In a native artifact the running program is the test itself, not a compiler, so the child would be a copy of the test.
