extern "C" int printf(const char *, ...);
typedef int M[2][3];
int main() {
    M m;
    for (int i = 0; i < 2; ++i) for (int j = 0; j < 3; ++j) m[i][j] = i * 10 + j;
    printf("sz:%d e:%d d:%d\n", (int)sizeof(M), m[1][2], (int)(&m[1][2] - &m[0][0]));
    return 0;
}
