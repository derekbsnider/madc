#!/usr/bin/env madc
#include <iostream>
#include <string>
#include <ns_php>
#include <ns_ruby>
using namespace std;

int main()
{
	array chars;
	string s = "Mad";
	ruby::chars(chars, s);
	cout << php::count(chars) << endl;
	return 0;
}
