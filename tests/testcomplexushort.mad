extern void abort(void);

struct s { _Complex unsigned short x; };
struct s gs = { 100 + 200i };

struct s foo(void)
{
	return gs;
}

int main()
{
	if (foo().x != gs.x)
		abort();
	puts("ok");
	return 0;
}
