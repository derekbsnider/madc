negative include control (M_PI must be UNDECLARED without <math.h> / import m): the darwin hosted build serves every standard C include from ONE flattened umbrella (scripts/gen_darwin_prelude.sh — a stub per header name pulls __madc_darwin_prelude.h, run-only Macs having no guaranteed SDK), so <stdio.h> alone already declares M_PI here where Apple clang rejects it; KG Gap darwin_umbrella_prelude_whole_set_visibility (fix = per-header scoping of the prelude, a darwin-host-port slice); the Linux and win64 lanes hold the control
