int main()
{
    int ch;
    int line_start = 1;
    while ( (ch = getchar()) != -1 )
    {
        if ( line_start )
        {
            printf("child: ");
            line_start = 0;
        }
        putchar(ch);
        if ( ch == '\n' )
            line_start = 1;
    }
    return 0;
}
