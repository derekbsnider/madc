GCC constant-folds the overflowing float->int casts, saturating to INT_MAX (verified: gcc -O0 prints 2147483647; the .expect IS canon); c2mir converts at runtime (cvttss2si -> INT_MIN). Old "legacy saturates" framing wrong — GCC itself saturates via front-end folding. Fix = fold these constant casts in madc's fold spine with GCC semantics (follow-on task filed); then this test lifts. Audit 2026-07-19.
