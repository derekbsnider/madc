void abort(void);
void exit(int);

__complex__ double f(__complex__ double x, __complex__ double y)
{
  x += y;
  return x;
}

int main(void)
{
  __complex__ double a, b, c;

  a = 1.0 + 1.0i;
  b = -2.0 + 2.0i;
  c = f(a, b);

  if (c != -1.0 + 3.0i)
    abort();

  exit(0);
}
