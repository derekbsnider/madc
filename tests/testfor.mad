#!../bin/madc

int main()
{
    uint32_t c;
    uint32_t a = 0;

    for ( c = 10; c; --c )
    {
	if ( a == 5 )
	{
	    printstr("a == 5");
	    break;
	}
	++a;
	puti(a);
    }

    int i;
    for ( i = 0; i < 5; i++ )
	puti(i);

    for ( i = 5; i > 0; i-- )
	puti(i);

    return 0;
}
