#include <stdio.h>

int main() {
    int *p = (int []){10, 20, 30};
    printf("%d %d %d\n", p[0], p[1], p[2]);

    // Explicit count
    int *q = (int [3]){40, 50, 60};
    printf("%d %d %d\n", q[0], q[1], q[2]);

    return 0;
}
