void abort (void);
void exit (int);

int count = 0;
int dummy;

static int *
bar (void)
{
  ++count;
  return &dummy;
}

int
main (void)
{
  asm ("" : "+r" (*bar ()));
  if (count != 1)
    abort ();
  puts("ok\n");
  exit (0);
}
