#include <stdio.h>

int main()
{
    int b = 1;
    int *c = &b;
    if (*c)
        *c = 1 % (unsigned int) *c | 5;
    int d = 1;
    int *p = &d;
    if (*p)
        *p = (1 % (unsigned int) *p) | 5;
    printf("%d %d\n", b, d);
    return 0;
}
