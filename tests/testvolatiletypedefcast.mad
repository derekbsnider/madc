#include <stdio.h>
#include <stdlib.h>

typedef struct {
    unsigned short a;
    unsigned short b;
} foo_t;

int main() {
    volatile foo_t *foo_p = (volatile foo_t *)malloc(sizeof(foo_t));
    foo_p->b = 0x0100;
    printf("%d\n", foo_p->b);
    return 0;
}
