#!/../bin/madc

extern void abort(void);

int main(void)
{
    int x = 17;
    int y = 0;
    int *(p[2]);

    p[0] = &x;
    p[1] = &y;
    p[1][0] = p[0][0];

    if (y != 17)
	abort();

    puts("ok");
    return 0;
}
