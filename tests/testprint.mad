#!../bin/madc
#include <iostream>
using namespace std;

void print(string &s)
{
    printstr(s);
//    puts(s);
}

int main()
{
    string hello = "Hello, World!";

//    printstr(hello);
    print(hello);

    return 0;
}
