cleanup calls php::unlink -> libc `unlink`, which win64 UCRT does not export (only `_unlink`); a MIR link-time undefined-import, same php::unlink-on-win64 gap testsmaug_requests skips. The V5 correlation/viewsync logic itself is win64-clean (verified: the test passes all 8 checks under wine with the unlink cleanup removed).
