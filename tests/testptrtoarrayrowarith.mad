#!/../bin/madc

#include <stdio.h>

int main(void)
{
    int a[1][4] = { 11, 12, 13, 14 };
    int (*p)[4] = a;
    int sum = 0;
    for (int i = 0; i < 1; i++)
	for (int j = 0; j < 4; j++)
	    sum += *(*(p + i) + j);
    printf("%d\n", sum);
    return 0;
}
